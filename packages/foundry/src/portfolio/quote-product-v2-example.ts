import type { SourceFile } from './products.ts';

/** Development-assistant reference/demo only. Never seed a live task with this
 * implementation or attribute its source to an actual-model runtime attempt. */
export function quoteProductV2Example(): SourceFile[] {
 return [{path:'app.html',content:APP}];
}

const APP=String.raw`<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Quote desk</title>
<style>
:root{font:16px/1.5 system-ui,sans-serif;color:#203c36;background:#f5f3eb;--green:#175b49;--line:#d9dfd5;--muted:#61766d}*{box-sizing:border-box}body{margin:0}button,input,select,textarea{font:inherit}button{min-height:40px;padding:9px 15px;border:1px solid var(--line);border-radius:9px;background:white;color:#234d40;cursor:pointer;font-weight:650}button:hover{border-color:#658e79;background:#f2f8f0}button:disabled{cursor:wait;opacity:.55}button.primary{background:var(--green);border-color:var(--green);color:white}button.primary:hover{background:#104836}button:focus-visible,input:focus-visible,select:focus-visible,textarea:focus-visible{outline:3px solid #daa849;outline-offset:2px}input,select,textarea{width:100%;min-width:0;min-height:42px;padding:10px;border:1px solid #b6c5b7;border-radius:8px;background:white;color:#183e32}input[aria-invalid=true]{border-color:#ab432a}label{display:block;font-size:.83rem;font-weight:650}label span{display:block;margin-bottom:5px}fieldset{padding:0;border:0;margin:0;min-width:0}h1,h2,h3,p{margin:0}h1{font-size:clamp(2rem,5vw,3.25rem);line-height:1.1;letter-spacing:-.055em}h2{font-size:1.3rem;letter-spacing:-.03em}h3{font-size:1.1rem;overflow-wrap:anywhere}small,.muted{color:var(--muted)}.topbar{border-bottom:1px solid var(--line);background:#fffef9}.topbar-inner{max-width:1240px;margin:auto;padding:18px 28px;display:flex;justify-content:space-between;align-items:center;gap:14px}.wordmark{font-weight:800;letter-spacing:-.03em;font-size:1.2rem}.dot{display:inline-block;width:10px;height:10px;border-radius:50%;background:#8bb86b;margin-right:8px}.local{font-size:.8rem;color:var(--muted)}main{max-width:1240px;margin:auto;padding:34px 28px 60px}.hero{display:flex;justify-content:space-between;align-items:end;gap:22px;flex-wrap:wrap;margin-bottom:24px}.eyebrow{font-size:.73rem;font-weight:750;letter-spacing:.15em;text-transform:uppercase;color:#6b806b;margin-bottom:12px}.hero p:last-child{margin-top:12px;max-width:560px;color:var(--muted)}.summary{text-align:right;min-width:150px}.summary strong{display:block;font-size:1.7rem;font-weight:600;letter-spacing:-.04em}.summary small{display:block}.layout{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:24px;align-items:start}.panel{background:#fffefb;border:1px solid var(--line);border-radius:16px;padding:24px;min-width:0}.panel-head{display:flex;justify-content:space-between;align-items:center;gap:12px;margin-bottom:22px}.badge{font-size:.72rem;border:1px solid #cbd9c7;background:#edf4e8;border-radius:30px;padding:4px 9px;white-space:nowrap}.customer{margin-bottom:20px}.section-label{font-size:.8rem;color:var(--muted);margin-bottom:9px}.line{display:grid;grid-template-columns:minmax(0,1fr) 72px 100px 35px;gap:9px;padding-bottom:15px;margin-bottom:15px;border-bottom:1px solid #e5e9e0;align-items:end}.line label{min-width:0}.remove{padding:0;min-height:42px;font-size:1.2rem;color:#6e7667}.line-actions{display:flex;justify-content:space-between;gap:10px;align-items:center;flex-wrap:wrap}.line-actions small{font-size:.76rem}.draft-total{margin:22px 0;padding:16px 0;border-top:1px solid var(--line);border-bottom:1px solid var(--line);display:flex;justify-content:space-between;align-items:center;gap:15px}.draft-total strong{font-size:1.8rem;letter-spacing:-.05em}.form-actions{display:flex;gap:10px;flex-wrap:wrap}.form-actions .primary{flex:1}.hint{font-size:.76rem;margin-top:14px;color:var(--muted)}#notice{min-height:28px;margin:0 0 20px;padding:12px 15px;background:#e8efe1;border:1px solid #d1dfc8;border-radius:10px;font-size:.85rem;overflow-wrap:anywhere}#notice.error{background:#fff0e8;border-color:#e3b29d;color:#8e351f}.records-head{display:flex;align-items:center;justify-content:space-between;gap:12px;margin-bottom:18px}.records-head span{font-size:.8rem;color:var(--muted)}article{border:1px solid var(--line);background:white;border-radius:12px;padding:18px;margin-top:14px}.record-top{display:flex;justify-content:space-between;align-items:start;gap:12px}.record-top>div{min-width:0}.record-top strong{white-space:nowrap;font-size:1.25rem;letter-spacing:-.04em}.record-id{font-size:.7rem;overflow-wrap:anywhere;margin:3px 0 11px;color:#718273}.work-lines{list-style:none;padding:0;margin:13px 0}.work-lines li{display:flex;justify-content:space-between;gap:12px;font-size:.84rem;border-top:1px solid #edf0e8;padding:8px 0}.work-lines li span:first-child{overflow-wrap:anywhere;min-width:0}.work-lines li span:last-child{white-space:nowrap;color:var(--muted)}.record-actions{display:flex;align-items:end;gap:8px;flex-wrap:wrap;margin-top:14px}.record-actions label{flex:1;min-width:140px}.record-actions button{font-size:.82rem}.status{font-size:.7rem;letter-spacing:.04em;text-transform:uppercase;color:#376044;background:#edf4e8;padding:4px 8px;border-radius:5px;display:inline-block}#empty-state{border:1px dashed #bbcbb5;border-radius:12px;padding:36px 22px;text-align:center;background:#f8faf3}#empty-state .empty-icon{font-size:1.7rem;color:#769060;margin-bottom:12px}#empty-state p{font-size:.9rem;color:var(--muted);max-width:280px;margin:8px auto 0}#export-panel{margin-top:24px}#export-data{min-height:140px;margin-top:12px;font:.8rem/1.5 ui-monospace,monospace;white-space:pre}#export-panel p{font-size:.85rem;margin-top:7px;color:var(--muted)}[hidden]{display:none!important}@media(max-width:850px){.layout{grid-template-columns:1fr}.summary{text-align:left}}@media(max-width:480px){main{padding:25px 16px 40px}.topbar-inner{padding:16px}.local{max-width:95px;text-align:right;font-size:.72rem}.panel{padding:18px}.line{grid-template-columns:minmax(0,1fr) minmax(0,1fr) 35px}.line .description-label{grid-column:1/-1}.hero{gap:18px}.summary strong{font-size:1.4rem}.summary small{display:inline;margin-right:8px}.record-top{flex-wrap:wrap}.record-top strong{font-size:1.2rem}.work-lines li{flex-wrap:wrap;gap:3px}.draft-total strong{font-size:1.6rem}}
</style></head><body data-ready="false">
<header class="topbar"><div class="topbar-inner"><div class="wordmark"><span class="dot"></span>Quote desk</div><span class="local">Your local work ledger</span></div></header>
<main><div class="hero"><div><p class="eyebrow">From estimate to next visit</p><h1>Quote the work.<br>Keep the job moving.</h1><p>Enter the customer and work below. Save a quote, reopen it to make changes, then turn accepted work into a tracked job.</p></div><div class="summary"><small>Open quote value</small><strong id="quoted-value">$0.00</strong><small id="record-count">No saved records</small></div></div>
<p id="notice" role="status" aria-live="polite">Loading your saved workspace…</p>
<div class="layout"><section class="panel" aria-labelledby="draft-heading"><div class="panel-head"><h2 id="draft-heading">New quote</h2><span id="draft-badge" class="badge">Draft</span></div><form id="quote-form" novalidate><fieldset id="form-fields" disabled><label class="customer" for="customer"><span>Customer or business</span><input id="customer" maxlength="200" placeholder="Who is the work for?" autocomplete="off" required></label><p class="section-label">Work &amp; materials · prices in USD</p><div id="lines"></div><div class="line-actions"><button id="add-line" type="button">+ Add line</button><small>Whole quantities · USD prices</small></div><div class="draft-total"><span>Quote total</span><strong id="draft-total">$0.00</strong></div><div class="form-actions"><button id="save" type="submit" class="primary">Save quote</button><button id="new-quote" type="button">New quote</button></div><p class="hint">Save to keep your work. Quotes and jobs stay available when you return. This local workspace does not send offers or collect payment.</p></fieldset></form></section>
<section class="panel" aria-labelledby="saved-heading"><div class="records-head"><h2 id="saved-heading">Saved work</h2><button id="export" type="button" disabled>Export CSV</button></div><div id="empty-state"><div class="empty-icon" aria-hidden="true">◇</div><h3>A clear desk. A new start.</h3><p>Your first saved quote will appear here. Add the real work and customer in the form to begin.</p></div><div id="quotes"></div></section></div>
<section id="export-panel" class="panel" hidden aria-labelledby="export-heading"><h2 id="export-heading">Your CSV export</h2><p>Copy this text into a .csv file or spreadsheet. It contains every saved quote and job; it is not an invoice.</p><label for="export-data"><span>CSV data</span><textarea id="export-data" readonly></textarea></label></section>
</main><script>
(()=>{'use strict';
const $=id=>document.getElementById(id),money=n=>new Intl.NumberFormat('en-US',{style:'currency',currency:'USD'}).format(n/100),escape=x=>String(x).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
let state={schemaVersion:1,quotes:[]},version=0,editing=null,loaded=false,busy=false,lineSequence=0;
const total=lines=>lines.reduce((sum,line)=>sum+line.quantity*line.unitMinor,0),statusName=s=>s.replace(/_/g,' ');
function say(message,error=false){$('notice').textContent=message;$('notice').classList.toggle('error',error);}
function lock(){ $('form-fields').disabled=!loaded||busy;document.querySelectorAll('#quotes button,#quotes select,#export').forEach(el=>el.disabled=!loaded||busy); }
function invalid(message,input){if(input){input.setAttribute('aria-invalid','true');input.focus();}throw Error(message);}
function quantity(input){const raw=input.value.trim(),value=Number(raw);if(!/^\d+$/.test(raw)||!Number.isSafeInteger(value)||value<1||value>10000)invalid('Enter a whole quantity from 1 to 10,000.',input);return value;}
function cents(input){const raw=input.value.trim().replace(/^\./,'0.');if(!/^\d+(?:\.\d{1,2})?$/.test(raw))invalid('Enter a nonnegative USD price with no more than two decimal places.',input);const parts=raw.split('.'),value=Number(parts[0])*100+Number((parts[1]||'').padEnd(2,'0'));if(!Number.isSafeInteger(value)||value>100000000)invalid('Each unit price must be between $0.00 and $1,000,000.00.',input);return value;}
function lines(){return [...$('lines').children].map(row=>{const description=row.querySelector('.description'),value=description.value.trim();if(!value||value.length>300)invalid('Describe the work or material on every line (up to 300 characters).',description);return {description:value,quantity:quantity(row.querySelector('.quantity')),unitMinor:cents(row.querySelector('.unit'))};});}
function updateTotal(){let value=0,valid=true;for(const row of $('lines').children){const q=row.querySelector('.quantity').value.trim(),p=row.querySelector('.unit').value.trim().replace(/^\./,'0.');if(!/^\d+$/.test(q)||Number(q)<1||Number(q)>10000||!/^\d+(?:\.\d{1,2})?$/.test(p)){valid=false;break;}const a=p.split('.'),unit=Number(a[0])*100+Number((a[1]||'').padEnd(2,'0'));value+=Number(q)*unit;if(unit>100000000||!Number.isSafeInteger(value)||value>1000000000000){valid=false;break;}}$('draft-total').textContent=valid?money(value):'Check amounts';}
function addLine(line={description:'',quantity:1,unitMinor:0}){if($('lines').children.length>=100){say('A quote can contain up to 100 lines.',true);return;}const n=++lineSequence,row=document.createElement('div');row.className='line';row.innerHTML='<label class="description-label" for="description-'+n+'"><span>Description</span><input id="description-'+n+'" class="description" maxlength="300" placeholder="Work or material" required></label><label for="quantity-'+n+'"><span>Quantity</span><input id="quantity-'+n+'" class="quantity" inputmode="numeric" required></label><label for="unit-'+n+'"><span>Unit USD</span><input id="unit-'+n+'" class="unit" inputmode="decimal" required></label><button type="button" class="remove" aria-label="Remove this line">×</button>';
row.querySelector('.description').value=line.description;row.querySelector('.quantity').value=String(line.quantity);row.querySelector('.unit').value=(line.unitMinor/100).toFixed(2);row.querySelector('button').onclick=()=>{if($('lines').children.length===1){say('Keep at least one work line on the quote.',true);return;}row.remove();updateTotal();};$('lines').append(row);updateTotal();}
function blank(announce=true){editing=null;$('customer').value='';$('customer').removeAttribute('aria-invalid');$('lines').replaceChildren();addLine();$('draft-heading').textContent='New quote';$('draft-badge').textContent='Draft';$('save').textContent='Save quote';if(announce){say('New quote ready. Your saved work is unchanged.');$('customer').focus();}}
function reopen(id){if(busy)return;const q=state.quotes.find(q=>q.id===id);if(!q)return;editing=q.id;$('customer').value=q.customer;$('customer').removeAttribute('aria-invalid');$('lines').replaceChildren();q.lines.forEach(addLine);$('draft-heading').textContent='Edit saved work';$('draft-badge').textContent=statusName(q.status);$('save').textContent='Save changes';say('Opened '+q.customer+'. Saving updates this record without creating a duplicate.');$('customer').focus();}
function render(){ $('empty-state').hidden=state.quotes.length>0;$('record-count').textContent=state.quotes.length+' saved '+(state.quotes.length===1?'record':'records');$('quoted-value').textContent=money(state.quotes.filter(q=>q.status==='quoted').reduce((n,q)=>n+total(q.lines),0));
$('quotes').innerHTML=state.quotes.map(q=>'<article><div class="record-top"><div><h3>'+escape(q.customer)+'</h3><p class="record-id">'+escape(q.id)+'</p><span class="status" data-status>'+escape(statusName(q.status))+'</span></div><strong data-total>'+money(total(q.lines))+'</strong></div><ul class="work-lines">'+q.lines.map(l=>'<li><span>'+escape(l.description)+'</span><span>'+l.quantity+' × '+money(l.unitMinor)+'</span></li>').join('')+'</ul><div class="record-actions"><button type="button" data-open="'+escape(q.id)+'">Open quote</button>'+(q.status==='quoted'?'<button type="button" class="primary" data-convert="'+escape(q.id)+'">Convert to job</button>':'<label for="job-'+escape(q.id)+'"><span>Job status</span><select id="job-'+escape(q.id)+'" data-job="'+escape(q.id)+'">'+['scheduled','in_progress','completed'].map(s=>'<option value="'+s+'"'+(s===q.status?' selected':'')+'>'+statusName(s)+'</option>').join('')+'</select></label>')+'</div></article>').join('');
document.querySelectorAll('[data-open]').forEach(b=>b.onclick=()=>reopen(b.dataset.open));document.querySelectorAll('[data-convert]').forEach(b=>b.onclick=()=>changeStatus(b.dataset.convert,'scheduled'));document.querySelectorAll('[data-job]').forEach(el=>el.onchange=()=>changeStatus(el.dataset.job,el.value));lock();}
async function persist(next,message){if(!loaded||busy)return false;if(new TextEncoder().encode(JSON.stringify(next)).length>512000){say('This workspace is at its storage limit. Export your records before making further changes.',true);return false;}busy=true;lock();try{const result=await window.midas.saveState(next,version);state=result.state;version=result.version;render();say(message);return true;}catch(error){render();say('Save was not confirmed. Refresh to inspect saved work before retrying. '+error.message,true);return false;}finally{busy=false;lock();}}
async function changeStatus(id,status){const quote=state.quotes.find(q=>q.id===id);if(!quote||busy)return;if(!['scheduled','in_progress','completed'].includes(status))return;const saved=await persist({...state,quotes:state.quotes.map(q=>q.id===id?{...q,status}:q)},'Saved '+quote.customer+' as '+statusName(status)+'.');if(saved&&editing===id)$('draft-badge').textContent=statusName(status);}
$('quote-form').onsubmit=async event=>{event.preventDefault();if(!loaded||busy)return;try{const customer=$('customer').value.trim();if(!customer||customer.length>200)invalid('Enter a customer name (up to 200 characters).',$('customer'));const work=lines(),amount=total(work);if(!Number.isSafeInteger(amount)||amount>1000000000000)invalid('The quote total exceeds the supported limit. Split the work into smaller quotes.');if(!editing&&state.quotes.length>=500)invalid('This workspace holds up to 500 records. Export your work before adding more.');const old=editing?state.quotes.find(q=>q.id===editing):null;if(editing&&!old)invalid('The original quote is unavailable. Refresh before saving.');let id=old?.id;if(!id){do{id='Q-'+(globalThis.crypto?.randomUUID?crypto.randomUUID():Date.now().toString(36)+'-'+Math.random().toString(36).slice(2));}while(state.quotes.some(q=>q.id===id));}const quote={id,customer,lines:work,status:old?.status||'quoted'},next={schemaVersion:1,quotes:old?state.quotes.map(q=>q.id===id?quote:q):[...state.quotes,quote]};if(await persist(next,(old?'Updated ':'Saved ')+customer+' · '+money(amount)+'.'))blank(false);}catch(error){say(error.message,true);}};
$('add-line').onclick=()=>addLine();$('new-quote').onclick=()=>blank();$('quote-form').oninput=event=>{event.target.removeAttribute('aria-invalid');updateTotal();};
function csvCell(value){let s=String(value);if(/^[=+\-@]/.test(s))s="'"+s;return '"'+s.replace(/"/g,'""')+'"';}
$('export').onclick=()=>{const rows=[['customer','status','total_usd','id'],...state.quotes.map(q=>[q.customer,q.status,(total(q.lines)/100).toFixed(2),q.id])];$('export-data').value=rows.map(row=>row.map(csvCell).join(',')).join('\r\n');$('export-panel').hidden=false;say('CSV ready with '+state.quotes.length+' saved '+(state.quotes.length===1?'record.':'records.'));$('export-data').focus();$('export-data').select();};
window.midas.getState().then(result=>{state=result.state;version=result.version;loaded=true;blank(false);render();document.body.dataset.ready='true';say(state.quotes.length?'Your saved work is ready. Open a quote to review or update it.':'Workspace ready. Start with a customer and the work you want to quote.');}).catch(error=>say('Could not load saved work. Refresh to try again. '+error.message,true));
})();
</script></body></html>`;
