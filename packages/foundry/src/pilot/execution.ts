import {AdaptiveWorkTools} from '../adaptive/tools.ts';
import {validateAcquisitionPolicy} from '../adaptive/acquisition.ts';
import {ADAPTIVE_TOOL,ADAPTIVE_VERSION,validateAdaptiveOptions} from '../adaptive/worker-contract.ts';
import {randomUUID} from 'node:crypto';
import {StateStore} from '../state.ts';
import {hash,requireThat} from '../contracts.ts';
import {Portfolio} from '../portfolio/core.ts';
import {PortfolioEngine} from '../portfolio/engine.ts';
import {EvidenceLibrary} from '../portfolio/evidence.ts';
import {LocalWorkTools,localToolIds,mandatoryCheckIds} from '../portfolio/tools.ts';
import {portfolioScope} from '../portfolio/contracts.ts';
import type {Task} from '../portfolio/contracts.ts';
import {FINALIZATION_PROTOCOL} from '../portfolio/finalization.ts';
import {createTaskPreparer} from '../portfolio/task-preparation.ts';
import {fixturePilotWorker} from './fixtures-execution.ts';
import {PilotKnowledge} from './knowledge.ts';
import {blankProjectFiles,parseProjectManifest} from '../portfolio/project-contracts.ts';
import {fixtureProjectWorker} from './fixtures-project.ts';
import {taskBusinessContext} from '../portfolio/task-context.ts';

export type PilotWorkflow='response-packet'|'business-site'|'campaign-packet'|'marketing-page'|'functional-project';
export type PilotJob={title:string;outcome:string;details:string;audience?:string;offer?:string};
export type PilotBusiness={id:string;name:string;mode:string;goal:string;website?:string;notes?:string};
export type PilotSource={id:string;title:string;text:string;rights:string;observedAt:string;permission?:string;validUntil?:string|null;version?:number;kind?:string;origin?:{kind?:string;url?:string|null;recordId?:string;contentHash?:string;extraction?:string;provenance?:string};provenance?:string;url?:string|null;publishedAt?:string|null};
export type PlanRequest={adaptive?:import('../adaptive/worker-contract.ts').AdaptiveOptions&{modelCalls:number;localToolRuns:number};taskId?:string;business:PilotBusiness;sources:PilotSource[];workflow:PilotWorkflow;procedureBinding?:{procedureScope:any;baselineProcedureId:string;id?:string;name?:string};intakeFields?:Array<{id:string;label:string}>;job?:PilotJob;campaignTaskId?:string;requiredSourceIds?:string[];selectedEvidence?:true};
const workTools=[...localToolIds,'research.read'];
const purpose:Record<PilotWorkflow,string>={'functional-project':'Build a bounded multi-file business application with a persistent local backend','response-packet':'Prepare a sourced inquiry-response packet for owner review','business-site':'Build a company-information and local inquiry preparation application','campaign-packet':'Prepare a coordinated commercial campaign and operating packet','marketing-page':'Build a responsive commercial page from the authoritative campaign'};
const companyContext=(business:PilotBusiness)=>({name:business.name,goal:business.goal,website:business.website??'',notes:business.notes??''});
const selectedContext=(sources:PilotSource[])=>sources.filter(s=>s.permission!=='excluded').map(s=>({id:s.id,title:s.title,textHash:hash(s.text),rights:s.rights,observedAt:s.observedAt,validUntil:s.validUntil??null})).sort((a,b)=>a.id.localeCompare(b.id));
/** Product facade over the existing finite controller. It grants no model or effect authority. */
export class PilotExecution{
 readonly store:StateStore;readonly portfolio:Portfolio;readonly tools:LocalWorkTools;readonly evidence:EvidenceLibrary;readonly engine:PortfolioEngine;
 constructor(store:StateStore,options:{root?:string;portfolio?:Portfolio;browserLauncher?:()=>Promise<any>}={}){
  this.store=store;this.portfolio=options.portfolio??new Portfolio(store);this.evidence=new EvidenceLibrary(store,{publicRead:false});
  this.tools=new LocalWorkTools({store,root:options.root??'.',scopeFor:portfolioScope,browserLauncher:options.browserLauncher});
  const standard=createTaskPreparer(this.portfolio,this.tools,this.evidence);
  this.engine=new PortfolioEngine({portfolio:this.portfolio,tools:this.workTools('fixture'),evidence:this.evidence,model:fixtureProjectWorker(fixturePilotWorker()),validateTask:task=>this.assertContextCurrent(task.id),prepareTask:task=>{
   if((task.inputs as any)?.pilotWorkflow==='functional-project'&&task.capability==='software.build'){this.tools.seed(task.ventureId,task.id,{kind:'software',files:blankProjectFiles(),inputs:{profile:'functional-project-v1',enforceHandoff:true,job:(task.inputs as any).job,companyName:taskBusinessContext(task,this.portfolio.getVenture(task.ventureId)).name,localOnly:true},provenance:'Controller-created blank project; following source edits retain their recorded worker/test-double authorship.'});return;}

   if(['business-site','marketing-page'].includes((task.inputs as any)?.pilotWorkflow)&&task.capability==='software.build'){
    const sources=this.evidence.forTask(task),v=taskBusinessContext(task,this.portfolio.getVenture(task.ventureId));
    let campaign:any=null,campaignArtifactHash:string|null=null,campaignObservations:any[]=[];if((task.inputs as any).pilotWorkflow==='marketing-page'){const source=this.campaignSource(task.ventureId,(task.inputs as any).campaignTaskId);campaign=source.campaign;campaignArtifactHash=source.artifact.hash;campaignObservations=source.observations;}
    this.tools.seed(task.ventureId,task.id,{kind:'software',files:[{path:'app.html',content:'<!doctype html><html lang="en"><title>Implementation pending</title><body><h1>Implementation pending</h1></body></html>'}],inputs:{profile:(task.inputs as any).executionProfile,job:(task.inputs as any).job??null,...(campaign?{campaign,campaignArtifactHash}:{}),enforceHandoff:true,companyName:v.name,intakeFields:(task.inputs as any).intakeFields,evidence:campaign?campaignObservations.map(o=>({sourceId:o.sourceId,title:o.statement,quote:o.quote})):sources.map(s=>({sourceId:s.id,title:s.title,quote:s.text})),localOnly:true},provenance:'Controller-created blank source; following source writes have separately recorded worker/test-double provenance.'});
   }else if((task.inputs as any)?.pilotWorkflow==='campaign-packet'&&task.capability==='service.brief'){const v=taskBusinessContext(task,this.portfolio.getVenture(task.ventureId)),sources=this.evidence.forTask(task);this.tools.seed(task.ventureId,task.id,{kind:'service',files:[{path:'brief.json',content:'{}'}],inputs:{profile:'commercial-campaign-v1',enforceHandoff:true,title:task.title,client:v.name,asOf:new Date().toISOString().slice(0,10),job:(task.inputs as any).job??null,sources:sources.map(s=>({id:s.id,title:s.title,text:s.text,rights:s.rights})),requiredSourceIds:(task.inputs as any).requiredSourceIds??[]},provenance:'Controller-created blank campaign; subsequent authored content has recorded worker/test-double provenance.'});}else return standard(task);
  }});
  this.portfolio.registerWorker({id:'pilot-generalist',name:'Business generalist · experimental',competencies:['evidence-grounded-work','artifact-correction','local-browser-implementation','commercial-writing','cross-artifact-consistency','commercial-information-design'],capabilities:['service.brief','software.build','quality.review'],procedureId:'portfolio-worker-v3',maxConcurrency:1},{ifAbsent:true});
 }
 workTools(provenance:'fixture'|'development'|'actual_model'='actual_model'){return new AdaptiveWorkTools({base:this.tools,store:this.store,root:this.tools.root+'/adaptive',taskFor:id=>this.portfolio.getTask(id),evidence:this.evidence,provenance});}
 async stopAdaptiveServices(businessId:string,taskId:string){const task=this.portfolio.getTask(taskId);requireThat(task.ventureId===businessId,'PILOT_TASK_SCOPE_DENIED');return await this.workTools().stopServices(businessId,taskId);}
 private assertTask(businessId:string,taskId:string){const task=this.portfolio.getTask(taskId);requireThat(task.ventureId===businessId&&(task.inputs as any)?.pilotRelease==='032','PILOT_TASK_SCOPE_DENIED');return task;}
 /** A changed owner scope blocks NEW inference, while admitted same-ID recovery
  * remains handled by the signed transport using its original request. */
 assertContextCurrent(taskId:string,requireStored=false){
  const task=this.portfolio.getTask(taskId),binding=(task.inputs as any)?.pilotOwnerContext;
  requireThat(binding&&(!requireStored||binding.kind==='stored_company'),'PILOT_OWNER_CONTEXT_BINDING_REQUIRED');
  if(binding.kind==='stored_company'){
   const knowledge=new PilotKnowledge(this.store),company=knowledge.company(task.ventureId),sources=knowledge.sources(task.ventureId);
   requireThat(binding.hash===hash({business:companyContext(company),sources:selectedContext(sources)}),'PILOT_OWNER_CONTEXT_CHANGED_PREPARE_FRESH_WORK');
   requireThat(sources.filter(s=>s.permission!=='excluded').every(s=>!s.validUntil||Date.parse(s.validUntil)>=Date.now()),'PILOT_STALE_EVIDENCE_REVIEW_REQUIRED');
  }
  return task;
 }
 private recordSources(business:PilotBusiness,sources:PilotSource[],commercial=false){
  const permitted=sources.filter(s=>s.permission!=='excluded');requireThat(permitted.length>0,'PILOT_PERMITTED_EVIDENCE_REQUIRED');
  requireThat(permitted.length<=(commercial?24:12)&&permitted.every(s=>s.text.length<=(commercial?24000:20000)),'PILOT_WORKER_BUNDLE_TOO_LARGE');
  requireThat(permitted.reduce((n,s)=>n+Buffer.byteLength(s.text),0)<=(commercial?160000:8000),'PILOT_EXECUTION_EVIDENCE_SCOPE_REQUIRED');
  requireThat(permitted.every(s=>!s.validUntil||Date.parse(s.validUntil)>=Date.now()),'PILOT_STALE_EVIDENCE_REVIEW_REQUIRED');
  return permitted.map(source=>{const publicSource=source.origin?.kind==='public-retrieval',fixture=business.mode==='fixture'||source.origin?.provenance?.includes('mock'),assistant=source.provenance?.includes('assistant');return this.evidence.add(business.id,{title:source.id+' · '+source.title,url:publicSource?source.origin!.url??source.url??null:source.url??null,text:source.text,observedAt:source.observedAt,publishedAt:source.publishedAt??null,rights:publicSource?'public_readonly':'owner_supplied',provenance:fixture?'offline_fixture':publicSource?'runtime_public_retrieval':assistant?'development_assistant_research':'owner_report',textKind:publicSource?'exact_excerpt':assistant?'assistant_summary':'owner_statement',interpretation:JSON.stringify({rule:'Retained permitted evidence; references are not independent verification and grant no authority.',originalSourceId:source.id,originalRights:source.rights,origin:source.origin??null,originalProvenance:source.provenance??null})});});
 }
 plan(input:PlanRequest){
  if(input.adaptive)validateAdaptiveOptions(input.adaptive);
  if(input.adaptive?.acquisition){validateAcquisitionPolicy(input.adaptive.acquisition);requireThat(input.business.mode==='fixture'||input.sources.filter(s=>s.permission!=='excluded').every(s=>s.origin?.kind==='public-retrieval'),'ACQUISITION_PRIVATE_CONTEXT_DENIED');}
  if(input.adaptive){requireThat(['baseline','lean','direct'].includes(input.adaptive.prompt)&&typeof input.adaptive.allowCommands==='boolean'&&Number.isSafeInteger(input.adaptive.modelCalls)&&input.adaptive.modelCalls>=3&&input.adaptive.modelCalls<=64&&Number.isSafeInteger(input.adaptive.localToolRuns)&&input.adaptive.localToolRuns>=4&&input.adaptive.localToolRuns<=128,'ADAPTIVE_ALLOCATION_INVALID');requireThat(!input.procedureBinding,'ADAPTIVE_PROCEDURE_BINDING_CONFLICT');}
  requireThat(['response-packet','business-site','campaign-packet','marketing-page','functional-project'].includes(input.workflow),'PILOT_WORKFLOW_UNSUPPORTED');
  const commercial=['campaign-packet','marketing-page','functional-project'].includes(input.workflow);const businessContext=companyContext(input.business),suppliedContext={business:businessContext,sources:selectedContext(input.sources)},stored=this.store.get('pilot-company',input.business.id);
  requireThat(input.selectedEvidence===undefined||input.selectedEvidence===true&&stored&&Array.isArray(input.requiredSourceIds)&&input.requiredSourceIds.length>0&&input.requiredSourceIds.every(id=>suppliedContext.sources.some(s=>s.id===id)),'PILOT_SELECTED_EVIDENCE_SCOPE');
  // Explicit outcome selection may narrow the worker bundle. The complete owner
  // context remains pinned, and every selected document must match it exactly.
  let context=suppliedContext;if(stored){const knowledge=new PilotKnowledge(this.store);context={business:companyContext(knowledge.company(input.business.id)),sources:selectedContext(knowledge.sources(input.business.id))};requireThat(commercial||input.selectedEvidence?hash(suppliedContext.business)===hash(context.business)&&suppliedContext.sources.every(s=>context.sources.some(c=>hash(s)===hash(c))):hash(suppliedContext)===hash(context),'PILOT_PLAN_CONTEXT_CHANGED');}
  const pilotOwnerContext={kind:stored?'stored_company':'supplied_fixture',hash:hash(context)};
  if(input.taskId!==undefined)requireThat(/^[A-Za-z0-9][A-Za-z0-9_-]{0,119}$/.test(input.taskId),'PILOT_STABLE_TASK_ID_INVALID');
  const planIntentHash=hash({...(input.adaptive?{adaptive:input.adaptive}:{}),...(input.selectedEvidence?{selectedEvidence:true}:{}),business:input.business,workflow:input.workflow,sources:input.sources,procedureBinding:input.procedureBinding??null,intakeFields:input.intakeFields??null,job:input.job??null,campaignTaskId:input.campaignTaskId??null,requiredSourceIds:input.requiredSourceIds??null,ownerContext:pilotOwnerContext});
  if(input.taskId){const existing=this.store.get('portfolio-task',input.business.id+'/'+input.taskId);if(existing){requireThat(existing.ventureId===input.business.id&&existing.inputs?.pilotPlanIntentHash===planIntentHash,'PILOT_STABLE_TASK_BINDING_CHANGED');this.assertContextCurrent(existing.id);if(input.workflow==='marketing-page'){const current=this.campaignSource(input.business.id,input.campaignTaskId??'');requireThat(existing.inputs.campaignArtifactHash===current.artifact.hash,'PILOT_STABLE_DEPENDENCY_CHANGED');}return this.portfolio.getTask(existing.id);}}

  let venture:any;try{venture=this.portfolio.getVenture(input.business.id);}catch{venture=this.portfolio.createVenture({id:input.business.id,name:input.business.name,goal:input.business.goal,mode:input.business.mode==='fixture'?'fixture':'local',summary:'Company evidence is incomplete. Supported local preparation only; no customer effects.'});}
  requireThat(venture.mode===(input.business.mode==='fixture'?'fixture':'local'),'PILOT_MODE_CHANGED');
  const intakeFields=input.intakeFields??[{id:'work',label:'Item or service requested'},{id:'scope',label:'Dimensions or relevant scope details'},{id:'condition',label:'Condition or relevant constraints'},{id:'contact',label:'Preferred contact method (test reference only in demonstration)'}];requireThat(intakeFields.length>=1&&intakeFields.length<=8&&new Set(intakeFields.map(f=>f.id)).size===intakeFields.length&&intakeFields.every(f=>/^[a-z][a-zA-Z0-9_-]{0,39}$/.test(f.id)&&typeof f.label==='string'&&f.label.length>0&&f.label.length<=150),'PILOT_INTAKE_FIELDS_INVALID');
  const sources=this.recordSources(input.business,input.sources,commercial),id=input.taskId??input.workflow+'-'+randomUUID().slice(0,8),software=['business-site','marketing-page','functional-project'].includes(input.workflow),profile=input.workflow==='functional-project'?'functional-project-v1':input.workflow==='marketing-page'?'marketing-page-v1':input.workflow==='campaign-packet'?'commercial-campaign-v1':'business-site-v1';
  if(input.job)requireThat(['title','outcome','details'].every(k=>typeof (input.job as any)[k]==='string'&&(input.job as any)[k].trim().length>0&&(input.job as any)[k].length<=4000)&&['audience','offer'].every(k=>(input.job as any)[k]===undefined||typeof (input.job as any)[k]==='string'&&(input.job as any)[k].length<=500),'PILOT_JOB_DETAILS_INVALID');
  const originalSources=input.sources.filter(s=>s.permission!=='excluded'),requiredOriginal=input.requiredSourceIds??(commercial?originalSources.filter(s=>s.kind==='notes').map(s=>s.id):originalSources.map(s=>s.id));requireThat(requiredOriginal.every(id=>originalSources.some(s=>s.id===id)),'PILOT_REQUIRED_SOURCE_SCOPE');const requiredSourceIds=originalSources.flatMap((s,i)=>requiredOriginal.includes(s.id)?[sources[i].id]:[]);
  const campaign=input.workflow==='marketing-page'?this.campaignSource(input.business.id,input.campaignTaskId??''):null;
  const extraCompetencies=input.workflow==='campaign-packet'?['commercial-writing','cross-artifact-consistency']:input.workflow==='marketing-page'?['commercial-information-design']:[];
  const assignedWorkerId=input.procedureBinding?.id??'pilot-generalist';if(assignedWorkerId!=='pilot-generalist')this.portfolio.registerWorker({id:assignedWorkerId,name:input.procedureBinding?.name??'Experimental business worker',competencies:['evidence-grounded-work','artifact-correction',...extraCompetencies,...(software?['local-browser-implementation']:[])],capabilities:[software?'software.build':'service.brief'],procedureId:input.procedureBinding!.baselineProcedureId,maxConcurrency:1});
  const plan=this.portfolio.addPlan(input.business.id,{id:'plan-'+id,rationale:'Owner selected a supported local outcome from company evidence. Worker assignment covers declared tools; competence is experimental and no second role is justified by evidence.',evidenceIds:sources.map(s=>s.id),tasks:[{id,title:input.job?.title??purpose[input.workflow],objective:(input.job?input.job.outcome+' '+input.job.details:purpose[input.workflow])+'. Read the required source evidence and retrieve decision-relevant remaining text through research.read. Identify unknowns. Do not send, book, quote an unverified price, or infer customer benefit.',lane:'build',capability:software?'software.build':'service.brief',dependsOn:campaign?[input.campaignTaskId!]:[],...(campaign?{inputArtifacts:[{artifactId:campaign.artifact.artifactId,version:campaign.artifact.version,sha256:campaign.artifact.hash}]}:{}),acceptance:[software?(input.workflow==='functional-project'?'Current multi-file project implements the declared job, meaningful empty/error/success states, safe record creation/edit/action, export, desktop/phone interface and backend restart persistence. Actual browser and HTTP observations must support the current reviewed version.':'Current app passes company-source, intake, invalid input, save/edit, status, export, desktop/phone and restart-persistence checks.'):'A readable packet quotes all required company sources, gives a usable draft/action with owner review, distinguishes unknowns and preserves obligations.','Current artifacts receive recorded worker submission, deterministic finalization and authenticated readback.','Local owner acceptance is separate from independent semantic validation and external-action approval.'],requiredCompetencies:['evidence-grounded-work','artifact-correction',...extraCompetencies,...(software?['local-browser-implementation']:[])],allowedTools:input.adaptive?[...workTools,ADAPTIVE_TOOL]:workTools,requiredChecks:mandatoryCheckIds(software?'software':'service',software||input.workflow==='campaign-packet'?profile:undefined),resource:{workerSlots:1,modelCalls:input.adaptive?.modelCalls??(input.workflow==='functional-project'?12:input.workflow==='campaign-packet'?6:8),localToolRuns:input.adaptive?.localToolRuns??14},maxAttempts:1,effectAuthority:{kind:'local',reference:'pilot-032-local-preparation-only'},inputs:{...(input.adaptive?{adaptiveExecution:{version:ADAPTIVE_VERSION,prompt:input.adaptive.prompt,allowCommands:input.adaptive.allowCommands,...(input.adaptive.allowServices!==undefined?{allowServices:input.adaptive.allowServices}:{}),...(input.adaptive.memoryPolicy?{memoryPolicy:input.adaptive.memoryPolicy}:{}),...(input.adaptive.acquisition?{acquisition:input.adaptive.acquisition}:{})}}:{}),pilotRelease:'032',pilotPlanIntentHash:planIntentHash,...(commercial?{requiredSourceIds}:{}),...(input.job?{job:input.job}:{}),...(campaign?{campaignTaskId:input.campaignTaskId,campaignArtifactHash:campaign.artifact.hash}:{}),businessContext,pilotOwnerContext,assignedWorkerId,feedbackPolicy:'defer_to_declared_decision',sourceAuthorship:'from-execution-provenance',pilotWorkflow:input.workflow,pilotMode:input.business.mode==='fixture'?'fixture':'owner',requiresGrant:input.business.mode!=='fixture',executionProtocol:FINALIZATION_PROTOCOL,...(software?{executionProfile:profile,intakeFields}:{}),enforceHandoff:true,sourceBindings:sources.map(s=>({id:s.id,sha256:s.sha256})),...(input.procedureBinding?{baselineProcedureId:input.procedureBinding.baselineProcedureId,procedureScope:input.procedureBinding.procedureScope}:{})}}]});
  return this.portfolio.getTask(plan.taskIds[0]);
 }
 campaignSource(businessId:string,taskId:string){const task=this.assertTask(businessId,taskId),artifact=this.artifact(businessId,taskId);requireThat((task.inputs as any).pilotWorkflow==='campaign-packet'&&task.status==='completed'&&artifact?.current&&artifact.taskId===taskId,'PILOT_CURRENT_CAMPAIGN_REQUIRED');const report=JSON.parse(this.tools.download(businessId,taskId,'brief.json').content);requireThat(report.campaign?.version===1,'PILOT_CAMPAIGN_SOURCE_REQUIRED');return {artifact,campaign:report.campaign,observations:report.observations};}
 replanDependent(input:{businessId:string;taskId:string;campaignTaskId:string}){const prior=this.assertTask(input.businessId,input.taskId);requireThat((prior.inputs as any).pilotWorkflow==='marketing-page'&&!prior.lease,'PILOT_DEPENDENT_REPLAN_SCOPE');const knowledge=new PilotKnowledge(this.store),business=knowledge.company(input.businessId),old=this.artifact(input.businessId,input.taskId);const pi=prior.inputs as any;const task=this.plan({business,sources:knowledge.sources(business.id),workflow:'marketing-page',job:pi.job,campaignTaskId:input.campaignTaskId,intakeFields:pi.intakeFields,...(pi.procedureScope?{procedureBinding:{procedureScope:pi.procedureScope,baselineProcedureId:pi.baselineProcedureId,id:pi.assignedWorkerId}}:{})});this.store.transaction(()=>{const current=this.portfolio.getTask(task.id);this.store.put('portfolio-task',task.id,{...current,...(old?{inputArtifacts:[...current.inputArtifacts,{artifactId:old.artifactId,version:old.version,sha256:old.hash}]}:{}),inputs:{...current.inputs as any,replannedFrom:prior.id,...(old?{authoritativeArtifactId:old.artifactId}:{})}},current._version);this.store.record(portfolioScope(business.id),'replan-'+hash(task.id).slice(0,24),'PilotDependentReplan',{taskId:task.id,priorTaskId:prior.id,campaignTaskId:input.campaignTaskId,automaticExecution:false,allowanceTransferred:false});});return this.portfolio.getTask(task.id);}
 run(businessId:string,taskId:string){const task=this.assertTask(businessId,taskId);requireThat((task.inputs as any).pilotMode==='fixture'&&this.portfolio.getVenture(businessId).mode==='fixture','PILOT_LIVE_GRANT_REQUIRED');if(task.status==='completed'){this.preserveOwnerState(task);return Promise.resolve(task);}this.assertContextCurrent(task.id);return this.engine.runTask(task.id,(task.inputs as any).assignedWorkerId??'pilot-generalist').then(result=>{this.preserveOwnerState(this.portfolio.getTask(task.id));return result;});}
 private preserveOwnerState(task:Task){
  if(task.status==='completed'&&(task.inputs as any)?.pilotWorkflow==='functional-project'&&(task.inputs as any)?.correctionOf){const key=task.id;if(this.store.get('pilot-project-state-migration',key))return;const source=(task.inputs as any).correctionSourceTaskId??(task.inputs as any).correctionOf,from=this.tools.projectRuntime(task.ventureId,source),to=this.tools.projectRuntime(task.ventureId,task.id);const schema=(workspace:any)=>{const manifest=parseProjectManifest(workspace.read('project.json').content);return {entities:manifest.entities.map(entity=>({id:entity.id,fields:entity.fields})).sort((a,b)=>a.id.localeCompare(b.id)),actions:(manifest.actions??[]).map(action=>({id:action.id,entity:action.entity,from:action.from??null,set:action.set})).sort((a,b)=>a.id.localeCompare(b.id))};};requireThat(hash(schema(from))===hash(schema(to)),'PROJECT_CORRECTION_SCHEMA_INCOMPATIBLE');const state=from.state(),current=to.state(),stateHash=hash(state);if(current.version!==0){requireThat(hash(current.entities)===hash(state.entities)&&hash(current.operations)===hash(state.operations),'PROJECT_CORRECTION_STATE_CONFLICT');this.store.transaction(()=>this.store.put('pilot-project-state-migration',key,{source,taskId:task.id,sourceVersion:state.version,stateHash,provenance:'Trusted idempotent preservation of owner records after exact declarative schema compatibility.'},null));return;}to.saveState({...structuredClone(state),version:0},0);const migrated=to.state();requireThat(hash(migrated.entities)===hash(state.entities)&&hash(migrated.operations)===hash(state.operations),'PROJECT_CORRECTION_STATE_MIGRATION_FAILED');this.store.transaction(()=>this.store.put('pilot-project-state-migration',key,{source,taskId:task.id,sourceVersion:state.version,stateHash,provenance:'Trusted preservation of owner records after exact declarative schema compatibility.'},null));return;}

  if(task.status!=='completed'||!['business-site','marketing-page'].includes((task.inputs as any)?.pilotWorkflow)||!(task.inputs as any)?.correctionOf)return;
  const key=task.id,priorTask=this.portfolio.getTask((task.inputs as any).correctionOf);
  // The corrected workspace was seeded from this exact artifact producer. Keep
  // the originating task explicit even when the owner addressed an older card.
  const producer=(task.inputs as any).correctionSourceTaskId??priorTask.id;
  const original=this.tools.load(task.ventureId,producer),next=this.tools.load(task.ventureId,task.id);
  if(this.store.get('pilot-preview-migration',key))return;
  const before=this.tools.previewState(task.ventureId,producer,{ventureId:task.ventureId,taskId:producer,manifestHash:original.manifest.sha256,operation:'read'}),after=this.tools.previewState(task.ventureId,task.id,{ventureId:task.ventureId,taskId:task.id,manifestHash:next.manifest.sha256,operation:'read'});
  requireThat(after.version===0||hash(after.state)===hash(before.state),'PILOT_PREVIEW_MIGRATION_CONFLICT');
  if(after.version===0)this.tools.previewState(task.ventureId,task.id,{ventureId:task.ventureId,taskId:task.id,manifestHash:next.manifest.sha256,operation:'write',expectedVersion:0,state:before.state});
  this.store.transaction(()=>this.store.put('pilot-preview-migration',key,{taskId:task.id,sourceTaskId:producer,sourceVersion:before.version,stateHash:hash(before.state),provenance:'trusted local state preservation; no customer effect'},null));
 }
 pause(businessId:string,taskId:string){this.assertTask(businessId,taskId);return this.portfolio.controlTask(taskId,'pause');}
 resume(businessId:string,taskId:string){this.assertTask(businessId,taskId);this.portfolio.controlTask(taskId,'resume');return this.run(businessId,taskId);}
 correct(input:{businessId:string;taskId:string;artifactHash:string;instruction:string;[key:string]:unknown}){
  const prior=this.assertTask(input.businessId,input.taskId),artifact=this.artifact(input.businessId,input.taskId);requireThat(artifact&&artifact.hash===input.artifactHash,'PILOT_CORRECTION_ARTIFACT_STALE');requireThat(typeof input.instruction==='string'&&input.instruction.trim().length>=5&&input.instruction.length<=2000,'PILOT_CORRECTION_INSTRUCTION_REQUIRED');
  const correctionInputs={...(prior.inputs as any)};delete correctionInputs.procedureScope;delete correctionInputs.baselineProcedureId;
  const current=this.store.get('portfolio-artifact',artifact.artifactId),id=typeof input.stableTaskId==='string'?input.stableTaskId:'correction-'+randomUUID().slice(0,8);if(input.stableTaskId){requireThat(/^[a-z][a-z0-9-]{0,119}$/.test(id),'PILOT_CORRECTION_ID');const existing=this.store.get('portfolio-task',input.businessId+'/'+id);if(existing){requireThat(existing.inputs?.ownerCorrection===input.instruction&&existing.inputs?.correctionArtifactHash===input.artifactHash,'PILOT_CORRECTION_ID_CONFLICT');return this.portfolio.getTask(existing.id);}}const plan=this.portfolio.addPlan(input.businessId,{id:'plan-'+id,rationale:'Owner correction of exact artifact '+artifact.hash+'. Preserve original source and independently recheck the new authoritative revision.',tasks:[{id,title:'Revise: '+prior.title,objective:'Apply the owner-requested correction to the actual current source. Preserve required sourced statements; report contradictions or unsupported instructions explicitly. The fixture demonstrates editing mechanics, not semantic understanding.',lane:'build',capability:'quality.review',dependsOn:[],inputArtifacts:[{artifactId:current.id,version:current.version,sha256:current.sha256}],acceptance:[...prior.acceptance,'Owner correction changes the actual recommendation or site introduction; copied original remains preserved.'],requiredCompetencies:['evidence-grounded-work','artifact-correction'],allowedTools:(prior.inputs as any)?.adaptiveExecution?[...workTools,ADAPTIVE_TOOL]:workTools,requiredChecks:prior.requiredChecks,resource:{workerSlots:1,modelCalls:['campaign-packet','marketing-page'].includes(correctionInputs.pilotWorkflow)?4:8,localToolRuns:14},maxAttempts:1,effectAuthority:prior.effectAuthority,inputs:{...correctionInputs,assignedWorkerId:'pilot-generalist',authoritativeArtifactId:current.id,ownerCorrection:input.instruction,correctionOf:prior.id,correctionSourceTaskId:artifact.taskId,correctionArtifactHash:artifact.hash}}]});
  const task=this.portfolio.getTask(plan.taskIds[0]);this.store.record(portfolioScope(input.businessId),'correction-'+hash(task.id).slice(0,24),'PilotCorrectionRequest',{taskId:task.id,priorTaskId:prior.id,artifactHash:artifact.hash,instruction:input.instruction,provenance:'owner-requested; fixture interpretation is deterministic and not semantic validation'});return task;
 }
 tasks(businessId:string){if(!this.store.get('portfolio-venture',businessId))return [];return this.portfolio.snapshot(businessId).tasks.filter((t:any)=>t.inputs?.pilotRelease==='032').map((t:any)=>({...t,workflow:t.inputs.pilotWorkflow,artifact:this.artifact(businessId,t.id),checkpoint:t.checkpoint??{lastTool:t.attempts?'execution admitted':'planned; no model attempt'},worker:{id:t.workerId??'pilot-generalist',name:'Business generalist',procedureVersion:this.store.get('portfolio-execution',t.id)?.procedureSelection?.procedureId??'portfolio-worker-v3',selectionReason:'One worker has the declared source, correction and browser tools. Relevant job competence is not established by fixtures.'},provenance:this.store.get('portfolio-execution',t.id)?.modelProvenance==='actual_model'?'Actual-model attempt recorded; status and current checks determine completeness':t.inputs.pilotMode==='fixture'?'Explicit development-authored offline fixture through shared controller':'Prepared real-company task; no live attempt recorded',nextAction:t.inputs.requiresGrant&&t.status==='queued'?'Review the exact prospective request and obtain a separately signed model grant':t.nextAction}));}
 artifact(businessId:string,taskId:string):any{
  const task=this.assertTask(businessId,taskId),binding=task.outputArtifacts[0]??task.inputArtifacts.find(b=>b.artifactId===(task.inputs as any)?.authoritativeArtifactId);if(!binding)return null;
  const a=this.store.get('portfolio-artifact',binding.artifactId);if(!a||a.ventureId!==businessId)return null;
  return {hash:a.sha256,artifactId:a.id,taskId:a.taskId,title:a.title,version:a.version,summary:a.summary,checks:a.checks??[],provenance:a.provenance,manifestHash:a.metadata?.manifestHash,sourceAuthorship:a.metadata?.sourceAuthorship,obligations:this.tools.load(businessId,a.taskId).published?.obligations??[],current:task.outputCurrent&&binding.sha256===a.sha256,previewUrl:'/preview?businessId='+encodeURIComponent(businessId)+'&taskId='+encodeURIComponent(taskId),downloadUrl:'/download?businessId='+encodeURIComponent(businessId)+'&taskId='+encodeURIComponent(taskId)};
 }
 preview(businessId:string,taskId:string){this.preserveOwnerState(this.assertTask(businessId,taskId));const a=this.artifact(businessId,taskId);requireThat(a,'PILOT_ARTIFACT_UNAVAILABLE');const w=this.tools.load(businessId,a.taskId);this.tools.download(businessId,a.taskId);return {kind:w.kind,files:w.files,inputs:w.inputs,executionProfile:w.inputs.profile,binding:{ventureId:businessId,taskId:a.taskId,manifestHash:w.manifest.sha256},...(w.kind==='service'?{html:this.tools.download(businessId,a.taskId,'report.html').content}:{})};}
 download(businessId:string,taskId:string,path?:string){const a=this.artifact(businessId,taskId);requireThat(a,'PILOT_ARTIFACT_UNAVAILABLE');return this.tools.download(businessId,a.taskId,path);}
 previewState(businessId:string,taskId:string,body:any){const task=this.assertTask(businessId,taskId),a=this.artifact(businessId,taskId);requireThat(a&&a.taskId===taskId&&a.current&&body.manifestHash===a.manifestHash,'PILOT_PREVIEW_ARTIFACT_STALE');this.preserveOwnerState(task);return this.tools.previewState(businessId,taskId,body);}
 async recover(){return this.engine.recover();}
}
