# Commercial operator release — engineering evidence checkpoint

Checkpoint: **2026-09-15**. Work is integrated in `codex/commercial-operator-release`, based on `296a109cd9e35d73bf3a54185da4c6f834402c3e`. Integrated implementation is `440fb6913a287b7166e84b6bc50f6f40fb184571`; the independently reviewed ablation correction is `8379986bcb94ae7b11d800ae8ac0c17cd2797b6c`. The final signed integration run passed 25/25 against the committed implementation. The subsequent trial-only correction passed 15/15 and strict semantic checking. [COMPLETION.md](COMPLETION.md) gives the owner handoff; the ignored final publication receipt records exact local/remote HEAD and draft PR after publication. This inventory does not declare release completion or independent competence. The integration owner maintains [EXECUTION.md](../intelligence/EXECUTION.md) and the final receipt.

Working software, actual model operation and comparative usefulness are separate acceptance levels. The engineering checks below exercise actual controller, browser and host behavior where specified. **The current release's actual-model workload and matched model comparison have not run.** Scripted decisions, local checks and host isolation tests do not establish business judgment, customer value or specialist advantage.

| Requirement | Implemented connection and retained engineering evidence | Remaining acceptance |
|---|---|---|
| Owner-directed operation | Found/Operate intake is distinct from owner/fixture provenance. Owner sessions and CSRF routes persist mandates, exact scope and finite limits. Explicit adaptive controls carry command, service and acquisition options into authoritative tasks. Signed mock checks reject changed options before admission and recover retained responses without another admission. | Final signed integration passed. No current actual-model owner journey has run. |
| Business understanding and judgment | Permitted sources, the sourced commercial planner, selection/rejection and corrections use the existing shared services. Outcome materialization uses family-specific source limits; required evidence remains mandatory, selected documents must match exactly, and complete owner context stays pinned. A nonrequired document over 9 KB no longer prevents a bounded response; a required oversized document blocks before dispatch. | Actual authorized model diagnosis, alternative selection and revision after changed evidence remain unobserved. Development-authored judgments do not satisfy this. |
| Substantial delivery | Two different contexts completed six shared-controller fixture tasks: commercial packet, local application and successor reassessment per context. Source, HTTP, browser, local record and restored-record checks executed. A functional-project correction uses its accepted three-call reserve after a fresh journey load, with one edit, actual checks and completion. | Choices and wording are scripted fixtures. Independent semantic quality, actual-model delivery and owner usefulness remain unmeasured. |
| Adaptive resource use | Command and temporary-service tools run through the shared controller. The final WSL/service suite passes 13 boundary tests: nine dispatch real host processes; four check structure, memory visibility or authority without WSL dispatch. Exact acquisition hosts and limits persist; private evidence is denied. Service start/status/request/stop, expiry, cancellation and restart are connected. | Worker-selected unfamiliar-obstacle diagnosis, resource acquisition and useful application have not been observed. The workspace storage watchdog is monitoring, not a filesystem quota. |
| Observation and continuation | Measurements, owner statements, hypotheses and pending feedback persist with distinct provenance. Nonpending evidence creates an unsigned successor mandate whose planner, task context and current memory contain that evidence. Rejection, withdrawal, idempotency and restart are tested; withdrawal stops affected continuation work. | Business impact and causal interpretation remain unknown. Pending feedback remains pending. Owner assertions do not become runtime facts or automatic external actions. |
| Reusable capability | Task-scoped episodes, retained candidates, source/procedure hashes, applicability and explicit memory visibility are connected to subsequent work. Retail exception and reporting-readiness packets use the same controller. | No independently qualified capability, developer-unseen obstacle, protected holdout or demonstrated cross-context transfer is established. |
| Learning and regression response | Provenance, separately trusted assessment boundaries, applicability, monitoring and rollback are implemented. The current learning/owner run passes 34 tests. Candidate-memory masking hides reuse without deleting historical candidates. The comparison panel labels scripted evaluation explicitly. | No independent assessor is configured. Matched strong-direct/current/adaptive model comparison, benefit and superiority evidence remain absent. |
| Operational reliability | V1–4 continuity checks pass. V4 retains bounded binaries, stopped uncertain command claims and trusted completion evidence. Active owner/worker processes and unconfirmed services prevent backup. Actual service integration verifies stopped backup excludes control handles and restore creates none. | Final signed integration passed. Multi-file backup is not atomic; quiescence is required. |
| Owner experience | Desktop/phone checks cover onboarding, evidence, outcomes, pending feedback, successor work, rejection and adaptive preparation. Pause/cancel latches precede service cleanup; unconfirmed cleanup stays visible. Real service tests verify owner stop after the latch. Managed-preview paging is repaired and covered by two real-browser regressions; the retail fixture is unchanged. | The final owner server was restarted and actual saved product/results inspected. UI capabilities do not grant provider spending or external effects. |
| Commercial readiness | Versioned public/synthetic evidence, complete samples, ten visible cases, lifecycle cases, proposed pilot scope, measurement fields, economics formulas and an unsent invitation are prepared. | No consenting buyer, agreed price, actual customer cases, independent labor measurement, willingness to pay, cash collection or commercial impact exists. Visible development cases are not an independent evaluation. |
| Reviewed release | Independent review found and repaired adaptive-scope expansion, retained-response recovery, family-specific evidence sizing and journey correction-budget defects. Focused checks pass; historical authority and earlier releases are preserved. | Source identity, signed integration, final trial review and strict checking are complete. Publication and scan results are in the final local receipt. Actual-model and independent-comparison acceptance remains unpassed. |

## Current verification receipts

These are distinct runs and must not be added as a count of independent acceptance cases. Some focused checks overlap the grouped runs.

| Run | Result | Evidence and interpretation |
|---|---|---|
| Mechanics and owner boundaries | **57 tests: 56 passed, 1 skipped, 0 failed** | [final-mechanics.txt](../../var/release-verification/final-mechanics.txt). Acquisition, scope, durable recovery, continuity, owner controls, observations and selected evidence. The skipped case is explicit opt-in real-public retrieval; this run makes no fresh public-retrieval claim. |
| Learning and owner integration | **34 passed, 0 failed, 0 skipped** | [final-learning-owner.txt](../../var/release-verification/final-learning-owner.txt). Shared adaptive execution, preserved baseline behavior, projections, comparison contracts, preparation, provenance, applicability and rollback. Model outputs are fixtures or mocks. |
| WSL and temporary-service boundaries | **13 passed, 0 failed, 0 skipped; 58.6 seconds** | [final-wsl-services.txt](../../var/release-verification/final-wsl-services.txt). Nine tests dispatch actual WSL commands or services; four reject invalid inputs or verify authority/memory structure without host dispatch. These are host-boundary tests, not actual-model tests. |
| Family-specific source selection | **3 passed** | `pilot-outcome-source-scope.test.ts`, also included in mechanics. Nonrequired oversized evidence is excluded without changing owner records; required oversized evidence blocks before task/worker creation; explicit selected scope cannot bypass required-source or default-plan binding. |
| Functional-project correction after restart | **1 focused test passed** | `pilot-outcome-journey-authorized.test.ts`. A three-call repair completes from a fresh loader through one targeted edit, actual HTTP/browser checks and completion. Completed replay adds no mock admissions. The final full signed-suite run independently passed all 25 tests. |
| Managed preview paging | **2 passed** | `project-preview-scroll.test.ts`. The real-browser regression failed before repair. Page Down/Up now return the same image hash as a later settled capture in both project and embedded managed previews. |

Final additional receipts: `final-committed-signed-integration.txt` records **25/25 passes, 67.23 seconds**; `final-trial.txt` records the final **15/15 passes**, independently rerun with zero skips; `typecheck.json` records **TypeScript 6.0.3, 101 source files, strict semantic checking, zero errors**. The final trial regression proves that a valid memory ablation does not waive flagship or transfer facts equality. The direct-procedure focused suite passed 6/6; five of these overlap the earlier grouped learning run.

Earlier focused evidence remains available: 16 owner tests, eight continuity tests and two archived UI journeys passed before the grouped checks. Acquisition controls were inspected at 390-pixel width without overflow. These historical focused runs are not an additional fresh full-suite result.

## Durable execution and continuity

The owner implementation reuses `StateStore`, `PilotKnowledge`, `PilotIntelligence`, `PilotOutcomes`, `PilotExecution` and `PortfolioEngine`. `pilot/observation-loop.ts` connects evidence to an executable decision request and current memory. It does not synthesize model reasoning, treat an owner checkbox as execution proof or invalidate unrelated work.

Actual host checks verify commands deny host, sibling, evidence and network access; deadlines and owner cancellation terminate scoped work; process fanout is bounded; and trusted completion survives lost controller observation without redispatch. Temporary services survive manager restart without duplicate creation, enforce scoped HTTP access, stop detached descendants, expire under their lease, reject changed unit identity and prevent finalization until confirmed stopped. Owner cleanup works after a stop-request latch.

V4 preserves a 3 MiB binary and a persisted dispatching claim through exact-hash backup/restore. A restored unresolved claim refuses same-ID and new-ID dispatch. Bound supervisor completion can reconcile after relocation without backend redispatch. Archive mutation, excessive bytes, active owner/worker PIDs, links and private configuration paths are rejected. The historical link test uses an actual Windows directory junction where file symlink creation is unavailable.

V1–3 hash/path semantics remain readable; V4 is explicit (`--format v4`). The service guard uses locked, fresh stopped-state readback. The actual service test refuses backup while live, then verifies stopped backup excludes the service-control tree and restore creates no handles. Credentials and grants are not restored. Fixture completion sidecars and real WSL completion checks retain separate provenance.

## Persisted demonstration and preview repair

The [local demonstration runbook](local-demo.md) documents creation, launch, inspection and restore at `var/commercial-release-demo`.

- `receipt.json`: version `commercial-release-demo-v1`, created `2026-09-15T01:07:27.135Z`; six completed controller tasks, ten original browser captures, zero provider calls and zero external business effects.
- `gallery/index.html`: actual application and owner desktop/phone captures for both contexts.
- `backup-v1/manifest.json`: V2 archive hash `fda9bdc5ec55b13d7a0dba5b4d2525d9353c077f54f11582c13a81ffff08babf`.
- `restored-v1`: local application exports match original hashes; both observation continuations remain completed; live authority stays disabled.
- Local owner launch was verified at `http://127.0.0.1:43147/#archive`. Choosing a scripted business exposes Outcomes, Results and Work. A running port is temporary state; code changes require a server restart.
- `review-preview-scroll/`: original and repaired retail captures plus `receipt.json`. Page Down previously returned a partially painted image with about 376 blank pixels at the top. The same browser produced a complete image after scrolling settled. Shared handlers now allow 350 milliseconds for native paging before capture. The repaired image and later settled image have hash `54ee49196214b93cedf7596660e2259afba5a4f34dc1652c4901b2bf42935149`; fixture source was unchanged.

The fixture worker's first context-selection failure and known-response recovery remain in the original database. Repair did not erase that attempt. Source authorship, scripted decisions, controller execution and browser observations remain distinct provenance categories.

## Actual-model and comparison status

The installed native Codex alpha allowed a synthetic sibling-file read and loopback access under tested restricted profiles. Its tool-free profile did not establish an enforced empty tool inventory. That route was rejected as the release runtime; local probes do not authorize weaker isolation.

The supported Responses route still needs one new exact numerical whole-release approval. Prior R5/034 unsigned packets do not authorize changed code. Owner-journey engineering made no new provider probe, credential-content read or paid call. Existing project access should be reused within the final approved scope; this checkpoint requests no replacement key.

The current release has no actual-model operating result, matched comparison result, independently selected later-input result, trusted independent semantic assessment or customer validation. Rejection and uncertainty remain valid outcomes. Local completion, visible development fixtures and successful isolation do not establish benefit, causal impact or production readiness.
