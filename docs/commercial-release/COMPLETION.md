# Commercial operator release — usable engineering checkpoint

**Open http://127.0.0.1:43147/#archive.** Select either explicitly labeled sandbox, open **Work**, select **Review deliverable**, and open its local application. The retail desk saves an exception handoff; the reporting desk saves a readiness review. Refresh, reopen a saved record and export it. In **Results**, inspect the recorded observation and its completed successor work. These are development-authored fixtures using real controller, storage and browser execution; they are not model-generated commercial results.

The connected software is implemented and reviewed. The full governing mission remains **incomplete at the actual-model and independent-comparison gates**. No new provider spending has been authorized or performed. A passing fixture, local artifact or signed mock grant does not pass those gates.

## What changed

- A business outcome now carries its actual adaptive command, service, acquisition and memory permissions through the signed planner into executable work. Modified scope is rejected before admission. Existing strong generalists and their procedure history remain intact.
- Workers can use task-scoped multi-file work, targeted edits, bounded public acquisition, isolated WSL commands and temporary private services through the shared controller. Failed operations, retained candidates, uncertain completion and unresolved obligations remain durable.
- An observed result can enter current business memory and create a source-bound successor mandate. Missing feedback remains a waiting state. Withdrawal stops affected work; a correction revises the authoritative artifact and invalidates dependent checks.
- The owner workspace connects artifacts, observations, decisions, cancellation and restart to actual stored state. Paging inside managed previews now waits for the browser's completed paint rather than returning a misleading partially blank image.
- The finite trial coordinator delegates to the existing signed runners. It supplies a global lease, fixed stage budgets/order, exact current bindings, same-arm lineage and candidate-memory ablation without another model loop. Strong direct, prior-procedure and lean-adaptive configurations share the declared tools. Role names are not competence evidence.

No company-specific recommendations were placed in the reusable execution core. The public-reference retail and hypothetical reporting scenarios live in versioned data. Prior Mission 027–034, R5 and Adaptive evidence and authority remain separate.

## What actually ran

| Evidence | Observed result | What it cannot establish |
|---|---|---|
| Two sandbox contexts through the shared controller | Six completed tasks: two commercial packets, two local applications and two follow-up reassessments. Actual source checks, local HTTP/browser interaction, persisted records and exact-hash restore ran. | Business judgment and text were scripted development fixtures; no model competence or customer benefit. |
| Owner and product browsers | Desktop and phone journey; ten original captures plus the reproduced and repaired preview-paint defect. Persisted records remained after restart. | No independent semantic acceptance or measured founder correction time. |
| WSL commands and temporary services | Thirteen boundary tests passed, nine dispatching actual isolated processes. Host/sibling/network denial, deadlines, cancellation, restart, descendant stop and service/command competition were exercised. | Not a universal sandbox proof, permanent hosting or model-selected obstacle recovery. |
| Signed runtime factories | Twenty-five integration tests passed on committed source using explicitly mock transports, including known-response recovery, exact adaptive scope, source withdrawal and a three-call functional correction. | No actual Responses model inference or current API project-access proof. |
| Learning and trial mechanics | Independent assessment trust, provenance, candidate applicability, rollback, three distinct procedures and lineage/memory checks run through tests. | No qualified specialist, independent trial result, measured transfer or demonstrated superiority. |

The concrete retained receipt is `var/commercial-release-demo/receipt.json`; screenshots are in `var/commercial-release-demo/gallery/`. The demonstration backup hash is `fda9bdc5ec55b13d7a0dba5b4d2525d9353c077f54f11582c13a81ffff08babf`. Restore reproduces the saved application exports and completed successor states without copying live authority. Original failed/recovered fixture attempts remain in the database.

## Reproduce and inspect

From this branch's checkout, using installed Node:

```powershell
node packages/foundry/tools/commercial-release-demo.mjs --fixture
node packages/foundry/src/pilot/cli.ts serve --root var/commercial-release-demo --port 43147
```

The first command prints the retained receipt when its default root is already complete. The second starts the owner workspace. It does not sign a grant or dispatch a provider. Use [local-demo.md](local-demo.md) for a fresh root and restore commands. [RELEASE-STATUS.md](RELEASE-STATUS.md) contains the acceptance inventory; [EXECUTION.md](../intelligence/EXECUTION.md) is the governing execution ledger.

`var/release-verification/` retains the nonpublic test logs and publication receipt. The grouped mechanics suite has 56 passes and one explicit public-network opt-in skip; learning/owner integration has 34 passes; WSL/service checks have 13 passes; signed integration has 25 passes. Focused trial and direct-procedure checks are reported separately to avoid double counting. TypeScript 6.0.3 strict semantic checking covers 101 source files with zero errors. These counts are regression evidence, not business outcomes.

## Limits that affect operation

The installed included Codex route failed the tested isolation requirement and was not used as an alternative provider proxy. No credential content was read. The prepared direct Responses route still requires the new exact approval, and current project access must be observed during that authorized work.

The verified executor uses Windows Node with existing Ubuntu WSL, Python, Bubblewrap and systemd. Memory/process/CPU controls are per unit, not host-wide. Workspace disk limits are monitored, not filesystem quotas. Services have finite leases and require confirmed stop; stop proof uses fresh systemd state, not a separate empty-cgroup measurement. Backups require quiescence and are not atomic across all files. Local services are not verified 24/7 hosting.

The proposed comparison is three procedure/configuration choices through a shared execution adapter. The prior procedure is preserved, but the historical platform has been ported to common tools; it is not an unchanged historical deployment. Current lineage supports a different task context within an arm's synthetic business namespace. It does not establish cross-business private-data transfer. An immutable provider snapshot is not presently established; exploratory observations must retain returned identities and timestamps.

## Commercial decision and next action

Public evidence argues against replacing an already sufficient native return portal or product FAQ. The prepared retail test concerns exception handoffs only. The reporting-readiness example tests a different job with the same execution boundary. Neither has a confirmed buyer, price, demand, sales or measured labor savings. [README.md](README.md) contains the concrete pilot scope, samples, unknown economics, measurement plan and unsent invitation.

The next authorized engineering outcome is already prepared: the live worker investigates the bounded public evidence, can challenge or reject the proposed work, and can produce a checked technical artifact and small commercial handoff. Actual artifact/check/resource observations then support a fresh next-action decision. Independent comparison cases stay outside this development host until a real custodian supplies them; this is not delegated to another agent with the same filesystem access.

The single [V2 whole-release resource request](LIVE-ENVELOPE-V2.md) proposes at most **138 admissions and USD 289.80**, inclusive of its explicitly unpriced count/retrieval allowance. It includes development, a three-arm comparison, transfer, nonreuse and a candidate-memory ablation, with unused allocations preserved. It is unsigned. No numerical ceiling, signature, independent assessor availability or private business access has been inferred.

New provider requests, settled charges and retained provider exposure for this release are **zero**. Included development-session usage and human engineering/review labor are not measured dollar costs. Historical mission reservations remain in their original ledgers; they are not new spending or a transferred allowance. Customer communication, account changes, purchases, deployment, merge and production qualification remain outside this release's execution authority.
