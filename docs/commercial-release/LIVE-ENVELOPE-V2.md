This prospective V2 supersedes the implementation-status and development allocation portions of [V1](LIVE-ENVELOPE.md), which remains preserved. The proposed whole-release ceiling is unchanged: **138 model admissions, $282.90 in inference reservations and $6.90 for unpriced count/retrieval uncertainty, totaling $289.80**. This is a ceiling for a meaningful initial evidence cycle, not a spending target or an invoice guarantee. The owner has not approved a dollar ceiling or this Max-effort route. This document is not a signed grant. Its preparation used no provider request or credential read.

The [machine-readable V2](live-envelope.v2.json) records the complete allocations and current implementation mapping. The concrete [eleven-stage manifest](trial-manifest.v1.json), [sealed preregistration and preparation receipt](trial-preparation.v1.json), and [binding instructions](trial-README.md) are now prepared; pure structure validation passes, and actual signed runner bindings remain explicitly deferred. Existing unsigned 034/R5 packets do not authorize the changed code. Historical attempts and unresolved exposure stay attached to their original accounting; no carry-in or old authority is silently transferred.

| Work | Maximum admissions | Inference reserve | Count/retrieval allowance | Proposed maximum | Decision value |
| --- | ---: | ---: | ---: | ---: | --- |
| Development journey | 28 | $57.40 | $1.40 | $58.80 | Produce and verify a useful local technical project and small commercial handoff; preserve a bounded correction if needed. |
| Fresh outcome successor | 4 | $8.20 | $0.20 | $8.40 | Use actual artifact, verification and resource observations to decide whether any next work is justified. |
| Strong direct, prior-procedure, adaptive flagship arms | 60: 20 each | $123.00 | $3.00 | $126.00 | Compare completed work, omissions, uncertainty handling, obstacle response and human effort. |
| Different-context transfer, three arms | 24: 8 each | $49.20 | $1.20 | $50.40 | Complete a service intake/readiness workflow and verify whether that arm's own earlier learning fits. |
| Inappropriate-reuse case | 6 | $12.30 | $0.30 | $12.60 | Observe justified nonreuse and a useful safe fallback when a retained candidate does not fit. |
| Paired candidate-memory workflow ablation | 16: 8 each | $32.80 | $0.80 | $33.60 | Compare a complete workflow with and without access to the same frozen retained candidate. |
| **Whole proposal** | **138** | **$282.90** | **$6.90** | **$289.80** | A finite exploratory release decision with failures retained. |

Development uses the existing journey allocation: discovery six, analysis one, planner one, functional project twelve, commercial packet five and exact repair reserve three. The journey grants seventeen primary work calls; the planner must produce a feasible project/packet plan within that allocation. Five packet calls fund a small, usable pilot offer, measurement/economic assumptions and unsent next action. They do not fund a large campaign. The three repair calls are reserved for an exact current-artifact correction, not an automatic retry or extra primary work. A structurally infeasible plan must be rejected before it is treated as a meaningful trial.

The concrete development packet is now prepared, unsigned, at [pilot.outcome.journey.authorization.request.json](../../var/commercial-release-live-development-v4/proposal/pilot.outcome.journey.authorization.request.json), with canonical proposal hash `91b072bc466fb198d6de7c60abbcb4aa30f0ddd98283ad5d6829daf866b19f28`, maximum 28 admissions/$58.80 and expiry `2026-09-22T22:00:00.000Z`. Its [preparation receipt](../../var/commercial-release-live-development-v4/preparation-receipt.json) records zero provider requests and credential reads. This V4 execution root supersedes the preserved unsigned V2/V3 roots after the final source correction; it does not supersede the V2 resource proposal. [prepare-commercial-release.mjs](../../tools/prepare-commercial-release.mjs) prepares a new root from public/synthetic evidence, labels assistant paraphrases and seeds no model answers. The four-call successor and independent trial packets remain unprepared because they need actual observations or independently supplied jobs.

The four-call successor is a **fresh outcome mandate and fresh grant** using the existing outcome runner: one planner call and at most three work calls, with no additional repair reserve. Its immutable observation IDs and observation-context hash must bind actual runtime artifacts, current checks and resource facts. It may reject after the planner call, preserving the unused three calls. It must not invent customer demand, revenue, owner acceptance or longitudinal feedback to make a closed loop appear successful. Changed source, observation, task or implementation bindings require new preparation; an old grant is not reused.

Each flagship arm has twelve project calls and eight packet calls. Transfer funds one complete service packet, including any candidate import, fit/precondition checks, command execution, reuse-outcome recording and finishing. It does not promise a second full application within eight calls. Nonreuse and ablation require an actual eligible candidate from the earlier runtime work. If none exists, report those stages as not run, retain their allocation, and obtain a revised decision before any different use. Do not manufacture a candidate or a challenge to spend the allocation.

The three executable procedures are now `adaptive.prompt: "direct"`, `"baseline"` and `"lean"`, compiled through the same trusted adaptive tool adapter and worker schema. The direct procedure is a strong generalist master instruction with full context, all declared tools, artifact checking, correction and commercial judgment. The baseline retains the prior compiled worker procedure through this shared executor. Lean uses the adaptive procedure and its conservative context projection. All three receive the same allowed acquisition/command/service capabilities in the proposed comparison; there is no deliberate direct-arm tool handicap. Exact compiled procedure, schema, tool, resource, source and request hashes must still be captured for the prepared jobs.

This is a **comparison of declared procedure/configuration choices on a shared adapter**. It is not raw chat versus MIDAS, and it is not the unchanged historical Windows implementation. The prior implementation did not advertise the new acquisition tools; porting its procedure to the verified adapter is a disclosed migration. Context projection and procedure differences are treatment differences. The common-facts comparison removes only the intentional prompt selector and the plan-intent hash derived from it; the complete original worker-facts hash remains bound. Every other task input, source, authority and candidate-memory flag remains significant. Any additional worker-facts, model, evidence, tool or resource differences must be declared before execution and emitted by preflight; otherwise it blocks. A product comparison with such differences cannot attribute an outcome solely to orchestration. No unsupported historical prompt, connector or platform-parity claim is made.

The actual executable coordinator is [adaptive/trial.ts](../../packages/foundry/src/adaptive/trial.ts), exposed by [adaptive-trial.mjs](../../tools/adaptive-trial.mjs). It delegates to `loadAuthorizedPilot`, `loadAuthorizedOutcomeJourney` and `loadAuthorizedOutcome`; it has no separate model loop, credential input or signing port. It validates actual signed runner snapshots, exact stage/task/configuration bindings and the sum of fixed stage allowances before dispatch. It records a durable stage attempt and global lease, follows one predeclared order, and resumes only the existing runner. Unknown request/command/service effects retain the lease and cannot trigger a replacement request. The fixed eleven-stage list includes development, its successor and the nine comparison stages. Development can begin under the whole-release allocation while the three flagship bindings remain deferred together as one future chunk. No flagship arm starts until all three actual job/configuration bindings are supplied and verified as distinct. Future successor/transfer/nonreuse/ablation chunks are similarly bound after preceding work completes. Roots, arms, phases, case IDs and allowances cannot change when future hashes are bound. No allocation is borrowed.

`node tools/adaptive-trial.mjs template` emits an intentionally incomplete three-arm template; `help` lists `inspect-stage`, `preflight`, `run`, `resume`, `lineage` and `bind-chunk`. The executable manifest schema is `adaptive-release-trial-v1`; this resource proposal is not that manifest. Final preparation must expand the template to the whole-release stage list, sum to the proposed ceilings, bind the initial development runner, preregister the population/case IDs/selection rules and rubric, and seal a fixed order. The independent job materials, exact worker-facts/evidence/configuration hashes and fresh grants can then be supplied with each whole deferred chunk. No material is generated here or passed to workers by the coordinator. The coordinator has no randomization generator; a custodian can independently choose an order and then bind it. With one run per arm, an order is not a counterbalanced design.

The proposed path runs the prepared journey through the coordinator, with the entire finite allocation recorded before it starts. Directly invoking a standalone journey runner would enforce only its own grant and would not acquire the global trial lease; that is not the proposed whole-release execution path. The $289.80 proposal still needs an executable manifest and exact new authority; prose and deferred hashes do not authorize provider work.

`prepareTrialLineage` uses the existing V4 `backupPilot`/`restorePilot` functions. It requires a completed same-arm, same-business source with stopped services, released execution leases and terminal response history. It copies original completed task/candidate records and bounded files into a new root, verifies their hashes, and excludes authorization files and service controls. The new selected task and account scope are fresh; copied old ledger rows remain sealed historical evidence and are not charged to the new allowance. A live adapter rejects fixture-origin lineage. Transfer is therefore a new service task/source context within each arm's synthetic sandbox business, not an invented relationship between a retailer and an unrelated real service business, and not cross-business authorization.

The ablation uses two separate new roots with the **same backup hash, normalized task facts, source evidence, model, tools and resources**. Each selected task pins `adaptiveExecution.memoryPolicy = {kind: "retained-candidates-v1", candidateIds: [...]}`; one has an empty list and the other an eligible frozen candidate list. The coordinator checks the pair. The tools enforce that list for candidate context, listing, import, reuse and reuse-outcome actions. This is specifically a retained-candidate access ablation: copied portfolio artifacts and other business evidence remain available in both roots. It is not removal of all historical knowledge. A favorable pair does not by itself grant worker qualification or procedure adoption.

Current verification is mechanical evidence. The coordinator's fifteen persisted-state tests pass, including actual signed mock pilot loaders with deferred credential access, lineage preparation and new-task preflight; lease/recovery contracts; mismatched caps/conditions; unknown responses/services; candidate/file tampering; matched ablation controls; and initial development under the global allowance while the complete flagship cohort is deferred. Three actual signed mock pilots using direct/baseline/lean compile to distinct configurations and full worker-facts bindings while passing common-controls preflight; a changed objective blocks. The final independent reviewer reran all fifteen with zero skips and reported no remaining material coordinator defect. These tests make no provider calls and do not establish runtime business competence, real transfer or commercial value. Existing visible commercial fixtures remain development-assistant material, not protected or independent cases.

The route is `gpt-6-astra`, Max reasoning, default tier, 32,768 counted input and maximum output tokens, provider concurrency one and zero automatic retries. Official pricing checked September 14, 2026 lists input $10/M, output $50/M and cache writes $12.50/M. Ordinary 32,768-in/32,768-out pricing is $1.96608, rounded to $1.97; the code's conservative cache-write input basis gives $2.048, rounded to $2.05. No discount is assumed. The separate five cents per maximum admission is a proposed uncertainty allowance, not a published endpoint price. [Official Astra model documentation](https://developers.openai.com/api/docs/models/gpt-6-astra)

The previously identified project is `proj_H01ORqdOPQM6vdGwQYsqFL5r`. The existing protected credential route is `C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/provider/openai.key`, referenced in [the earlier live request](../foundry/034/LIVE-REQUEST.md). Neither its contents nor current project/model access were checked. No reselection is required merely to prepare this proposal. The request route uses `background: true` and `store: true`; final approval must cover that retention choice. Requested settings, returned model/tier, actual usage and durable request identities must be reported separately. An immutable model snapshot and the account's availability are unverified. [Official background-mode documentation](https://developers.openai.com/api/docs/guides/background)

The current Windows worker route uses WSL, bubblewrap, a network-off process namespace and systemd user cgroups. Actual host tests cover command execution, network/path denial, process fanout, cancellation, deadlines and trusted-completion recovery. Each command/service unit has 1 GiB memory, zero swap, 64 tasks, 100% CPU and a finite unit lifetime with control-group termination. This is a per-unit process-tree cap, not a universal one-GiB release cap. The 256 MiB/4096-entry workspace watchdog is monitored storage, not a disk quota. Services are private loopback and require confirmed stop before completion. Public acquisition remains trusted-host, exact-task, counted, deadline-bound and separate from worker networking. The already prepared development mandate allows eight acquisition requests per task, 16 MiB each, a 15-second acquisition deadline, and exactly docs.python.org, developer.mozilla.org and registry.npmjs.org. Its commands and private services are enabled; worker networking stays off. Its task tool allowance is derived by the existing outcome materializer: 48 for a twelve-call project and 32 for a five-call packet. The separate four-request/30-second and local-tool figures in the JSON are unbound proposals for later independent jobs only. Each later job must bind its exact hosts, byte/request/tool limits and effects; those defaults do not modify the development packet.

The actual finite time controls are request creation 60 seconds, token count 10 seconds, per-response completion 15 minutes, each retrieval 15 seconds, at most 240 retrievals, a 24-hour same-ID resume window and exact absolute grant expiry. There is **no implemented 48-hour whole-release wall timer** or per-stage active-wall timer in this coordinator. V2 makes no such claim. An expiry may stop new admissions while safe recovery remains governed by the existing runner. The prepared development packet expires `2026-09-22T22:00:00.000Z`; use that same explicit expiry when preparing the remaining proposed packets, unless a new owner decision changes it. Included Codex remains only a possible alternative: the tested native route failed isolation and is unsuitable; no substitute route is funded or inferred.

Development can precede independent case custody: its remaining gate is the exact approved development authority and whole-release manifest with the preregistered population, case IDs, selection rules, rubric reference, order and finite allocations. The later independent chunks additionally need selected worker facts, exact job/configuration grants and a named independently controlled custodian/assessor before their dispatch. Owner approval must cover this complete envelope, Max and data retention. The proposed attention is owner 45 minutes, assessor 180 minutes and custodian 60 minutes; these people are not scheduled, and any human fee is unknown and unapproved. Judge-model admissions are zero. The coordinator keeps semantic acceptance and customer outcomes unknown and does not verify a claimed assessor identity from a label. Protected custody is unavailable; no hidden final cases were created here.

The source is now frozen at implementation hash `6d981ee25d8c834aa0fd30a069e74d96895bb99782b71f98d1724e1fc473444a`. The exact unsigned V4 development packet, whole-release manifest and sealed preregistration are prepared; [pure verification](trial-verification.v1.json) passes all 22 structural and consistency checks. Actual signed-loader inspection necessarily follows approval and signing, because the signed authority payload does not exist yet. Obtain those real initial snapshot hashes, bind the first development chunk and run preflight before provider dispatch. Later exact job and lineage bindings follow actual observations or independent selection. Any code change invalidates the frozen hashes. Additional parent calls, expanded tools/hosts, external effects, changed routes, repeated failed arms or spending transfers have zero authority until explicitly rebound and approved. Once executed, release decisions should use current artifact checks, independent semantic review, actual episode/reuse records, human intervention, latency, usage and unknown cost. One exploratory run per arm cannot establish statistical superiority, customer demand or profit.
