The [whole-release manifest](trial-manifest.v1.json) is a concrete, structurally valid eleven-stage input for the existing coordinator. It allocates exactly **138 admissions/$289.80**, uses `common-controls` with no globally allowed differences, and pins the frozen reachable implementation hash. It creates no authority and has not executed. All actual runner bindings remain explicitly `deferred`; no unsigned proposal was relabeled as a signed binding.

The [preparation receipt](trial-preparation.v1.json) records the canonical manifest/document hashes, exact absolute roots, validation results and current development packet. The [pure verification receipt](trial-verification.v1.json) passes 22 structural and consistency checks without creating coordinator state. Its [population](trial-population.v1.json) seals the [selection rules](trial-selection.v1.json), [rubric](trial-rubric.v1.json) and [fixed order](trial-order.v1.json). These are development-assistant preregistration, not independently selected cases or completed assessment. No final case contents, model answers, protected material, assessor signature or customer facts were created.

| Chunk | Stage order | Maximum admissions | Maximum USD | Material still needed |
| --- | --- | ---: | ---: | --- |
| Development | development-journey | 28 | $58.80 | Approval/signing of the already prepared exact V4 packet, then actual loader binding |
| Observed successor | development-successor | 4 | $8.40 | Actual current artifact/check/resource observations, a fresh mandate and grant |
| Flagship | direct → prior → adaptive | 60 | $126.00 | One independently selected job replicated with equal facts/controls and three distinct signed configurations |
| Transfer | prior → adaptive → direct | 24 | $50.40 | One materially different service job, same-arm frozen lineage and fresh grants |
| Nonreuse | adaptive | 6 | $12.60 | An actual retained candidate and independently supplied applicability challenge |
| Ablation | masked → candidate | 16 | $33.60 | One matched complete service workflow and two roots from the same frozen adaptive backup |

The order was fixed by the development assistant before any trial result. It is not randomized or a counterbalanced experiment. Position rotation across flagship and transfer is disclosed; it does not support statistical claims. A failed or blocked stage is retained. Under the current coordinator, later stages do not bypass an incomplete predecessor; an unavailable candidate or independent case leaves affected work not run and its allocation unused.

The initial development root is `var/commercial-release-live-development-v4`. Its exact unsigned journey proposal hash is `91b072bc466fb198d6de7c60abbcb4aa30f0ddd98283ad5d6829daf866b19f28`, maximum 28 calls/$58.80, expiry `2026-09-22T22:00:00.000Z`. Earlier V2/V3 roots remain preserved and superseded. Its actual adaptive acquisition scope is **eight requests per task, 16 MiB per request, 15-second acquisition deadline**, with only `docs.python.org`, `developer.mozilla.org` and `registry.npmjs.org` allowed. Commands and private services are enabled in the exact mandate; worker networking stays off. The ordinary bounded public discovery session is separately bound by the journey packet. These are the existing prepared settings, not a request to add hosts or increase limits.

The initial stage's unsigned proposal reference is concrete, while its signed `bindingHash` is deferred because signing changes the verified authority payload. After explicit approval and installation of the actual signed grant, `inspect-stage` obtains the real snapshot without provider dispatch. Use the resulting `bindingHash`, `snapshot.configurationHash`, `snapshot.workerFactsHash` and `lineageHash: null` in a copy of the first manifest stage. `bind-chunk` takes a one-element array for development; its predecessor set is empty. No independent case needs to exist before development can start under the global coordinator.

The relevant existing commands are:

```powershell
node tools/adaptive-trial.mjs help
node tools/adaptive-trial.mjs inspect-stage --stage-root C:\Users\14844\Downloads\MIDAS\var\commercial-operator-release-worktree\var\commercial-release-live-development-v4 --runner outcome-journey --arm adaptive-midas
node tools/adaptive-trial.mjs preflight --root C:\Users\14844\Downloads\MIDAS\var\commercial-operator-release-worktree\var\commercial-release-trial-coordinator-v1 --manifest C:\Users\14844\Downloads\MIDAS\var\commercial-operator-release-worktree\docs\commercial-release\trial-manifest.v1.json
```

The inspection currently requires the missing actual signed grant. Preflight can report deferred bindings, but does not authorize or fill them. The pure structural validation already performed did not create a coordinator database or modify the development state. The exact manifest hash is in `trial-preparation.v1.json`; `bind-chunk`, `run` and `resume` require that hash. Future bound stage arrays retain the manifest's IDs, roots, arms, phases, case IDs, order and allowances. Use only snapshots from the real signed loaders, never a development-authored substitute or a hash of an unsigned envelope.

All three flagship stages bind together. Direct, prior and adaptive use the actual `direct`, `baseline` and `lean` procedures through the same tool adapter. Shared semantic-facts comparison omits only the intentional prompt selector and its derived plan-intent hash; full task/facts/configuration bindings remain checked. Flagship and transfer require equal observed facts/model/evidence/tools/resources. The ablation's intentional candidate-list difference is accepted only when the stricter same-backup, normalized-facts, model, evidence, tool and resource predicate passes with one empty and one nonempty eligible policy. It does not waive other phases' controls.

Before transfer, prepare V4 lineage from each arm's completed, stopped flagship root into that arm's new transfer root. Use `release-trial-sandbox` as the common sandbox business ID across the three isolated arm roots, with the same independently supplied business/source/task IDs. This gives comparable records without cross-arm material sharing. The actual retained asset differs by arm and is reported as learning history; transfer does not assert cross-business authorization. Nonreuse and both ablation roots use the completed adaptive transfer root as the source, preserving that source unchanged. The ablation copies must have the same backup hash; each copy gets a new grant/account scope and the explicit immutable candidate policy. A missing candidate cannot be replaced by a synthetic one.

The grader record and signatures stay outside worker context. The rubric defines evidence-based human judgments and critical failures; the coordinator reports mechanical results and keeps independent semantic/customer outcomes unknown. A named independently controlled custodian and assessor remain unavailable. A label in this manifest does not establish either identity, protected custody or adoption authority. Their actual selection/exposure and assessment records are required before an independent claim is made.

Current checks cover structure, exact aggregate allocation, real current implementation hash, sealed document links, the unchanged exact V4 unsigned proposal, fixed order and deferred-binding rules. Fifteen coordinator regressions pass with zero skips; the independent reviewer reran them. Strict TypeScript checking reports 101 source files and zero errors. Actual signed initial-loader validation, future candidate/material bindings, current account/model access, execution outcomes, independent assessment and commercial value necessarily await their real inputs and authority.
