# Exact unsigned outcome proposal — Mission 033

**No execution authority has been granted.** This is a new public-evidence outcome experiment, separate from Mission 031/R5 and all earlier budgets.

Proposal SHA-256: **`846b6c5652d810f245382f48fe9776d4b71fc6d327201612f1b5803ad602a4a7`**.

Exact payload: `proposal/pilot.intelligence.authorization.request.json`. Exact first Responses body, local schema and request: `proposal/intelligence-exact-initial-request.json`. First serialized body is **14,364 UTF-8 bytes**; its provider token count has not been requested. Configuration and request audit are beside them. Do not sign a modified payload under this hash.

## Useful result and release sequence

The question is whether an authorized strong generalist can choose consequential public evidence, develop specific sourced commercial opportunities, and turn one approved opportunity into a consistent campaign and checked local page through the existing controller. A generic report, unsupported claims or unusable page is a failure of the relevant stage, even if its JSON parses.

1. **Investigation:** start with the actual retained FitKitty homepage prefix, owner objective and 44 discovered links. The worker chooses further permitted reads, rendered evidence or focused source/image inspection. Six decisions include its substantive `finish` action; it must preserve unresolved limitations. The supplied Claude report is excluded.
2. **Analysis:** produce the complete source-linked assessment, alternatives, contradictions, economic unknowns, priorities and executable deliverable proposals. No mandatory favorable conclusion or invented opportunity.
3. **Owner selection:** the dashboard asks Mason to select or reject one supported opportunity. This is a work-direction choice, not another numerical spending grant. If none is supported, stop with the assessment and preserve the unspent allowance.
4. **Delivery:** from blank sources, the existing signed worker creates the coordinated seven-asset campaign and then a page inheriting its exact foundation. Source edits, targeted repairs, actual browser observations, deterministic checks/publication/readback and completion are inside these stage caps. The workspace can complete campaign then page without asking for each ordinary step.
5. **Optional correction:** up to four additional decisions for one exact current artifact and a recorded owner instruction. This does not authorize independent model judges or automatic procedure comparison.

Both drafting and review use a strong generalist procedure with the same company facts, evidence and useful tools. No specialist advantage is assumed. This is a single-business operating observation, not a paired superiority experiment or customer validation.

## Exact scope and cost ceiling

| Stage | Maximum inference admissions | Maximum inference reservation |
|---|---:|---:|
| Investigation, including finish | 6 | $12.30 |
| Commercial analysis | 1 | $2.05 |
| Coordinated campaign | 6 | $12.30 |
| Marketing-page implementation/checks/repair | 8 | $16.40 |
| Optional exact-artifact correction | 4 | $8.20 |
| **Inference maximum** | **25** | **$51.25** |
| Single unpriced count/retrieval allowance | — | **$2.75** |
| **Maximum new aggregate exposure** | **25 inference + 25 supporting count requests** | **$54.00** |

No stage transfers, retries, replacement inference, fallback, probes or hidden helpers. The old Mission 031 retained **$19.17** stays separate; it is neither charged into this new proposal nor released/transferred. New Mission 033 actual inference/count usage and retained exposure are zero.

Route: direct `POST https://api.openai.com/v1/responses`, **`gpt-6-astra`, reasoning `max`, default tier**. Per admission: **32,768 complete-input tokens**, **32,768 total output tokens including reasoning**, **$2.05** maximum reservation. Exact input admission uses `POST /v1/responses/input_tokens`, including structured schema, instructions, image content when present, and all transmitted input. Maximum serialized count body is **196,608 bytes**. A provider count exceeding the ceiling blocks inference; no guessed character-count substitute.

Official documentation rechecked September 14 UTC / September 13, 2026 local confirms Responses, Max, structured outputs and image input. Rates are $10/M ordinary input, $12.50/M cache-write input and $50/M output. The conservative calculation is `ceil((32768 × 12.50 + 32768 × 50) / 1,000,000 × 100) = 205 cents`. The reused route's effective-price field remains September 12; this re-verification confirms the same numerical ceiling. [Official model and pricing](https://developers.openai.com/api/docs/models/gpt-6-astra), [image-input semantics](https://developers.openai.com/api/docs/guides/images-vision).

The $2.75 allowance is an explicitly unpriced uncertainty buffer, not an asserted count or GET fee. A local admission cap does not guarantee external billing limits. Missing provider usage remains unknown; estimates are provisional; reservations are retained until supported reconciliation. No billing signatory is configured in this unsigned packet, so authoritative settlements require actual project billing records and separately bound reconciliation authority. A signature alone cannot establish the charge.

## Deadlines, privacy and recovery

Concurrency one. Count timeout 10 seconds. Background create deadline 60 seconds. Poll every 5 seconds, individual retrieval deadline 15 seconds, normal completion window 15 minutes, bounded same-ID resumption window 24 hours, at most 240 GETs per response across restart, stop after three consecutive read errors. All gates also stop at **September 25, 2026, 22:00 UTC**.

Explicit `background:true, store:true`; approval must include provider retention of **at least 30 days** under the disclosed storage policy. Only the selected company's initial data and this bound public discovery session may enter the requests. No credentials, source report answer key or private evaluator data enters prompts. Optional screenshots are limited to one exact bounded PNG/JPEG per applicable request and retain image identity/provenance. Images increase admitted input and remain within the same ceiling. [Official background semantics](https://developers.openai.com/api/docs/guides/background).

The protected project must support that route, billing, input counting and stored background Responses. Project association does not prove key access, available credits or route entitlement. No access probe has been made. A failed first useful request is preserved and blocks affected continuation; it is not automatically retried. Unknown creation without a response identity cannot be resent. With a persisted identity, only the existing response is retrieved within the signed bounds. Terminal failed/incomplete responses stay distinct from unknown completion.

**Correction limit:** the optional correction can revise a current campaign or page. A campaign correction after page publication invalidates that page. A fresh linked replacement page is implemented and verified offline, but is excluded from this packet's paid tasks; it must remain blocked pending separately approved scope. Prefer reviewing the finished offer and correcting the page when that satisfies the real need. Do not pretend an invalidated dependent page is current or borrow another stage's calls. This limitation is explicit before approval.

## Exact owner/account steps

1. Confirm this **public-only FitKitty reference investigation** is the desired first run and approve the exact hash, $54 maximum, storage and exclusions. It does not assume that Mason owns FitKitty or authorizes account access. A different business changes the binding and requires a fresh unsigned packet.
2. Verify the existing project `proj_H01ORqdOPQM6vdGwQYsqFL5r` has credits/access and permits the route/storage. Reuse the previously approved protected file at `C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/provider/openai.key`. Its path was recovered from the preserved public authorization configuration; its contents were not opened, copied or displayed.
3. After exact approval, the operator signs through the existing protected owner keys. No new account, credential staging or infrastructure setup is required if those connections still work. No signing action has occurred in this release.
4. After the assessment, select one supported opportunity in the workspace. Inspect the final artifacts and supply a specific correction only if material. Independent human timing/expertise remains unknown unless actually measured; approval, usefulness and commercial outcome are separate records.

No social login, Gmail, hosting, customer contact, purchases, payments or publication account is required or included. The optional image input is public/synthetic retained evidence, not hidden account content.

## Prepared commands after approval

Run from `C:/Users/14844/Downloads/MIDAS/var/business-intelligence-worktree-033`. The signing command must be invoked only after actual approval; replace the final reference with that real approval, never a fabricated record.

```powershell
node packages/foundry/src/pilot/cli.ts sign-intelligence --root var/business-intelligence-033 --proposal docs/intelligence/proposal/pilot.intelligence.authorization.request.json --approve-proposal-hash 846b6c5652d810f245382f48fe9776d4b71fc6d327201612f1b5803ad602a4a7 --principal Mason --approval-reference "ACTUAL_OWNER_APPROVAL_OF_EXACT_033_PACKET" --owner-public-key C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/owner.pub --owner-private-key C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/owner.key
node packages/foundry/src/pilot/cli.ts run-intelligence-approved --root var/business-intelligence-033
```

The same execution command resumes after opportunity selection. Alternatively use **Run approved investigation** and **Continue approved work** in the workspace; they use the same signature and ledger. Completed analysis/work is reused. For an exact covered owner-correction task, `run-intelligence-approved --root var/business-intelligence-033 --task <persisted-task-id>` resumes only that task.

Success yields actual model-attributed sourced findings, a justified selection or rejection, coordinated editable assets, a browser-checked local page, preserved revisions and an evidence-backed next action. Similar adequacy retains the single generalist; a consequential omission or correction becomes an evidence-linked candidate for a future fair test. No automatic qualification, production promotion or economic success follows from local completion.
