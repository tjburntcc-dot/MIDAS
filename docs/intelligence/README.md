# MIDAS business intelligence and execution release 033

Open **http://127.0.0.1:43144/**. Select **FitKitty — public evidence workspace** at the top. Read the retained source and investigation details. This is an actual public retrieval workspace, prepared by the development assistant; it has no model-authored business assessment yet. The supplied Claude report remains a separate reference and was not loaded into worker evidence.

Open **Archive** and choose the saved **Signal Bench** service or **Moss & Kettle** retail demonstration. In Intelligence, inspect the full sourced proposal and alternatives. In Work, open the corrected campaign and the latest marketing page, use its local form and preview, and compare earlier versions. These are explicit scripted fixtures exercising the actual controller and browser, not examples of measured model competence.

## Run and inspect

From `C:/Users/14844/Downloads/MIDAS/var/business-intelligence-worktree-033`:

```powershell
node packages/foundry/src/pilot/cli.ts serve --root var/business-intelligence-033 --port 43144
node packages/foundry/src/pilot/cli.ts status --root var/business-intelligence-033 --business company-cb653fd4af0d4b81b679
node scripts/intelligence-offline-demo.mjs --root var/business-intelligence-033
```

The demonstration command reuses its persisted completed examples. It cannot access a provider, credential or public site. Do not start a second server if 43144 is already listening. The earlier 032/43143 and R5/43142 servers are preserved. Use the existing pilot backup/restore commands for SQLite and task artifacts; stop the server before replacing a data directory.

## What is connected

| Owner operation | Executable connection and verification | Evidence boundary |
|---|---|---|
| Website, social links and objective | `pilot/server.ts` → `PilotService.createBusiness` → `PilotDiscovery.seed`; robots, DNS/socket pinning, durable sources/frontier | Real public homepage retrieval verified. Follow-up choices require an approved ModelPort. |
| Worker-directed investigation | `PilotDiscovery.next` → bounded read/follow/render/read-source/finish; exact request/response identity | Mock-verified; no live commercial investigation in 033. |
| Visual evidence | Isolated retained HTML/CSS render, screenshot or owner image; optional bounded `input_image`, complete count projection | Browser rendering is actual. The sanitized view excludes external assets/scripts. No actual model image analysis occurred. |
| Business understanding | `PilotIntelligence.analyze` → strict sourced claims, contradictions, unknowns, full report and opportunity graph → persistent versions | Observations, estimates, assumptions and hypotheses are distinct. References are deterministic checks; causal usefulness remains a proposal. |
| Prioritized work | Owner selects or rejects a current opportunity; stable planning intent → registry assignment → campaign dependency → marketing page | Priority is model-proposed. Selection is human-owned. Unknown competence stays unqualified. Unsupported jobs cannot run. |
| Coordinated delivery | Shared `PortfolioEngine`, blank source, targeted edits, canonical campaign foundation, local finalization and authenticated artifact identities | Seven coordinated business assets plus working page demonstrated with fixtures. No sent campaign or deployment. |
| Correction and feedback | Exact artifact correction, current browser checks, stale-dependent invalidation, fresh linked page, outcome → stale assessment/next-action revision | Actual software behavior verified. Scripted owner observations are not real customer feedback or revenue. |
| Worker development | `PilotLearning` and `CommercialReview` → observed correction/omission → scoped candidate → existing comparison/selection registry | Fixture comparison retains the strong baseline; no qualified specialist or measured advantage. |
| Authorized live continuation | `intelligence-authorized.ts` → existing `OperatingModels`, scoped ledger, durable Responses; server/CLI use the same binding | Signed mock path verified including correction. New exact approval is required; R5 remains unsigned. |

## Actual public source and limits

On September 14, 2026 UTC (September 13 locally), the repaired reader retained the first **350,000 bytes** of the FitKitty homepage, extracted bounded visible text and discovered **44 links**. The source describes its catalogue and advertised shipping/sale policies. Those are publisher assertions, not verified fulfillment, demand or economics. The remaining body was aborted; unseen material may change the assessment. Hashes identify the exact retained prefix, not the complete page.

Two earlier failures remain preserved: Node24's DNS callback shape prevented a request, then the original strict body cap rejected an oversized homepage. The reader now supports explicitly partial capture only for discovery page reads; robots and historical research defaults remain strict. New linked sessions document each repair. No historical result was silently changed.

Authenticated social content, paywalls and account-only data are unavailable. Public social pages use the same access checks. Owners can add permitted screenshots or exports. The browser-rendered preview is a sanitized retained view, not a fully loaded original website. Text-only model requests cannot establish visual inspection.

## Readable evidence

Under `var/business-intelligence-033`:

- `fitkitty-public-retrieval-v2.json`: actual source status, partial coverage and frontier.
- `reports/offline-demonstration.json`: both complete archive workflows and corrected artifact identities.
- `reports/service/campaign.json`, `reports/retail/campaign.json`: corrected coordinated campaign assets.
- `reports/service/page.html`, `reports/retail/page.html`: exported test pages; use the workspace preview for contained persistent interaction.
- `reports/browser/`: desktop/phone screenshots and actual browser inspection receipt.
- `pilot.sqlite`: sources, requests, decisions, work, corrections and immutable record history.

The controller can finish local checks, publication and readback without separate paid judgments. It cannot invent semantic acceptance, publish externally, send messages, access accounts or claim commercial outcomes.

## Verification and unresolved evidence

Focused suites cover discovery failures and child-process restart; both business types; source permission withdrawal and stale company context; lost model acknowledgement and same-ID retrieval; terminal failures without usage; exact stage/aggregate caps; rejected recommendations and invalid artifacts; campaign/page consistency; local publication identity; owner correction and stale approval; strong-baseline retention; and actual desktop/phone controls.

The 26-test execution/authorization regression passed. The 45-test UI/model/finalization group exposed a context-change message mismatch, corrected and retested successfully; its other 44 tests passed. Eighteen discovery/research checks and nine intelligence/server checks pass. Strict installed TypeScript 6.0.3 checked 61 scoped source files with zero diagnostics. No dependencies were installed. These tests establish implementation mechanics, not commercial quality.

The service fixture guided development. The materially different retail case exercises transfer through the same contracts, with no company/industry branches in reusable procedures. It is developer-visible exploratory material, not a confidential holdout. No independent model-quality comparison, independent correction timing, customer demand or economic improvement has been measured. The original Claude prompt/model/settings/connectors, full retrieval trail, timing and cost were not supplied, so parity with that baseline is unestablished.

Remaining external requirements are exact provider approval and verified project access/credits for the new packet, followed by a human opportunity selection and semantic review. The public-only first packet needs no Gmail, social login, deployment or customer-contact permission. Real private company records can later deepen the assessment under a revised scoped authorization.
