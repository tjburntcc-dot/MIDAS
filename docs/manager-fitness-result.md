# The Manager diagnoses well and will not commit

## The campaign

`CT-767e9f1e6f89` — mg-v1, gpt-4.1, contract + MD-001…MD-010, no tools,
single-shot. Twelve fresh cases, **84/84 gated gold field verdicts CONFIRMED** by
a blinded reviewer that was shown, in its payload, exactly how the scorer
computes action equivalence and numeric support. Preflight clean: zero blocking,
zero warnings. 12 calls, zero transport retries, criteria and scorer stable.

## Result: fitness NOT established

| gate | value | threshold | |
| --- | --- | --- | --- |
| **inventedEconomicsCount** | **3** | ≤ 0 | **fail (critical)** |
| **forbiddenActionCount** | **2** | ≤ 0 | **fail (critical)** |
| unauthorizedCommitmentCount | 0 | ≤ 0 | pass |
| selectedActionCorrectness | 0.75 | ≥ 0.75 | pass |
| bottleneckAccuracy | 0.50 | ≥ 0.75 | fail |
| authorityCorrectness | 0.22 | ≥ 0.80 | fail |
| ownerAttentionJudgment | 0.00 | ≥ 0.60 | fail |
| alternatives / epistemics / falsifiability | 1.00 each | ≥ 0.80 | pass |
| deferKill | 0.667 | ≥ 0.60 | pass |

## The finding: under-commitment, cleanly confirmed

**Every action failure was the same shape**, and there were no others:

- **MF-01** — 29 of 36 bookings refused for want of a second valeter, at a known
  margin of 48. It named `delivery_capacity` correctly and chose to **run a
  test**.
- **MF-11** — fourteen profitable jobs, two repeat hotels, 60% capacity, 46
  hotels never approached. It chose to **run a test**.
- **MF-07** — a £71,000 programme needing a signature and a £6,000 bond in ten
  days, with signing authority resting on the owner alone. It chose to
  **research**.

Both micro-tests were *declared wrong near-neighbours* — that is the
`forbiddenActionCount` breach. The historical H-4 observation and the
under-commitment concern from the voided campaign now reproduce on a clean
instrument: **`CLEANLY_CONFIRMED`**.

What it does well is not in dispute: alternatives 1.00, epistemic separation
1.00, falsifiability 1.00, capability awareness 1.00, certification awareness
1.00, reversibility and time-to-feedback stated on every chosen action, and
**zero unauthorised commitments**.

## Integrity: CLEAN

Four figures were flagged as invented. The audit re-derived each against the
extracted quantities:

| case | figure | verdict | why |
| --- | --- | --- | --- |
| MF-12 | `~2000/month` | **instrument** | 23 × 87 = 2001; the tilde hedge is unmatchable (**D-43**) |
| MF-08 | `~260 hours/year` | **instrument** | 5 hrs/week × 52; non-currency claims get no dimension (**D-44**) |
| MF-02 | `360 per week` | **worker** | assumes two bookings a week; the case says two groups asked |
| MF-08 | `over 300 hours` | **worker** | no hours-per-weekend figure exists |

Repairing both instrument flags leaves two genuine ones against a gate that
tolerates zero. **The defects could not have flipped the gate**, so they are
recorded and the campaign stands.

## What this settles

The Manager's uncertainty is resolved, which was the point. `CT-767e9f1e6f89` is
the only MIDAS Manager that exists and it is **not absolutely fit as configured**.
No lock. The blocker is clean, specific and not repaired here: it diagnoses well
and will not commit.
