# Adaptive Operator implementation handoff

14 September 2026. **The integrated engineering path is implemented and verified offline. The primary live unfamiliar-obstacle milestone remains unproven.** No fixture is promoted to model competence.

## 1. OPEN THIS

[Owner workspace launch and walkthrough](RUNBOOK.md). Run the two commands there and open the resulting local MIDAS interface. The Adaptive view is part of the existing product, with persisted business-scoped episodes, commands, candidate skills and new-assignment preparation. No externally accessible deployment was made.

## 2. WATCH THIS JOURNEY

The reproducible fixture begins with an incorrect comma-delimiter assumption. Its first transformation rejects the pipe-delimited header. A targeted workbench edit changes the parser configuration. Real transformation checks pass; the same existing controller resumes and finalizes the original operating packet. A different task imports the candidate, checks its precondition and processes a second input. All episodes and results survive restart and backup/restore. Exact steps are in the runbook.

## 3. WHAT MIDAS SOLVED WITHOUT BEING GIVEN THE SOLUTION

**No such runtime claim is established.** The fixture's obstacle and decisions were scripted. Development work independently diagnosed actual signed-runner tool-envelope and schema mismatches, restore identity tied to absolute paths, omitted adaptive backup files, missing finishing feasibility, and browser-module resolution. These were development-assistant repairs, not MIDAS worker discoveries. A developer-unseen live obstacle remains a required next evaluation.

## 4. WHAT CAPABILITY IT ACQUIRED OR BUILT

Reusable execution mechanics: business/task-scoped multi-file operations, exact hash-bound patches, durable intent/effect receipts, isolated command port, interruption reconciliation without redispatch, evidence-backed acquisition episodes, current-file verification, parent resumption, unqualified candidate retention and later applicability/results. The sample candidate is a simple delimiter configuration; it is not an independently learned general parser.

Existing baseline instructions remain available. The lean candidate removes redundant scaffolding while preserving evidence, tool contracts, obligations and authority. A three-arm comparison protocol covers a strong direct worker, current MIDAS and adaptive MIDAS; it has not been run or scored.

## 5. WHAT ACTUALLY RAN

- Shared-controller fixture acquisition and second-task reuse; actual file mutations, transformations, deterministic checks, local artifact publication and readback.
- Signed adaptive preparation/load/run with ephemeral keys and injected count/background transport, followed by no-extra-dispatch replay. This caught and repaired two integration defects before any real paid run.
- Scope, stale-context/hash, forged evidence, unknown command, restart, capacity, candidate import and backup regressions.
- Actual owner HTTP service plus DOM execution of its app: saved state rendering, acquisition/reuse display, preparation dialog, CSRF form submission and persisted assignment. DOM verification is **not** browser rendering or mobile layout acceptance.
- Backup/restore preserved 16 workbench/receipt sidecars; restored execution defaults off.
- Strict TypeScript semantic checks. Exact results and reproducible test files are in [VERIFICATION.json](VERIFICATION.json).

A broader adjacent run had 54 passing tests and one failure: the historical signed business-site browser journey could not launch the absent Chromium binary. Its preserved check says `site.journey`, `Actual browser journey stopped at startup`. The sanctioned browser download was blocked by network policy; the cloud browser denied localhost. The failure was not hidden, converted to success, or bypassed with a fake browser.

Bubblewrap namespace probes failed on this host. The maintained Codex app-server was evaluated, but the installed stable protocol did not establish the necessary restricted read isolation. No unrestricted host backend was substituted. These environment limits prevented actual isolated command and unseen-worker execution here. See the [executor evidence](EXECUTOR-CHOICE.md).

## 6. WHAT IT LEARNED AND WHETHER THAT LEARNING TRANSFERRED

The registry retains acquisition evidence, package hashes, required conditions and later results. Candidates remain unqualified. A second **scripted** task reused the sample successfully, proving mechanics only. No live transfer, specialist advantage, direct-baseline superiority or economic benefit was measured. Self-authored passing checks cannot certify a worker.

## 7. REPOSITORY / IMPLEMENTATION IDENTITY

Repository `tjburntcc-dot/MIDAS`; independently fetched and verified completed base `15cb7731803e03344deee97f8608a14426f7d4dd`. Integration branch `codex/adaptive-operator-integration`. Historical Mission034 and prior grants were not edited or reused. New adaptive code is included in the existing pilot implementation binding; opt-in task identity also freezes effect authority. The original nonadaptive tool/schema path is preserved.

The code changes live under `packages/foundry/src/adaptive` and the existing pilot/portfolio integration points. The existing execution plan was updated rather than replaced by a competing roadmap. Final Git identity is supplied by the review commit/PR, avoiding a self-referential hash in this file.

## 8. RESOURCE USE AND UNKNOWN ACCOUNTING

Zero real provider inference, count or retrieval calls; zero provider credential reads, connected-account reads, new live signatures or new provider reservations. Public documentation/package downloads and local tests occurred. Development subscription usage, local compute and labor were not measured. Historical Mission031's reported $19.17 retained exposure remains separate, with authoritative settlement unavailable. This implementation grants no spending, customer communication, purchase or deployment authority.

## 9. NEXT THREE READY ACTIONS

1. Run the owner walkthrough from this branch and inspect the real connected state and locally finalized fixture packets.
2. Run the isolated executor and browser acceptance on a compatible host; complete the missing documentation/package acquisition adapter only within an explicit read/network scope. This is a remaining capability limitation, not a request to disable isolation.
3. Under a fresh exact execution scope, give a worker an independently selected unfamiliar obstacle and later unseen transfer task. Compare direct/current/adaptive workflows with comparable evidence, tools, limits and measured owner intervention. Preserve failures and reject the candidate if it does not transfer.

The implementation removes several concrete blockers, but does not establish the requested AGI-like milestone. No live approval is being requested for an untested replacement runtime.
