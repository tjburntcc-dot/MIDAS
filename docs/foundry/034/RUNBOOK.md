# Mission 034 — use the integrated owner workspace

Open **http://127.0.0.1:43145/**. The retained root is `var/overnight-product-034` in `C:/Users/14844/Downloads/MIDAS/var/overnight-product-worktree-034`. This is the continuing product release; previous workspaces and experiments are preserved.

## Try the connected result

1. In the explicit demonstration/archive selector, open **Signal Bench — synthetic analytics service** or **Moss & Kettle — synthetic tea retailer**. These are different fixture businesses using the same controller. Their findings and worker outputs are test-double results, never measured AI competence.
2. Open **Outcomes**. Inspect the source-grounded plan, selected strong generalist, operating brief and dependent application. Both current outcomes completed after an assisted fixture correction changed the parent brief and regenerated its dependent application. Older versions remain available.
3. Open the application deliverable and its **interactive preview**. Create a labeled local record, reopen and edit it, mark it reviewed, refresh, and export/read back the saved JSON. A real contained backend stores these records. Close the preview when finished.
4. Inspect **Connections**: three maintained read families, consent, exact scope, imported evidence and limitations. Fixture-configured accounts remain explicitly fixtures. Configuring a real connection does not authenticate it or perform a read.
5. Inspect **Learning**: an actual retained correction links to a versioned candidate and comparison. The fixture comparison retains the strong baseline. No independent semantic review or specialist advantage is claimed.
6. Select **FitKitty Culture — public-only preparation** for the prospective real run. This workspace was prepared by the development assistant from the user-named reference, with ownership unconfirmed. It contains a real bounded public homepage retrieval and a current link frontier; it contains no new model analysis. The reference audit and its proposed answers are excluded from worker evidence.
7. A new business can start from a name, website and objective. Add permitted documents or configure reads; inspect evidence selection before approving work. The source selection can be narrowed or explicitly include future permitted sources. Revocation and existing signed scope remain authoritative.

The owner can record a correction, artifact outcome and assistance honestly. Those observations change future context and the next recommendation. Acceptance is not demand, revenue or causal improvement. Do not enter real customer data into the fixture workspace.

## Launch, status and continuation

Use the existing Node 24.19+ and installed browser dependencies. From the 034 worktree:

```powershell
node packages/foundry/src/pilot/cli.ts serve --root var/overnight-product-034 --port 43145
node packages/foundry/src/pilot/cli.ts journey status --root C:/Users/14844/Downloads/MIDAS/var/overnight-product-worktree-034/var/overnight-product-034
```

Do not start another server on an occupied port. The current root records `owner-service.json` with its exact PID. Stop only that verified process. Restart with the same root to retain state. The local owner session expires; application and business state do not.

No account credentials are accessed on server startup. Public HTTPS retrieval requires permitted outbound networking; a sandbox `EACCES` is an environment failure, not absence of a website. Signed, current scoped authority is required for all model calls and account reads, even when the server has network access.

An approved outcome can run from **Continue approved outcome** or:

```powershell
node packages/foundry/src/pilot/cli.ts journey run --root C:/Users/14844/Downloads/MIDAS/var/overnight-product-worktree-034/var/overnight-product-034
node packages/foundry/src/pilot/cli.ts journey resume --root C:/Users/14844/Downloads/MIDAS/var/overnight-product-worktree-034/var/overnight-product-034
```

These commands load the installed exact grant; they cannot receive a model, key or replacement-attempt override. They first recover persisted results or the same known provider response. Unknown create completion without a response identity stops, retains exposure and never silently resubmits. Pausing preserves state; it does not claim to cancel provider billing for already admitted work.

## Account and deployment dependencies

[CONNECTIONS.md](CONNECTIONS.md) provides exact per-account configuration, consent, protected token-file and signed read-host commands. Google Workspace, Shopify and GA4 are implemented and transport-tested; none was authenticated live in034. Additional social/CRM/messaging products are not represented as installed adapters. Permitted exports and transcripts remain usable inputs.

Back up while the034 server and project previews are stopped. The v2 snapshot includes SQLite plus allowed functional-project source and runtime state, rejects links/path escapes, and authenticates every included file. Credentials and signing files are excluded. Restored data is not restored execution authority. See the exact backup/restore commands in [CONNECTIONS.md](CONNECTIONS.md).

The existing `deploy/pilot/midas-pilot.service` is the prepared private-host path. Use an approved Node/browser host, protected persistent volume and an authenticated SSH tunnel; keep the service bound to loopback. Set this034 checkout/root/port explicitly. Host/account access, remote dependency installation, provisioning and deployment require separate authorization. Public multi-owner hosting requires an identity/TLS boundary and additional tenancy review; it is not completed by this local release.

## What controls the result

| Component | Implemented behavior and limit |
|---|---|
| Discovery and intelligence | A prospective model chooses bounded public retrievals, focuses retained permitted sources, analyzes their support and proposes opportunities. Deterministic extraction is not visual intelligence. Screenshots can be retained; no actual-model image inspection occurred here. |
| Plan and assignment | The model can prepare, block or reject work and propose dependencies/competencies. Deterministic validation checks sources, task capabilities, allowances and registry selection. Strong generalist remains the selected worker because superiority is unproven. |
| Execution | Model workers edit current sources, inspect rejected candidates/checks and make source-specific completion judgments. Ordinary checks, publication/readback and closure are deterministic. All prospective inference uses the same grant and ledger. |
| Functional software | Four worker-editable source files plus a trusted declarative local backend; validation, state transitions, persistent records, browser tests and preview. Arbitrary backend code, shell commands, package installation, authentication/payments and outbound application networking are unsupported. |
| Review and correction | Correct the authoritative parent, invalidate prior dependent work, and reserve/rebuild its descendants from the actual new artifact. If scope or capacity is insufficient, show the blocker. No stale artifact becomes current by relabeling it. |
| Adaptation and learning | Recorded outcomes/corrections change context and rule-based next actions. A versioned candidate can enter a signed matched comparison. Self-asserted independence cannot qualify/adopt a worker; genuine independent evaluation and measured benefit remain external gates. |

## Verification artifacts

`var/overnight-product-034/final-boundaries.log`: scoped authority, same-ID recovery, interrupted withdrawal, atomic capacity, HTTP journey, comparison and connection-host tests. `regression-core.log`: relevant core regressions. Exact browser report and screenshots: `packages/foundry/var/overnight-product-034/browser-evidence/owner-journey-2026-09-14T03-46-01-824Z.json` and adjacent PNGs. These inspect the actual034 server and persisted application records, with zero external browser requests.

The historical `portfolio-stage-contracts` byte-snapshot mismatch occurs identically on the033 base and034; its nine payloads remain within72,000bytes. Historical expected values were not rewritten. This limitation is separate from the passing new release checks.
