# Exact unsigned outcome execution request — Mission 034

Prepared 14 September2026 after implementation and offline verification. **Not signed, not executed.** This does not inherit R5 or033 authority. Historical031 retained exposure is **$19.17**, with authoritative settlement unavailable; it is displayed separately and never contributes spending permission here.

## Owner decision

Approve this exact proposal, maximum **USD72 new exposure**:

`77b292260e84368e7b232dc0c7b21f9bc69eb275ed28ebb28811b4ca086fd479`

The proposal is `var/overnight-product-034/preparations/company-1b47bfe5a3d54b21b430/journey-034-7c147482c12f4a0a8c52201d37a7a09b/pilot.outcome.journey.authorization.request.json` in this worktree. The same directory contains the initial model request, exact canonical Responses bytes, discovery schema and execution limits. It is also downloadable from the existing owner workspace's prepared outcome.

This covers one public-only business outcome for the user-named FitKitty reference. The development assistant prepared the workspace and retained its public homepage; this is not a claim of business ownership or a model-authored diagnosis. The original audit and its answers are not supplied to workers. The initial record is explicitly a partial350KB homepage prefix, with useful text and44 observed links; independent follow-up remains the model's job. No private account read is needed for this first proposal.

## Result this buys the opportunity to observe

The worker investigates consequential uncertainty, produces a sourced commercial assessment, and makes a real prepare/block/reject decision. If justified, it chooses the smallest supported dependency graph, authors actual local operating/creative deliverables and a website or functional application where useful, inspects checks and browser observations, and delivers current authenticated artifacts. The owner sees current sources, reasoning, assignments, outputs, unresolved conditions and accounting in one workspace.

An adequate result may remain unchanged. A genuine rejection cancels dependent work. The grant can support an in-scope correction and dependent regeneration, but does not purchase a predetermined application, a favorable diagnosis or a specialist win. A commercial observation or material owner correction can change subsequent recommendations; no artifact automatically proves demand or revenue.

## Exact route and allocations

Direct OpenAI Responses, project `proj_H01ORqdOPQM6vdGwQYsqFL5r`, `gpt-6-astra`, **Max**, default tier. No fallback, hosted search, auxiliary judge or optimizer. Both reasoning and final output share32,768 output tokens; complete admitted input ceiling32,768 tokens. Global provider concurrency1. Maximum33 inference admissions and33 supporting counts.

| Stage | Maximum admissions | Maximum inference reservation |
|---|---:|---:|
| Bounded discovery | 6 | $12.30 |
| Business analysis | 1 | $2.05 |
| Substantive outcome planner | 1 | $2.05 |
| Primary coordinated work | 13 | $26.65 |
| Held correction and dependent regeneration | 12 | $24.60 |
| **Inference total** | **33** | **$67.65** |

Add **$4.35 unpriced uncertainty reserve** for supporting counts/same-ID reads: **$72 total admission ceiling**. This reserve is not a provider price. Stage caps cannot be transferred or replenished. Failed and uncertain admissions count. Prior observed costs do not imply a future charge. The local ledger controls admission, not the provider's external bill.

The13 primary calls can support a bounded brief and a multi-file project with checks/finishing; the planner can choose less work. The12 held calls can support a parent correction plus reauthoring/checking its dependent project. These are capacity ceilings, not predicted usage or instructions to exhaust the allowance. A plan that cannot finish inside its allocation is refused before graph execution.

Official model documentation checked14September2026 lists $10/M input, $12.50/M cache-write input and $50/M output. The existing route conservatively reserves `ceil(32768 × (1250 + 5000) / 1,000,000) = 205` USD minor units per admission. Max and the output ceiling are supported. Actual project entitlement, available credits and latency remain unverified; no separate probe is included. See [official Astra model documentation](https://developers.openai.com/api/docs/models/gpt-6-astra).

The canonical first request is15,028 UTF-8 bytes, SHA-256 `ce67caf179fb58943436f89bdc6c59b32424bfabc00baf6c709b87d4395d54d5`. This is a byte measurement, not a token estimate. Every request must pass the existing complete-payload count admission and196,608-byte bound before inference. Later requests bind actual discoveries, source edits and observations. No code or source substitutions after signing.

## Deadlines, durable recovery and privacy

- Expiry: **25September2026,22:00UTC**.
- Count deadline10seconds; create deadline60seconds; background completion window15minutes.
- Same-response GET deadline15seconds, polling5seconds, maximum240 reads per response, stop after3 consecutive read errors. Resume within24hours and the grant expiry.
- Persist response identity and observations before interpretation. Recover that same response; no replacement inference, fallback or automatic retry is authorized. An unknown create with no identity remains unresolved with retained reservation. Terminal failed/incomplete results preserve primary errors separately from missing billing/usage.
- `background:true`, `store:true`. This asks approval for provider-side storage, including the disclosed at-least30-day standard Responses retention. Background storage is incompatible with a ZDR policy that forbids it. See [background execution](https://developers.openai.com/api/docs/guides/background) and [data controls](https://developers.openai.com/api/docs/guides/your-data).

The supported model alias has no independently verified immutable revision in the current official catalog. Record actual returned identity, settings and timestamps. This exploratory run is not a protected comparison and cannot waive that future revision requirement.

## Access and ordinary continuation

Use the existing protected API file at `C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/provider/openai.key` and the established protected owner signing mechanism only after exact approval. The path exists; its bytes were not read, copied, logged or verified for current entitlement. Subscriptions and account credit are not spending authority.

After approval the operator signs the exact file through:

```powershell
node packages/foundry/src/pilot/cli.ts journey sign --root C:/Users/14844/Downloads/MIDAS/var/overnight-product-worktree-034/var/overnight-product-034 --proposal <exact-proposal-file-above> --approve-proposal-hash 77b292260e84368e7b232dc0c7b21f9bc69eb275ed28ebb28811b4ca086fd479 --principal Mason --approval-reference <actual-owner-approval-reference> --owner-public-key <existing-protected-public-key-file> --owner-private-key <existing-protected-private-key-file>
node packages/foundry/src/pilot/cli.ts journey run --root C:/Users/14844/Downloads/MIDAS/var/overnight-product-worktree-034/var/overnight-product-034
```

The owner need not approve routine stages or deterministic local checks. The existing workspace supports pause/resume and an exact artifact correction within remaining scope. Unsupported authority, unavailable access, changed sources/contracts, exhausted capacity or uncertainty stops affected work. Independent semantic judgments, real customer outcomes and correction time cannot be manufactured to get past a wait.

## Exclusions and later account choice

No customer communication, account mutations, purchases, ads, deployment, real transactions, protected evaluation, worker certification, model substitution, hidden helper calls or new execution grant. All artifacts remain local preparation.

Google Workspace/Shopify/GA4 account use is a **separate optional choice**, not part of this public-only proposal. Its exact business, scopes, protected token and signed40-read grant must be approved through [CONNECTIONS.md](CONNECTIONS.md). No account connection or billing-reconciliation authority is assumed merely because a person is named. Authoritative billing evidence is still needed for definitive cost qualification; the unsigned grant has no billing public key, and reservations will not be released on an unsupported assertion.
