# Mission 028: authorized synthetic-policy and exploratory-review amendment

The founder explicitly authorized this narrow contract change in the current chat. It is not a claim that the original measured-review gate passed. No governing restriction outside that authority was found for this synthetic exploratory work. The USD55 aggregate envelope, historical carry-in, stage/call ceilings, project, exact Astra High route, timeouts, expiry and protected-evaluation restrictions remain unchanged.

The original spec, grant, cases, rubric, implementation release, outputs and reviews remain in place. New files `cases.synthetic-v3.json`, `rubric.synthetic-v3.json` and signed `exploratory-amendment.json` in `var/foundry-bounded-028` bind the original grant/execution hash, new code hash, new case/rubric hashes, founder authorization and existing assisted-calibration evidence. The existing ledger/account retains every admission and reservation. No account is recreated. Each new attempt records the new task specification/rubric/amendment identity; its exact worker request also contains the versioned policy. Administrative metadata and labels remain outside worker inputs. Both conditions use the same cases and task policy.

The new synthetic policy permits verification without immediate referral for coherent pending status merely lacking collection confirmation or pending-start time. It requires referral for conflicting authoritative status, refund requests, missing usable status evidence and evidence unresolved after the permitted verification. The assistant recommends referral without claiming completion. This is an authorized synthetic task definition, not real-business policy.

All 18 existing open cases were audited. Existing escalation expectations already match the clarification. Conflict citation requirements now include both competing invoice sources. Every case explicitly checks uncertainty disclosure because even coherent pending observations lack collection confirmation. Invoice facts, source IDs, dates, timing estimates, business authority, family/split and baseline procedure remain unchanged. No case was made harder to manufacture a challenger. D-001's original acceptance is recorded under the founder's explicit interpretation of the ambiguous v2 policy; it remains assisted judgment, not retroactive independent ground truth.

The amendment admits assisted calibration and deterministic triage for exploratory development. New reviews use a separate `exploratory-assisted-review` entity with actual output/amendment bindings, declared assistance, evidence and reasons; unresolved dimensions stay null. They cannot create `experiment-review`, measured human effort, original calibration completion or protected qualification. Candidate construction still requires an actual reviewed baseline failure under the same new specification. Paired open validation, if justified, must lock the same task/policy, both roles, rubric, ordering and assisted selection rule first. Independent effort stays unknown and cannot support a correction-time advantage. Protected freeze and final analysis still require their original independent custody, review, revision and resource evidence.

Promise triage v2 distinguishes local negation, quotation and potentially asserted phrases, including mixed statements. It checks the entire user-visible response/playbook, with no cross-sentence negation suppression. Quotations can themselves endorse a promise, so quotation classification is not semantic exoneration. Lexical checks never set a critical semantic verdict: that field remains null pending assisted or human assessment. Original grades are retained; preserved outputs can be rechecked without a provider call.

Before dispatch, the 103 relevant Foundry/experiment tests passed, including changed gate behavior, signed amendment/scope/tamper checks, unchanged accounting, protected refusal and promise-context cases. Strict semantic typecheck passed using installed TypeScript 6.0.3 across 15 source files with skipLibCheck; no dependencies installed. Tests use fixtures, not actual inference.

Official pricing and request semantics were rechecked on September 11, 2026 at https://developers.openai.com/api/docs/models/gpt-6-astra and https://developers.openai.com/api/reference/typescript/resources/beta/subresources/responses/methods/create. Standard input USD10/M, cache-write ceiling USD12.50/M, output USD50/M. Conservative maximum reservation: ceil((8192*1250 + 8192*5000)/1,000,000) = 52 USD cents. The existing 52-cent admission is sufficient; count activity remains separately counted with unresolved billing. High/default, complete-payload count, 8192 input/output caps, 10s count/180s inference deadlines and concurrency one remain unchanged. No immutable snapshot beyond the alias was established; protected freeze remains blocked on revision evidence.

Planned first-pass decision: run one baseline attempt for each of the eight development cases without a development observation. First batch tests another missing-evidence case, another status conflict, and the two untested superseded-policy cases. Second batch tests the remaining coherent-pending variant, injected refund instruction and two explicit refund/account-change requests. No repeats, challenger or validation calls are justified solely by stylistic differences or by four earlier calibration passes. Reassess consequential failure mechanisms after each batch.
