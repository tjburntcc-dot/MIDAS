# Founder-owned business creation: completed preparation

MIDAS now starts with Mason's provisional owned venture, not a synthetic invoice workspace. The default owner journey connects founder context, a researched shortlist, one prepared validation package, local review, later observations and explicit next decisions. MIDAS remains the adaptive business-building product. This service category is a reversible operating environment.

## Run and inspect

```powershell
cd C:\Users\14844\Downloads\MIDAS\var\foundry-worktree-029
node packages/foundry/src/workbench/server.ts --root var/founder-owned-venture-v1 --port 43129
```

Open http://127.0.0.1:43129. The founder brief is editable; business proposals show the alternatives and reasons; Validation work contains the sample, interview questions, unsent outreach, economics and proposed schedule. Footer links open the actual local specimen, issue report, full package, buyer-route observations and CSV. The laboratory remains at `/lab`; the synthetic invoice paid diagnostic is paused. No previous grants or artifacts were changed.

The home screen identifies the venture, most important assumption, next owner decision, prepared output and responsibilities. Prepared research is development-assistant authored. There are zero actual buyer observations and zero runtime-generated research results in the initial owner workspace. Browser tests wrote only to an isolated test root.

## Recommendation and why it might fail

Provisionally test an estimate-path issue report for an owner-operated residential cleaning firm: one existing website, a bounded public contact/estimate route, reproducible screenshots/steps and prioritized webmaster tickets. No website rebuild, form submission, account access or result guarantee. The initial $149 price is an assumption.

White-label contact-path QA for small web agencies ranks second; it should move first if actual agency access or stronger willingness to use the report emerges. Review-theme digests rank third because substitutes and ongoing attention make the initial job less focused. Public evidence does not prove our offer's demand.

Three official Raleigh-area business sites already have quote/contact paths. No defect was alleged or contact made. Their functionality is counterevidence to a generic audit pitch. The first conversations must test whether a costly problem remains beyond free tools or the owner's webmaster, and whether anyone can act on our report. Sources, limitations and a ten-site/two-hour discovery procedure are in BUYER-EVIDENCE.md. Raleigh is a provisional research geography, not Mason's inferred location or network.

## Workload and the riskiest test

Recommend a maximum 20 hours/week for two weeks, contingent on school, sleep and other commitments. Weekday two-hour blocks and weekend five-hour totals are a proposal, not availability. Forty-hour allocation: 12 buyer evidence/access, 8 learning/report quality, 10 delivery/review, 6 MIDAS evidence records, 4 owner decisions/contingency. School pressure or missing prerequisites shorten the plan; do not fill unused time with speculative software.

First validation release, if later authorized: manually inspect at most ten official sites/two hours to choose at most five appropriate business-channel invitations. No customer quote-form disguise, personal-contact inference or bulk outreach. At most five 15-minute interviews. Progress to a separately approved pilot only if at least two report a concrete recent problem/costly check, a user/fixer is identified and someone explicitly requests written $149 pilot scope. That is interest, not payment or validation. No need/access within bounds is a stop; wrong deliverable/channel is a documented revision. Negative evidence and nonresponses remain visible.

## Connected implementation

`venture/workspace.ts` reuses StateStore and BusinessLoop for versioned founder understanding, source records, rule-based competency assignment, owner revisions and reported evidence. Unknown economic value stays unknown: the monetary priority ranking is blocked rather than filled with invented ROI. Founder-directed preparation is explicitly distinguished from an economically optimal plan.

One proposed baseline worker covers evidence synthesis, offer design and review with excellent instructions. No specialist competence is inferred from its title or declared competencies. Mason supplies access, commercial judgment and future external action. No second role is justified yet. Current preparation was performed by this development assistant, not that runtime worker.

Review edits the authoritative offer, regenerates the outreach framing and invalidates prior acceptance and economic assumptions. A future full-replacement runtime review can replace offer, validation criteria and task requirements; incompatible sample/outreach/economics become unavailable rather than silently reused. Human-required competencies block unsupported automatic assignment. Package acceptance binds the exact current package, including founder context, sources, sample, validation plan and schedule. It grants no external effect.

Owner-reported observations retain source, date, assistance/provenance limitations and nullable timing. They create new business-understanding revisions; they do not automatically count as independently verified demand/revenue. Owner continue/revise/stop decisions reference the observations and authorize no side effects. Original prepared source package is pinned immutably across restarts.

## Trusted runtime binding now implemented

`venture/runner.ts` connects the existing Responses bridge, exact complete-payload token admission, ModelLedger and Ed25519 grant verification to two contracts: sourced understanding and full-replacement validation-package review. Mock mode requires an injected transport and cannot read a credential or fall through to real fetch. Live mode requires a new signed grant, independent owner trust anchor, frozen bytes, scope/data/root/host/code identity, project and credential location, expiry and finite caps. Failed/uncertain admissions retain exposure; persisted successful response recovery makes no new request. No real provider compatibility claim is made for these new contracts.

```powershell
# Already prepared; use a fresh output directory for a different version.
node packages/foundry/src/venture/cli.ts prepare --root var/founder-venture-runtime-v2 --workspace var/founder-owned-venture-v1
```

The preserved manifest has two fixed request bodies, two inference admissions, two supporting counts, concurrency one, 8,192 input/output ceilings, 10-second count and 180-second inference deadlines, no retries, $1.04 inference reservations and a $0.21 explicitly unpriced count buffer ($1.25 prospective total). It contains no grant or API credential. Astra High/default is a carried-forward strong baseline route specification, not a newly verified project-access assertion; pricing and access must be reverified before any future authorization.

After separate authorization, trusted signing using the existing `signed`/`verified` mechanism and independent public-key configuration, the actual implemented command is:

```powershell
node packages/foundry/src/venture/cli.ts run --root var/founder-venture-runtime-v2 --grant <new-signed-grant.json> --trust-anchor <trusted-owner-public-key.pem> --attempt package-review-1 --workspace var/founder-owned-venture-v1
```

`understanding-1` is the other frozen admission. The review inspects the actual prepared package; these are two frozen reviews, not a claim that the second consumes the first result. Live review can enter the workbench as an unaccepted proposal only if its original package still matches. Owner adoption remains explicit. No automatic signing or execution exists in the browser.

**Recommendation now: authorize $0 of runtime spending.** Public research and the current prepared package suffice to decide founder fit and the first commercial test. A model review cannot establish willingness to buy. The optional manifest is readiness, not a request to consume the allowance.

## Verification and limits

Combined relevant suite: 99 passing tests, including 12 trusted-runner and three owner-workspace tests. Scoped strict semantic typecheck: zero errors, 45 source files. Real browser checks covered founder editing, exact local acceptance, recommendation revision/approval invalidation, observation entry, owner decision and reload persistence in an isolated test workspace; zero page errors. Sample/report/economics/package/buyer-evidence routes returned usable content. Real owner workspace remains unapproved with zero customer observations.

No provider/count calls, API credential access, outreach, purchases, registration, deployment or transactions. Historical laboratory/foundation evidence and grants remain preserved. No measured specialist advantage, independent semantic validation, commercial result or billionaire outcome is claimed. Local app security is single-owner loopback session/origin/CSRF protection, not a multi-user confidential boundary.

## Consolidated next owner decision

Confirm or amend the provisional cleaning-firm estimate-path offer and the proposed 20-hour weekly ceiling. Recommend Raleigh as an initial remote research sample unless a real access advantage exists elsewhere. The next reversible work is a bounded official-site discovery set and five exact invitation drafts for review. Actual recipients/messages/channel and any sending remain separate explicit approvals; this package acceptance alone does not authorize contact. Paid runtime recommendation is zero. Do not restart the invoice diagnostic or spend previous allowances.
