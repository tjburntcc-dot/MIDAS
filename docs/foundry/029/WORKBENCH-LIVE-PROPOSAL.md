# One consolidated prospective live diagnostic

**Not authorized or executed.** This replaces no old grant and transfers no historical allowance. The smallest useful release is **one evidence-to-understanding admission plus one four-stage supplier-invoice workflow**, AP-001, single strong worker, no seeded draft corruption, five counts and five inference admissions total. Do not repeat the closed support laboratory or buy a team arm without a substantive unresolved mechanism.

## Decision and fair scope

Does the connected path preserve source-supported business proposals and execute a useful multi-record recommendation with correct review, owner authority and independently verified delivery? A sufficient single worker is success. The baseline receives the complete authorized corpus, current policy, permitted lookup, persistent task state, excellent source-first instructions and a full replacement review opportunity. The understanding proposal is owner-adopted before planning; an unsupported proposed plan blocks, rather than being silently converted to the expected task. No learned-specialist, team-selection, reliability or general business inference follows from one case.

## Proposed envelope

- Direct OpenAI Responses `gpt-6-astra`, High reasoning, default tier. Retains the historically exercised strong baseline; this is not an assertion that a cheaper route cannot suffice.
- Project `proj_H01ORqdOPQM6vdGwQYsqFL5r`; reuse the approved protected Mission 028 `auth/provider/openai.key` through the existing trusted credential boundary, without copying it. No credential was accessed for preparation.
- Five inference admissions, five counts, 8,192 complete admitted input tokens and 8,192 output tokens including reasoning per admission. Concurrency one; count timeout 10 seconds; inference timeout 180 seconds. No fallback, automatic retries, replacement calls or recovery allowance.
- At most 52 cents per inference: 260 cents aggregate inference reservations plus **53 cents explicitly unpriced supporting-count uncertainty allowance**, **USD 3.13 new aggregate cap**. This is admission control, not an external billing guarantee.
- Proposed expiry September 25, 2026, 22:00 UTC. One exact owner-approved internal fixture publication capped at 25 simulated cents; no financial action. No protected evaluation, customer communication, certification or production.
- Stage limits: understanding one/52 cents; invoice workflow four/208 cents. Failed/uncertain attempts consume allowances and retain exposure until authoritative reconciliation.

Official [Astra model documentation](https://developers.openai.com/api/docs/models/gpt-6-astra), read September 12 UTC during this preparation, lists Responses/High support and prices of $10/M input, $1/M cached input, $12.50/M cache writes and $50/M output. Conservative input reservation uses $12.50/M, not a cached-input discount: ceil((8192 × 12.50 + 8192 × 50)/1,000,000 × 100) = 52 cents. Reverify current terms before signing/dispatch. Documentation does not prove this project's present access. No immutable dated Astra revision was verified; exploratory identity/timestamps must be recorded, and protected final freeze remains blocked on its separate revision requirement.

Historical Mission 029 remains separately held at $9.72 ($4.68 inference reservations + one $5.04 old count buffer), containing $1.28 provisional token estimate. No authoritative charges. The new 53-cent allowance is for a new proposed experiment, not another allocation inside the old grant. No Mission 028 transfer. The original $30 proposal is preserved; this $3.13 diagnostic deliberately purchases fewer consequential observations.

## Concrete preparation and execution gates

```powershell
node packages/foundry/src/workbench/prepare-live.ts var/workbench-live-proposal-v1
```

Creates `authorization.request.json` (unsigned), `understanding.request.json` (exact initial serialized request, source/contract hash) and `invoice.schemas.json`. Exclusive writes prevent replacing an existing proposal. Subsequent invoice request bytes depend on actual earlier results and must be recorded and counted at each admission. Never prefill expected answers.

**Remaining local live integration:** bind the new understanding and invoice contracts to the existing trusted Responses/admission runner, preserving these limits, and exercise that binding with a transport mock before signing. The current workbench deliberately refuses live ports. There is no honest live execution command for these new contracts yet; do not use the historical support CLI to simulate one. This proposal is concrete scope and cost preparation, not a claim that new live compatibility has been tested.

Owner work: adopt or reject the sourced interpretation; approve the exact final fixture artifact; answer only a material semantic question if mechanics cannot resolve it. No promised review duration or independent expertise. Count review/correction time only if actually measured reliably; AI assistance remains disclosed. Obtain provider billing evidence separately, not by buying another model call.

Stop after missing access, contract/authority/accounting failure, unsupported plan, uncertain inference, insufficient evidence, limit/expiry, or no remaining decision value. Preserve observations and version any repair. If the complete single workflow is adequate, retain it. A reusable reasoning failure can justify a future procedure-only intervention; a shared evidence/specification defect must be repaired equally first. A team has not been tested. Inconclusive means name the missing fact/review/measurement and obtain that smallest evidence, not more agents.
