# Integrated workbench: reconciliation at the accepted foundation

This is the **pre-extension audit**, against accepted foundation commit `9de39a57807929f49d768f011e8853493065d073`. It preserves the difference between completed components and the still-open owner journey. New workbench implementation and verification should be assessed against this table; historical completion receipts are not rewritten.

The governing mission remains open at this checkpoint. The closed W-001/W-006 laboratory and accepted business foundation are completed subsets. Neither a static HTML report nor mock outcomes close the interactive workbench requirement. This assessment is development-assistant authored from source and local evidence, not a founder interview, independent audit or measured business diagnosis.

## What existed before this continuation

| Deliverable | Status at foundation | Implementation and verification | Outstanding integration |
| --- | --- | --- | --- |
| Durable brief, evidence rights, authority and partial business understanding | Complete for bounded local input | `packages/foundry/src/business/core.ts`: `validateBrief`, `BusinessLoop.revise`, immutable revisions and stale-write checks. `test/business-loop.test.ts`; foundation receipt records 63 passing relevant tests and strict semantic check of 33 source files. | Owner-facing editing, proposal acceptance and evidence provenance inspection. No fabricated complete understanding. |
| Evidence freshness, supplied hypothesis ranking and bounded worker assignment | Complete as rules, partial as adaptive product capability | `core.ts`: `assess` excludes stale/superseded facts, distinguishes missing facts and ranks supplied comparable value estimates; `assign` searches supplied workers with competency/evidence/authority constraints. | ModelPort evidence extraction, contradictions, requests, competing hypotheses and plans are absent. Existing diagnoses are locally authored inputs, not autonomous discoveries. |
| Model-controlled support workflow | Complete in the bounded environment | `workflow/task.ts`, `runner.ts`, `runtime.ts`, `model-port.ts`, `experiment/ledger.ts`; `DESIGN.md`, `COMPLETION.md`, workflow tests and archived demonstration. Four stages receive evidence, actual draft, or actual receipt/readback. | General business proposal ingestion and an additional real adapter; preserve original fixture CLI live refusal. |
| Exact approval, fixture action, independent readback and safe resumption | Complete for existing workflow | `authority.ts`, `lab/fixture-service.ts`, workflow runner; `workflow-process.test.ts`, budget/recovery/W006 tests. Actual W-006 response-loss checkpoint precedes deterministic reconciliation of exactly one effect. | Interactive owner presentation must invoke these state changes rather than imitate them in a report. Approval remains separate from semantic acceptance and spending authority. |
| Six episodes, both fixed configurations, offline comparison | Complete offline | `docs/foundry/029/evidence/demo-summary.json`: 12 terminal mock runs, 42 mock calls, six fixture effects. Original test log records 148 pass/0 fail; not evidence of live team competence. | Do not reopen the completed laboratory or purchase redundant support variations. |
| Actual live workflow observations | Complete and narrowly scoped | `LAB-CLOSURE.md`, W001/W006 completion observations and SQLite snapshots. Two completed related single-agent synthetic workflows; original failed admission preserved. | No paired live team result, reliability estimate, measured economic benefit, independent human semantics/timing or authoritative billing. |
| Persistent verified feedback and controlled reassessment | Complete for support bridge | `business/support-adapter.ts` authenticates readback before `BusinessLoop.observe`; core feedback deduplicates completed opportunity hashes. Foundation demo completes first plan and blocks follow-up on unknown outcome/missing competency. | Expose feedback and revision decisions in the owner journey; next assignment must reuse this seam. |
| Interactive integrated workbench | Absent | `business/cli.ts` supplies commands and writes static `business-report.html`; there was no interactive server/client at this checkpoint. CLI approval is real, but HTML is read-only. | Load/edit permitted brief and evidence; request/inspect/accept proposals; plan/assign/run; exact approve/resume; inspect artifacts and updated state without editing raw JSON. |
| Evidence-to-understanding ModelPort path | Absent | No business analysis ModelPort task in foundation source; core accepts `Brief.opportunities` and locally authored claims. | Strict sourced proposals, bounded calls, persisted attempts, reference/rights/freshness/arithmetic checks, no hidden diagnostic lookup or answer leakage. Offline only in this continuation. |
| Supplier-invoice assignment | Absent implementation; proposal complete | `NEXT-PHASE-BUSINESS-ASSIGNMENT.md` describes eight-invoice source reconciliation, cash constraint, register and memo. Existing support adapter explicitly refuses other jobs. | Implement bounded adapter, permitted retrieval, strict packet, independent arithmetic, review correction of authoritative decision and all derived views, exact-payload approval invalidation. |
| Failure-to-procedure comparison | Partial | `core.ts` has `proposeProcedure` requiring assigned worker/observed failure and `freezeComparison` preserving baseline/shared settings; existing `experiment/` harness separately executes comparisons. | Connect this preparation to observable offline comparison mechanics; disclose mock provenance. No automatic adoption or qualification. |
| Evidence-backed MIDAS operating packet | Absent as connected owner artifact | Capability reports cover laboratory worker scope, not the ranked MIDAS product operating decision. | Provide sourced capabilities, limitations, three explicit bottleneck hypotheses and ranked work with next owner decision in the workbench. |
| Current finished-product live proposal | Partial / superseded | Original `LIVE-EXPERIMENT.md` and later grants remain historical artifacts. They do not authorize new analysis/invoice calls. | Quote the actual finished call path, count all analysis/review calls and preserve historical exposure separately; new unsigned grant required before any dispatch. |

## Historical evidence that must not be overstated

`var/business-foundation-029-v1/completion-receipt.json` records accepted foundation HEAD, 63 tests, zero type errors, zero provider requests and hashes of its demo. `var/workflow-029-w006-v1/completion-receipt.json` records laboratory closure HEAD `f15951656c6fb8a5122c25e50bd1d63a77effc72`. The sources and tests above were inspected; historical counts are archival verification, not claims that this continuation reran those exact suites.

W-001-single-r1 and W-006-single-v1 each completed four live admissions. W-006 corrected a disclosed inherited draft defect during drafting; review tightened authority wording. The runtime, rather than the model, reconciled the deliberately lost fixture response. Both publications had authenticated readback and one effect. Human approval and AI-assisted artifact assessment do not establish independent semantic validation.

Mission 029 historical totals remain nine inference admissions and nine counts, $1.28 provisional inference estimate, $4.68 retained inference reservations plus one $5.04 unpriced count buffer: $9.72 held and $20.28 uncommitted. No authoritative charges are recorded; actual billing is unknown. These values describe historical evidence, not a refreshed provider balance or new spending authority. Mission 028 is separate. New offline telemetry must never be added as real spending or measured competence.

## Acceptance for this continuation

The finished interface must operate on persisted records, not a parallel UI simulation. A browser journey must demonstrate evidence → proposed understanding → explicit adoption/revision → bottleneck/plan/worker rationale → execution → exact approval → authenticated outcome → business feedback for both supported assignments. Unsupported teams/jobs, missing evidence, stale records, budget limits, waiting approval and failed/uncertain calls must remain visible. A refreshed page and restart must recover the same checkpoint.

For evidence analysis, test schema-compliant wrong references, denied source use, contradiction/freshness handling, unsupported numeric estimates, and varied supplied source facts. For invoice review, demonstrate a changed recommendation propagating through register, totals, memo and exact approval hash; reject a prior approval. For improvement mechanics, retain an excellent baseline and tie the candidate to a persisted observed failure; mock wins remain mechanics only.

## Consolidated live-quote arithmetic to finalize against implemented stages

Preparation only: a value-gated first release could include **up to two business-analysis admissions plus four admissions for one invoice workflow**, with six supporting counts. At the historically documented 52-cent conservative inference reservation this is $3.12, before a separately declared unpriced count-exposure allowance. A second independently justified workflow of the other supported assignment would bring the ceiling to ten admissions/counts and $5.20 inference exposure. Do not buy a repeat of the closed support laboratory merely to fill this allocation; it must test the new business-context integration and a specific unresolved decision.

The final quote must use the number of calls actually implemented, current official route/pricing verification, full serialized input and output ceilings, finite deadlines, concurrency one, no hidden helper/optimizer calls, zero automatic retries and a separate unsigned grant. Old $30/$15 proposals are not inherited authority. Historical $9.72 remains displayed separately; unused allowance does not transfer. Human fixture approval is required for each effect. Billing reconciliation and reliable independent semantic/correction review remain external evidence dependencies, not reasons to purchase more model calls. Protected evaluation still needs real custody and immutable-revision evidence.
