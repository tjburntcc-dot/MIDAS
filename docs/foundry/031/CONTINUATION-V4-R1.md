# Mission 031: exact executable continuation

**Ready for exact owner approval; unsigned and unexecuted.** This is the continuation of the existing stopped V4 experiment, not a new mission or a new budget. Preparation made zero provider/count requests and read neither provider credentials nor the private signing key.

Proposal SHA-256: **`872dc6c35184a4366ff4ea70f6e7493d4e3c5c78bb38db3f8d526328959ff682`**.

Maximum proposed incremental exposure is **USD 46.59**. Historical retained exposure is **USD 5.23**, counted once: the original inference reservation **USD 1.23** plus the same **USD 4.00 unpriced count uncertainty buffer**. Combined maximum is **USD 51.82**. The original provisional token estimate is **USD 0.46**, not an additional reservation or an authoritative charge. No charges are settled; billing and labor costs remain unknown. Unused headroom is not execution authority.

## What the continuation does

The first request gives the model its **unchanged 20,542-byte draft**, its current source hash, explicit feedback to meet the existing **18,000-byte** report limit, and the existing **quote-to-job-v2** product contract. The model must shorten and correct its own work. The development assistant has not rewritten, minified, endorsed or replaced its draft.

The remaining path is correction/investigation → consequential build-or-stop decision → blank-source implementation → actual browser checks and repair → authoritative product review → operating deliverable → reassessment. A supported rejection cancels dependent execution. An operator interview is not a prerequisite for a justified bounded engineering experiment. Commercial demand, independent human acceptance and labor savings remain unproven.

The application still begins with the controller's documented blank `app.html`. No development-assistant application is supplied to the worker. The worker can retain adequate work unchanged; no cosmetic revision, mandatory search, artificial defect or specialist comparison has been added.

| Remaining allocation | Ordinary inference admissions |
| --- | ---: |
| Correct and finish investigation | 7 |
| Build-or-stop decision | 1 |
| Implement, check and repair | 10 |
| Review the actual product | 6 |
| Deliver operating assets | 6 |
| Reassess | 1 |
| **Ordinary total** | **31** |

Separately: **two held known-incomplete recovery admissions** and **two optional hosted-search admissions**, with at most one native search per hosted call. Maximum **35 new inference admissions and 35 supporting counts**; including history, **36 inference admissions and 36 counts**. No transfers between task caps. Ordinary/recovery reservations are USD 1.23 each; search reservations are USD 3.00 each: `(31 + 2) × 1.23 + 2 × 3.00 = 46.59`.

The completed historical response is **not** a known-incomplete recovery. New task IDs end in `-v4-r1`; the fresh first attempt is `p031-8c77e4ad0e739a12-0`. All six original tasks and their observations remain unchanged. The explicit continuation uses the seven ordinary investigation decisions remaining after the original one, rather than resetting eight or consuming a held recovery.

## Scope and focused repairs

Unchanged route: direct OpenAI Responses, **`gpt-6-astra`, Max reasoning, default service tier**, project **`proj_H01ORqdOPQM6vdGwQYsqFL5r`**. Maximum complete admitted input **32,768 tokens**; output **16,384 including reasoning**; inference timeout **180 seconds**; count timeout **10 seconds**; concurrency **one**. Expiry **September 25, 2026, 22:00 UTC**. Reuse the previously approved protected credential file without copying it. No access probe, fallback, automatic retry, hidden helper, sending, deployment, purchase, customer action, protected evaluation or procedure comparison.

The existing isolated repair rejects oversized new source before committing it and supplies the declared downstream product contract. This continuation adds an exact inherited-draft read exception: only the preserved failed draft can exceed the normal context source limit. New writes, independent checks, publication and downstream delivery still enforce the original source limits. The original draft cannot pass publication unchanged.

The budget uses the existing ledger's scoped carry-in mechanism. Signing appends explicit retirement markers to the parent's execution authority, without modifying its signed grant, attempts, response, source, task states or immutable records. Parent retirement is mandatory before any new dispatch. A different continuation, changed parent exposure, pending parent work or missing parent evidence blocks admission. Original and new grant scopes cannot be stacked through this continuation mechanism.

The full mock-transport run exposed one additional shared contract mismatch: the count adapter's legacy 65,536-byte body guard was smaller than the workflow's permitted 72,000-byte context plus JSON serialization overhead. The new inner grant explicitly binds **196,608 bytes of local serialized-request containment**, enough for that existing context and schema overhead. The default remains 65,536 for other grants. This is not an increase in admitted tokens, calls, output, deadlines or money. Full-payload provider counting, its existing safety margin and the 32,768-token admission ceiling remain mandatory. Oversized local requests fail before ledger/count/credential access and are identified as known local failures, rather than an uncertain provider effect.

Public compatibility and prices were verified immediately before the original call at **September 13, 2026, 00:30 UTC**, and the original response confirms actual Astra Max transport. Retained official sources: [Astra model documentation](https://developers.openai.com/api/docs/models/gpt-6-astra), [pricing](https://developers.openai.com/api/docs/pricing), [token-count API](https://developers.openai.com/api/reference/typescript/resources/responses/subresources/input_tokens/methods/count). The unchanged conservative reservation uses USD 12.50/M input and USD 50/M output: `ceil((32768 × 1250 + 16384 × 5000) / 1000000) = 123` cents. This does not guarantee future access or external billing limits; no preliminary paid probe is included.

## Exact artifacts and verification

Use the existing repair worktree: `C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-031-handoff-r1`, branch `codex/portfolio-handoff-repair-v4-031`.

- Engineering commit: `771242d4e5b94f2809eb51a0e9381287d3e1dab1`.
- Frozen implementation hash: `ddac493ceb6e392ae2e839a03889a60048ca55ef66ed373633fb3c93afd823b2`.
- Runtime root: `var/portfolio-031-continuation-r1`.
- Packet: `var/portfolio-031-continuation-r1/proposal/portfolio-031-value-build-v4-r1/portfolio.authorization.request.json`.
- Same directory: `task-manifest.json`, `initial-responses-bytes.json`, `initial-count-bytes.json`, `original-runtime-draft.json`, `payload-audit.json`.
- Exact final preflight and original-table preservation: `var/portfolio-031-continuation-r1/reports/continuation-preflight.json`.
- Initial body hash: `3c21d4ec6f293cdb5519126e4c102318d6e980970c8ceecba8ba3d559594fcdd` (53,013 bytes; context 47,138 bytes). Actual input tokens remain unknown until the authorized count.
- Original draft hash: `6290519b4366f4f6f6d25cd221d59346e71c22f208b3b75a55e86cbb95f39847`.
- Parent approved packet remains `3dfaafa823588386aca3dc410c25c19d1ee915b908bac9f86152374011cfff30`; original checkout remains `21c876f58624d5ef8be5f4160e68f2e169500655`.

The exact prepared first request was rebuilt and compared byte-for-byte with its frozen file. The original database's entities, events, immutable records and artifacts still exactly match its preserved post-stop backup. No parent retirement has occurred during preparation. There are zero new model attempts or signatures in the continuation root.

Verification covers 65 relevant cases across the completed runs: explicit parent exclusivity, carried exposure, initial-byte binding, local count-byte boundaries, provider failures, recovery restrictions, durable resume, accounting and operating boundaries. One regression required preserving the existing billing-only error identifier; that targeted provider suite then passed all 14 cases. The final continuation suite passed four cases, including both the rejection branch and the complete positive branch through a real local browser with **only in-memory Responses transports**. Mock output is test-authored and never real model competence. The positive fixture used 17 ordinary decisions and retained the original draft unchanged. Strict TypeScript 6.0.3 semantic checking passed across 38 source files, with dependency declaration checking skipped. No dependencies installed.

Logs: `var/continuation-verification.txt` preserves the initial run, including the discovered regression; `var/continuation-provider-final.txt` and `var/continuation-final.txt` record its corrected relevant runs. The initial full-path test exposed and preserved the request-byte mismatch before any paid continuation.

## Exact post-approval commands

Run from the repair worktree above. These commands are prepared, not executed. The established signing command authenticates the newly approved exact packet, retires parent execution authority, and installs only the fresh grant. It does not mutate the historical grant or release money.

```powershell
node packages/foundry/src/portfolio/cli.ts sign-proposal --root var/portfolio-031-continuation-r1 --proposal var/portfolio-031-continuation-r1/proposal/portfolio-031-value-build-v4-r1/portfolio.authorization.request.json --approve-proposal-hash 872dc6c35184a4366ff4ea70f6e7493d4e3c5c78bb38db3f8d526328959ff682 --owner-public-key C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/owner.pub --owner-private-key C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/owner.key --principal Mason --approval-reference "Owner approval in this Mission 031 conversation of exact continuation 872dc6c35184a4366ff4ea70f6e7493d4e3c5c78bb38db3f8d526328959ff682; incremental USD 46.59; historical USD 5.23 retained once; combined maximum USD 51.82; unchanged limits and exclusions."
node packages/foundry/src/portfolio/cli.ts preflight --root var/portfolio-031-continuation-r1 --live
node packages/foundry/src/portfolio/cli.ts run --root var/portfolio-031-continuation-r1 --live
node packages/foundry/src/portfolio/cli.ts report --root var/portfolio-031-continuation-r1 --live
```

After approval, the assistant carries this sequence through completion, supported rejection or a genuine governed stop without routine stage confirmations. Only a recorded eligible known-incomplete failure can use the existing `recover-incomplete --live --task <exact task> --parent <exact attempt>` path within the two held admissions. Unknown completion retains exposure; no blind resend occurs. The current historical size failure is not that recovery condition.

The resulting operating package will identify the actual reviewed product or supported rejection, observed defects and interventions, accounting uncertainty, and one prepared next customer-learning action. Customer contact, deployment, commercial transactions and any additional execution still require separate authority.
