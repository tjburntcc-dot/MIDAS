# Mission 031 V4 R3 — durable execution repair

Prepared 2026-09-13. Local engineering and explicit mock verification only. No provider request, token count, protected credential/private-key access, signing, or remote mutation occurred during this repair. The actual engineering experiment remains incomplete.

## Preserved observation

The inspected R2 worktree is `var/foundry-worktree-031-finalization-r2`, branch `codex/portfolio-finalization-repair-v4-031`, HEAD `94434228144f73790ec4ec125337516af81e8247`. Its stopped attempt `p031-e2fd5c331b2304ca-0` counted 9,506 input tokens successfully, dispatched inference at 2026-09-13T15:30:50.716Z, then failed locally at 15:33:50.765Z with numeric code 23. That timing/code is consistent with the configured 180-second Node timeout. It does not prove the provider stopped or failed.

The exact request used `store:false`; there is no retained response ID, inference response, usage, or decision. The count request ID identifies the count, not the missing inference. Supported response retrieval requires the missing response ID. There is no supported local recovery of this decision from the available records. We have not resent it, searched private provider metadata, or released its 123-cent reservation. Billing evidence could reconcile cost but cannot supply the missing decision.

The R2 stop backup manifest remains `8710f65f0e3a3ae37b073254b9e9d8154313d8700576677318e8bdb0e5ccc742`; database SHA-256 `c596db5974009bae617e5bcd9f05c0db135538ee14718ecc27c0118cf6182e45`. R3 copies this authenticated snapshot into a fresh root. Original grants, attempts, source, stop classification and accounting remain intact. Retirement of R2 authority is deferred until exact R3 approval and signing.

## Official options and selected implementation

OpenAI documents asynchronous `background:true` creation, response-ID status retrieval, and resumable background streaming with sequence cursors. The background guide specifically demonstrates `gpt-6-astra`. Selected: a short non-streaming background acknowledgement, durable ID capture, then bounded same-ID GETs. Streaming would add an event parser/cursor protocol without eliminating the initial identity-loss window. Merely extending a synchronous deadline would preserve the original failure mechanism. [Background mode](https://developers.openai.com/api/docs/guides/background), accessed 2026-09-13.

The new body explicitly uses `store:true`. Current documentation describes stored Responses application state as retained for at least 30 days. ZDR treats this setting as false; temporary background storage is roughly ten minutes. Therefore this packet requires project storage behavior compatible with `store:true`, checks the returned storage field, and makes no ZDR compatibility claim. The same authorized public/synthetic task material and generated outputs can now remain with the provider beyond execution. No credential is included in that material. Exact provider deletion timing is not guaranteed; this grant does not authorize remote deletion or cancellation. [Data controls](https://developers.openai.com/api/docs/guides/your-data), accessed 2026-09-13.

The model/settings remain direct Responses `gpt-6-astra`, `reasoning.effort:max`, default service tier. Official pricing lists $10 input, $1 cached input, $12.50 cache writes and $50 output per million tokens. The unchanged conservative reservation is `ceil((32768*12.50 + 16384*50)/1000000 * 100) = 123` USD cents. Output includes reasoning. The model route is not claimed to be an immutable provider revision. [Astra model and pricing](https://developers.openai.com/api/docs/models/gpt-6-astra), accessed 2026-09-13.

## Durable behavior across all remaining stages

- One count and one create admission per new request, bound to exact serialized bytes. Generation-only `background`/`store` do not enter the supported count projection; all worker input/instructions/schema remain covered by admission.
- Persist create intent before POST, sanitized HTTP identity before body processing, response ID before polling, and allowlisted terminal output/usage before local contract validation. No authorization header, credential, uncontrolled provider body or reasoning trace is persisted.
- Count deadline 10 seconds; background-create acknowledgement deadline 60 seconds; each GET 15 seconds; polling interval 5 seconds. Automatic completion window is 15 minutes from initial dispatch. It never resets on restart.
- At most 240 retrievals per admitted response, including failed reads. Three consecutive transient read failures stop the loop; permission/not-found failures stop immediately. A later explicit resume can inspect the same ID once after the completion window, within 24 hours of dispatch and grant expiry, with the same lifetime cap. Up to 6,000 GETs across 25 new admissions; no hosted search.
- A live PID owns retrieval; a dead owner can be reclaimed. A local failure does not free global provider concurrency while a background outcome is unresolved. Saved terminal output is replayed without another count, create or GET. Failed/incomplete output remains a failure, with usage and exposure preserved.
- An absent initial response ID remains unknown and cannot be resubmitted. A local deadline does not cancel remote work. A saved ID is not a guarantee of provider availability, completion, useful output or exact charges.
- The finite controller resumes the original request and saved step, not a newly assembled prompt. Decision, implementation, review, operating delivery and reassessment use the same port. Existing bounded finalization still performs current checks, authorized local publication/readback and closure; it does not replace the model's build-or-stop judgment.
- The old five-minute stall detector now recognizes recent bounded retrieval progress, but still recovers dead workers, expired leases and elapsed completion windows. The workspace shows response identity, retrieval use and persisted-provider-result status separately from task acceptance.

## Verification and contribution boundaries

The focused regression set passed 106/106 tests: real child-process exits after identity and terminal persistence; delayed completion beyond the former 180 seconds; lost POST acknowledgement; lost/truncated GET; transient HTTP failures; expiry; read caps; wrong response/model/storage; duplicate inference prevention; scoped concurrency; provisional usage; failed output; source/authority changes; finalization and prior continuation boundaries. Strict installed TypeScript 6.0.3 semantic checking passed with zero errors across 42 source files, excluding dependency declaration checking.

The explicit offline end-to-end demo completed decision → build/browser checks → review → operating deliverable → reassessment in nine mocked admissions, nine mocked counts and ten mocked retrievals, including one lost GET and same-ID recovery. The rejection variant used one mocked admission and cancelled all downstream work. The demo reuses the prior stage profiles with R2 task IDs in an isolated fixture clone; it does not impersonate the exact signed R3 grant. R3 signature/retirement/task lifecycle validation is independently tested against the preserved parent and disposable copies.

Real browser inspection verified persisted status after refresh, recovery details, product visibility and 390px layout with no horizontal overflow or browser errors. These checks exercise actual local tools with development-authored fixture source. They are not live background compatibility, runtime-authored product, customer-value, or specialist-competence evidence. Initial demo runs exposed only an undersized mock-grant request ceiling; the mock was corrected to the existing production ceiling, without increasing that ceiling. Failed offline runs remain in `var`.

Evidence: `var/durable-r3-verification/tests.txt`, `owner-browser.json`, screenshots; `var/durable-r3-demo-positive-v3/offline-result.json` and `var/durable-r3-demo-rejection/offline-result.json`. New production code is isolated in branch `codex/portfolio-durable-responses-v4-031`. No R2 code or record was edited. Human correction time and development labor cost remain unmeasured.

## Exact prospective scope

Reuse the accepted 17,908-byte research unchanged, SHA-256 `fae4225c8d37b82b2ad8fc10a622988081538da7a794338e6bfac09bd583d34e`, its source records and authenticated publication/readback lineage. A fresh `quote-desk/decide-v4-r3` is explicitly linked to the unknown R2 attempt; it is not an alleged recovery of its answer. No application workspace is seeded.

Ordinary allocations remain decision 1, build/test/repair 10, review/correction 6, operating deliverable 6, reassessment 1. One separately held known-incomplete recovery admission remains; the second former held slot is removed to stay inside the approved combined ceiling. Recovery cannot replace an unknown completion, permission failure or exhausted ordinary allowance. No borrowing, fallback, extra helpers, comparison calls or automatic replacement. A rejection cancels downstream work. A genuine unsupported boundary preserves outputs and stops the affected workflow.

Historical nine inference/count admissions retain $11.07 inference reservations plus the single $4.00 uncertainty buffer, totaling $15.07. Historical token-based provisional cost is $3.84; no authoritative settlements are recorded. New maximum is 25 inference admissions/counts × $1.23 = $30.75. Combined maximum is **$45.82**, counting history once. The shared $4.00 buffer now explicitly includes unpriced count/retrieval uncertainty; no second buffer and no claim that GETs or counts are free. These local admission controls do not guarantee external billing limits.

Same project and protected credential path, global concurrency one, expiry 2026-09-25 22:00 UTC, synthetic/public data only, no external communications, deployment, purchases, customer actions or production promotion. Exact new proposal approval must include durable storage, same-ID retrieval and retirement of the prior authority. Until then there is no active R3 grant and no provider execution.
