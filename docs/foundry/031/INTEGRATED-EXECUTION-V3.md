# Mission 031 — executable business-building reconciliation

Prepared September 12, 2026. This is a focused continuation of the completed local release, not another infrastructure mission. No provider/count requests, account access, signing, sending or deployment occurred. The original $59.05 packet and earlier completion evidence remain preserved.

## What the old packet actually bought

`release.ts:prepareOperatingRelease` assigns each of two ventures an eight-call investigation, six-call report review and one-call planning decision. An internal MIDAS plan adds one call: 31 primary work calls, two ordinary follow-up slots, two held recovery slots, four separately metered hosted searches and a $4 unpriced count allowance. Its maximum was $59.05.

Those are predominantly research, reports and plans. Although the signed capability vocabulary includes `software.build`, the release assigns no explicit software build, and only two ordinary follow-up slots remain after the 31 primary calls. Advertising executable software tools did not make that allocation an end-to-end product-building experiment.

The preserved initial body is `var/portfolio-031/proposal/portfolio-031-operating-v2/initial-responses-body.json`. It requests `release-readiness/investigate-v2`, strict one-action JSON and workspace/research tools. It does not request a product implementation. Its proposal hash remains `b367e630e211b184ab078d6d3a0c9a6283d21ada1f8bd4b070128bfcf2dfa5d6`.

## Capability and executable connection

| Action | Actual connection | Practical boundary |
| --- | --- | --- |
| Research | `PortfolioEngine.tool` dispatches the chosen `research.search`, `research.fetch` or `research.read`; `HostedSearch` and `EvidenceLibrary` retain results | Search is a separately admitted Astra inference; fetch is restricted public read. No account browsing, customer interviews or demand proof |
| Create tasks | `PortfolioEngine.plan` validates `planningSchema`, then `Portfolio.applyPlanning` atomically persists tasks, dependencies, priorities and cancellation | Planning stages create tasks; the general worker action schema has no task-creation tool. The revised grant cannot execute newly generated tasks |
| Implement/modify software | `createTaskPreparer` seeds blank `app.html`; model supplies full file bytes via `workspace.replace`; `LocalWorkTools.execute` applies exact-hash edits and invalidates checks/publication | Substantive source changes are possible. Only the trusted quote-to-job browser execution/check profile is implemented; arbitrary products/backends are not |
| Run/test | `check.run` invokes `checkQuoteApplication` in real isolated Chromium; preview state uses `LocalWorkTools.previewState` | Quote arithmetic, job transitions, persistence/export and containment checks; no arbitrary shell, package installs, test runner or deployment |
| Repair | Real tool observations enter the next `PortfolioEngine.context`; the model can choose another `workspace.replace`, then `check.run` | The runtime does not supply the repair. A model can fail or exhaust the finite allowance; checks cannot be weakened through worker tools |
| Produce service deliverables | Blank `brief.json` → model-written sourced recommendation/steps/obligations → `checkServiceBrief` → rendered HTML and authenticated local download | Actual document creation is supported. The format is a bounded sourced brief; it is not unrestricted professional-service delivery or verified customer acceptance |
| Respond to outcomes | Actual dependency artifacts, local observations and owner-recorded observations enter planning/reassessment; decisions update priority and tasks | Interpretation is a model proposal. No causal, revenue, labor-saving or customer-success inference is established automatically |

The current quote-to-job product's original HTML/JavaScript comes from development-assistant-authored `products.ts:quoteTrackerFiles`. `preparePortfolio` seeded it; `offlinePortfolioModel` selected predefined replacement source after a real check failed. This is a working developer-built product with an exercised mock repair loop, not an actual-model build. Conversely, the prospective live preparer supplies a blank file, not `quoteTrackerFiles`: live source authorship must appear in preserved Responses output and tool edits.

## Focused integration completed

`integrated-release.ts` declares six assignments for one strong single-worker baseline. The original v2 tasks are cancelled only where unstarted; original artifacts and results are unchanged.

1. **Investigate (8 work calls):** examine buyer need, reachability, substitutes and contrary evidence; produce a checked sourced decision brief. Up to two hosted searches are separately priced. Fetch/read/write/check/publish/complete decisions consume this same eight-call allocation.
2. **Decide (1):** read the full actual brief and choose whether the bounded prototype is worthwhile. The worker-visible contract requires the named `quote-to-job` alternative. Only `prototype` releases the four downstream jobs. Other decisions cause controller-recorded cancellation; the received model output is preserved unchanged. The sourced brief and decision are the useful terminal outputs of a rejection.
3. **Build/run/test/repair (10):** write a new local quote/job application from blank source; run real browser checks, diagnose actual feedback, repair where needed, and publish checked source locally. No inherited injected defect is supplied in this live assignment.
4. **Product review (6):** clone the actual built artifact, assess the source and verified execution evidence, make justified corrections, rerun checks, replace the authoritative artifact and preserve versions. This is an explicit strong-baseline review, not an independent-human or specialist comparison.
5. **Operating/validation deliverable (6):** read actual reviewed source, readback, checks and revision evidence. Deliver product-use/demo steps, the precise offer or rejection, founder interview questions, unsent outreach, economics assumptions with labor unknown, and success/revision/stop criteria in a sourced HTML/JSON packet.
6. **Adapt (1):** use that packet and persisted observations to revise priority and create the smallest next task proposals or stop. Those follow-up tasks remain outside this grant.

Previously, non-review downstream work could receive artifact metadata without full report content; software evidence was also missing from planning. Now the engine supplies the full checked report or actual software files, verified publication hash, checks, obligations and source-change evidence. Full planning proposals reach subsequent work as well. Handoffs fail explicitly above their disclosed byte bounds; no silent truncation claims completeness. Software source remains untrusted data, never new authority.

The outer grant now freezes each task's definition and ordinary admission allowance. It rejects changed definitions, unlisted generated tasks and borrowing another task's unused ordinary calls before provider transport. Two global recovery calls remain held separately, with local task ceilings leaving room for them; they cannot turn into extra exploration. The optional extension leaves historical grant bytes unchanged.

## Revised concrete request: maximum USD 51.82

| Allocation | Maximum | Reserved exposure |
| --- | ---: | ---: |
| Six primary work stages above | 32 inference admissions | $39.36 |
| Fresh linked known-incomplete recovery | 2 inference admissions | $2.46 |
| Hosted public search | 2 inference admissions, one web-search tool call each | $6.00 |
| Supporting token counts | At most 36, one per admitted inference | $4.00 unpriced uncertainty allowance |
| Total | 36 inferences + 36 counts | **$51.82** |

This replaces the unsigned $59.05 request; it is not additional to it. No historical budget transfers. New actual-model exposure is currently zero. Local compute and engineering labor are unmeasured.

Route: direct OpenAI Responses `gpt-6-astra`, `reasoning.effort=max`, `service_tier=default`. This preserves the governing highest-supported-effort preference for this difficult integrated job; it is not a claim that a fair cheaper-route comparison was performed. Complete admitted input ceiling 32,768 tokens, maximum output 16,384 including reasoning, 180-second inference deadline, 10-second count deadline, provider concurrency one. Expiry **September 25, 2026, 22:00 UTC**. No fallback or automatic retries.

Official documentation rechecked September 12: [Astra pricing/settings](https://developers.openai.com/api/docs/models/gpt-6-astra) list $10/M input, $12.50/M cache writes and $50/M output with max supported. We conservatively reserve ceil((32,768 × 1,250 + 16,384 × 5,000) / 1,000,000) = 123 USD cents per work call. [Tool pricing](https://developers.openai.com/api/docs/pricing) lists search at $10/1,000 tool calls plus token charges. Each search reserves $3 using the existing disclosed 131,072-token search-context uncertainty bound: conservative calculation $2.8772, rounded $2.88 below $3. This engineering reserve is not an external billing guarantee. Count pricing remains unresolved; $4 is an uncertainty allowance, not an asserted price. Source dates, actual returned model identity and usage are retained; immutable revision availability remains unverified.

Access prerequisites: the confirmed project is `proj_H01ORqdOPQM6vdGwQYsqFL5r`; the existing protected file must still contain an active project-permitted key with Responses, token-count and Astra/search access plus API billing. This was not probed and the credential was not read. Historical access does not prove current access. The first admitted count/response doubles as transport verification; there are no hidden preliminary calls. Access, schema/authority defects, uncertain completion, exhausted limits or expiry stop affected work rather than trigger replacement.

Recovery is only the established `known-incomplete-v1` mechanism: preserved terminal incomplete status, correct model/provider ID and usage, eligible unrecovered parent, fresh exact linked intent with the frozen correction instruction, original exposure retained. At most two recoveries across the six tasks; no recovery of recovery, no search recovery, no automatic repetition of access failures, and no uncertain-call resubmission. No other semantic failure grants replacement calls; source repairs use the stage's ordinary remaining decisions.

Owner dependencies: approve this exact new request and reuse of the established signing/credential boundary. No additional account connection or recipient is needed. Keep the existing local host available. Customer access, outreach approval, customer feedback, authoritative billing statements and independent correction timing remain separate dependencies for those conclusions. The optional Gmail test remains separately prepared and unselected; this request authorizes no Gmail account access or sending.

## Exact prepared artifacts and commands

Owner entry point: `http://127.0.0.1:43131/` → **Owner operating packet**. Six fresh tasks are visible and gated. Existing three local artifacts remain unchanged.

Directory: `var/portfolio-031/proposal/portfolio-031-integrated-build-v3-r2/`:

- `portfolio.authorization.request.json`: unsigned outer/inner grant, six frozen task definitions and caps.
- `initial-responses-body.json`, `initial-count-body.json`: readable complete first request and token-count projection.
- `initial-responses-bytes.json`, `initial-count-bytes.json`: canonical request bytes, with no credentials.
- `task-manifest.json`: actual task definitions, source identities and future-stage context rules.
- `payload-audit.json`: hashes, serialized bytes and explicitly unknown tokens/access.

Exact proposal SHA-256: `4e6e7dd6807837e8032e3bf371f15d056a182e3dcd0f63ce6322c399a0123da1`.
Implementation hash: `6e0cfe78ecf1fc02ca95fbc94067601893112501015bb8211b15a43adbefe782`.
First body hash: `1e1c5d3c7a9ff543e71ea9f9108634e29e1af7403d4a279e1667f7e3fd6860d9` (14,041 serialized bytes, **not tokens**).

The earlier unsigned `integrated-build-v3` and `integrated-build-v3-final` candidates are also preserved. Their code bindings are superseded by serialization/audit-label and worker-visible ordinary-budget corrections. None was signed or executed. The exact initial request now reports eight ordinary investigation decisions, excluding the two reserved recovery slots. Controller-added rejection cancellations are recorded separately from the raw model proposal. Later request bytes necessarily depend on actual research/tool responses and artifacts; their assembly contracts and task scopes are frozen, and exact bytes are persisted before each admission.

From `C:\Users\14844\Downloads\MIDAS\var\foundry-worktree-031`:

```powershell
# Safe now: no credential read or provider request.
node packages/foundry/src/portfolio/cli.ts preflight --root var/portfolio-031

# ONLY after exact owner approval; the assistant supplies that approval's real reference.
node packages/foundry/src/portfolio/cli.ts sign-proposal --root var/portfolio-031 --proposal var/portfolio-031/proposal/portfolio-031-integrated-build-v3-r2/portfolio.authorization.request.json --approve-proposal-hash 4e6e7dd6807837e8032e3bf371f15d056a182e3dcd0f63ce6322c399a0123da1 --approval-reference "ACTUAL_OWNER_APPROVAL_REFERENCE" --principal Mason --owner-public-key C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/owner.pub --owner-private-key C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/owner.key
node packages/foundry/src/portfolio/cli.ts preflight --root var/portfolio-031 --live
node packages/foundry/src/portfolio/cli.ts run --root var/portfolio-031 --live --task quote-desk/investigate-v3
node packages/foundry/src/portfolio/cli.ts run --root var/portfolio-031 --live
node packages/foundry/src/portfolio/cli.ts report --root var/portfolio-031 --live
```

Run the first assignment before draining to inspect transport/accounting. Continue the covered chain without another spending approval when usable. If the model rejects, cancelled descendants cannot run. If a generated next task is attempted, the frozen grant denies it before provider transport; it remains an unexecuted proposal. Already persisted results resume without another request. Known-incomplete recovery requires `recover-incomplete --live --task <same-task> --parent <exact-preserved-attempt>` before resuming; the assistant resolves identities from the ledger rather than the owner inventing them.

The local UI can remain open during CLI execution; refresh shows persisted work. Its mock-mode server cannot launch grant-gated tasks. Switching it to `serve --live` after signing is optional; avoid a duplicate listener. No historical result needs rerunning.

## Verification and inference limits

New integrated tests execute a blank workspace, a deliberately injected mock-authored faulty implementation, an actual failed Chromium check, a mock repair, actual product review/change, full software readback into the operating deliverable, and a subsequent priority decision. That sequence uses 21 mock decisions and real local tool effects. A second test verifies rejection cancels four downstream jobs with zero admissions and does not mutate the preserved model response. The fixture source comes from the development assistant; this is wiring evidence, not autonomous construction or natural-error frequency.

All 62 portfolio tests and 42 relevant earlier operating regressions passed; strict TypeScript 6.0.3 semantic checking reports zero errors across 30 source modules. Focused provider tests verify frozen task caps, changed-definition rejection and unlisted generated-task denial using mock transport that cannot fall through to OpenAI. Existing tests cover uncertainty, recovery, concurrency, retained exposure and billing boundaries. Browser verification confirms the new owner packet, six gated zero-attempt tasks, three unchanged artifacts and no page errors. Independent human semantic review, actual project access for this new job, measured correction time and customer/business outcomes remain unmeasured.

The useful decision after execution is whether the existing strong baseline can turn research into a checked local product and usable operating packet within this bounded surface. If it can, prefer that sufficient baseline and seek owner/customer evidence before expanding it. If it cannot, classify the observed source, reasoning, tool, contract or delivery failure and propose the smallest evidence-based correction. No specialist comparison is released by this packet, and no result proves optimal team assembly or general autonomous business success.
