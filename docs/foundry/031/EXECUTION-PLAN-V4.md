# Mission 031 — outcome preparation, revision 4

Integration owner: /root. Starting implementation: clean `686fe1786ae06a4ab199830225f3f834cc275de6`. The owner authorized reversible engineering, public read-only research and delegated development work. The $51.82 packet remains unsigned; no provider/count calls, credential access, external sending or deployment are permitted during preparation.

## Decision and strongest alternative

A capable development agent could build this small application directly, probably with less setup and more flexible browser access. MIDAS should not claim an advantage merely because it reproduces that application through a loop. The incremental experiment is a persisted, scoped worker executing from its own evidence and source: real feedback, budget-aware repair, authoritative review, usable local delivery, explicit stop decisions and a next-task plan. The same profile boundary, evidence handling and observation path must make the next different assignment cheaper to integrate. A second management layer, forced specialist or repetitive report would not advance this decision and will not be added.

## Inspected gaps and work

1. **Relevant evidence / root + owner_portfolio_audit.** Replace the prospective starting corpus with directly retained quote/job workflow and substitute evidence; retain old summaries and founder history but exclude irrelevant/stale inputs from the new snapshot. Distinguish original excerpts, vendor/user claims, founder reports and assistant interpretation. Current authority comes from trusted execution context, never an old source or retrieved instruction.
2. **Explicit product profile / root.** The v1 checker relies on undocumented selectors and exact strings and does not exercise invalid input, reopening or narrow layouts. Add a versioned, worker-visible v2 contract and meaningful independent journey checks. Keep v1 unchanged for historical consumers. Retain source/current hashes in context to avoid paying for redundant reads.
3. **Actionable observation / root.** Return bounded rendered text, control/accessibility information, geometry, errors and actual journey feedback through existing `check.run`; record screenshots for development/owner inspection with manifest binding. Model receives text/DOM observations, not vision, unless an explicitly supported future image route is added. No extra model helper.
4. **Call-aware execution / root.** Preserve 8/1/10/6/6/1 ordinary allocations, two held recoveries and two hosted searches. Show accurate remaining capacity and completion obligations. Leave enough decisions for write/check/correct/recheck/publish/complete. Allow adequate review unchanged; reject false completion. Freeze new tasks, procedures, sources, schemas and code under a new unsigned packet.
5. **Owner journey / owner_ui_030.** Make product, reason, status, review changes, money and next action immediately understandable using actual persisted fields. Preserve multi-venture backlog. Verify desktop/narrow layouts and browser/backend refresh behavior with isolated test mutations.
6. **Independent critique / portfolio_runtime_audit.** Review exact request/schema/local rules and handoffs for hidden requirements or an accepted but useless result. Root integrates material findings and runs focused fault/regression tests and semantic typechecking.
7. **Runnable handoff / root.** Show the strengthened development example and packet in the owner workspace, label provenance, preserve historical artifacts and grants, commit locally, then request one exact final approval. Actual runtime product authorship and commercial evidence remain unmeasured until authorized execution and later real observations.

## Continuation and stops

The research may support a commercial prototype, a bounded engineering prototype answering a named execution/product question, or a specific rejection. Missing interviews alone do not prohibit useful engineering; a supported profile alone does not justify it. The model's explicit decision governs paid descendants. Unsupported alternatives may be entered as scoped backlog work, not silently substituted into this grant. Product acceptance includes a usable first-time journey, safe money, input feedback, persistent consistent state, understandable empties/status and narrow layout; semantic usefulness and independent human timing retain their actual provenance.

Completion checkpoint: the relevant retained corpus, visible V2 product contract, connected V4 task path, usable developer preview, owner interface, exact initial-request binding and durable continuation are implemented. The full portfolio suite passed 104 tests, the operating regressions passed 42, and strict semantic typechecking passed. Actual owner image/toolbar operations and backend restart were verified on an isolated consistent copy of the main state. See COMPLETION-031-V4.md for the final binding and evidence. Provider activity: zero; paid runtime authorship remains awaiting one exact final approval, not claimed as completed.
