# Ready-to-approve Mission 031 V4 R3

The durable execution repair is complete locally. No new live result exists. The engineering experiment remains incomplete until the linked decision and applicable downstream workflow execute successfully or produce a supported rejection.

Exact unsigned proposal hash:

`af14b710d2b1ed96f10cd4cdef28bdccbe2025254ba8b5d6ea814052b6663bc2`

Implementation binding: `a0fb095e12028ce547827ffc381bd372ebb02076c7e25afd9ffba6790c1e26b6`.

Implementation commit: `52e40ba` on `codex/portfolio-durable-responses-v4-031`, based on preserved R2 `94434228144f73790ec4ec125337516af81e8247`.

Working directory: `C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-031-durable-r3`.

Prepared root: `var/portfolio-031-continuation-r3`. Its immutable proposal is under `proposal/portfolio-031-value-build-v4-r3/portfolio.authorization.request.json`. A reviewable identical copy, exact initial Responses/count bytes, task definitions and payload audit are preserved in `docs/foundry/031/continuation-r3/`.

## Decision requested

Approve this exact R3 packet, up to **$30.75 additional exposure plus the existing $15.07 retained once, $45.82 combined**, including explicit provider `background:true, store:true`, bounded same-ID GET retrieval, the fresh decision linked to the unknown R2 attempt, use of the existing protected signing/credential mechanism, and retirement of R2 execution authority while preserving all records. All other exclusions remain in force. No signing or provider execution has occurred under R3.

The 25 new inference/count ceiling consists of 24 ordinary calls (decision 1; implementation/check/repair 10; review/correction 6; operating package 6; reassessment 1), plus one held known-incomplete recovery. No hosted search or additional research purchase. The accepted 17,908-byte investigation is reused unchanged; the prototype begins blank if the model's real build decision justifies proceeding. There is no substitute development-authored product in the paid path.

Background creation uses a 60-second acknowledgement deadline, 15-second GET deadline, five-second polling interval, 15-minute automatic completion window, and same-ID recovery bounded to 24 hours and grant expiry. At most 240 GETs per response / 6,000 total. Three consecutive transient read errors stop; access/not-found errors are not blindly repeated. Expiry remains **2026-09-25 22:00 UTC**. Model/settings remain **gpt-6-astra / max / default**, input 32,768, output 16,384 including reasoning, $1.23 maximum inference reservation, concurrency one.

**Material change:** provider response application state is now explicitly stored, with current documentation describing retention of at least 30 days. ZDR can override storage; project compatibility is unverified and a returned storage mismatch stops execution. The old R2 response has no saved ID and cannot be retrieved from the available records. New initial-acknowledgement loss remains unknown and is never resubmitted. Local deadlines do not cancel remote work. The same $4.00 uncertainty buffer covers unpriced counts/retrievals; GETs are not asserted free and the local cap is not a billing guarantee. See [repair and official-source decision](DURABLE-RESPONSES-R3.md).

## Commands

These commands are prepared for the integration owner. **Do not run signing or `--live` commands before exact approval.** The approval reference must describe the actual future owner approval; the placeholder is not an approval.

```powershell
Set-Location C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-031-durable-r3

# Offline status; no credential or provider access.
node packages/foundry/src/portfolio/cli.ts status --root var/portfolio-031-continuation-r3

# Only after exact approval: public/private keys are read by the established signing mechanism,
# never printed or copied. This also retires both R2 execution grants to the exact signed R3 identity.
node packages/foundry/src/portfolio/cli.ts sign-proposal --root var/portfolio-031-continuation-r3 --proposal var/portfolio-031-continuation-r3/proposal/portfolio-031-value-build-v4-r3/portfolio.authorization.request.json --approve-proposal-hash af14b710d2b1ed96f10cd4cdef28bdccbe2025254ba8b5d6ea814052b6663bc2 --owner-public-key C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/owner.pub --owner-private-key C:/Users/14844/Downloads/MIDAS/var/foundry-worktree-028/var/foundry-smoke-028/auth/owner.key --principal Mason --approval-reference "ACTUAL_OWNER_APPROVAL_OF_THIS_EXACT_R3_PACKET"

# Signed preflight does not dispatch. Run drains only covered eligible tasks.
node packages/foundry/src/portfolio/cli.ts preflight --root var/portfolio-031-continuation-r3 --live
node packages/foundry/src/portfolio/cli.ts run --root var/portfolio-031-continuation-r3 --live
node packages/foundry/src/portfolio/cli.ts report --root var/portfolio-031-continuation-r3 --live

# Following interruption: local recover first; ordinary run also performs these recovery gates.
node packages/foundry/src/portfolio/cli.ts recover --root var/portfolio-031-continuation-r3
# Optional exact-task GET-only recovery; requires a saved response ID and still-valid grant.
node packages/foundry/src/portfolio/cli.ts resume-response --root var/portfolio-031-continuation-r3 --task quote-desk/decide-v4-r3 --live

# Read-only owner workspace; disabled model means this server cannot spend.
node packages/foundry/src/portfolio/cli.ts serve --root var/portfolio-031-continuation-r3 --port 43139
```

The one held recovery is prepared only when the established `recover-incomplete` validator finds an authenticated, known-incomplete response. It retains that failure and reservation and binds a fresh attempt to the exact parent request. It is unavailable for missing identities, uncertain outcomes, access failure, or task-cap exhaustion. All accepted terminal outputs resume without another inference. No remote cancellation/deletion is included.

If completed, the workspace will expose the runtime-authored local application, actual browser observations, authoritative reviewed source, operating package and reassessment. If rejected, it will retain the decision/evidence and cancel dependent spending. If blocked, it will preserve the precise step, source version, response identity if known, diagnostics and accounting. No interview, customer contact or deployment is a prerequisite for the engineering experiment.

## Existing evidence and limitations

Local verification: 106 focused tests passed; strict installed TypeScript semantic check passed; actual local browser verification passed. Delayed completion, lost acknowledgement, lost GET, real process termination/restart, expiry, malformed output, scoped concurrency, stale bindings, preserved finalization and downstream artifact transfer are covered. The full offline positive path used nine mocked inference admissions and ten mocked GETs; the rejection path stopped after one. Mock outcomes are not live compatibility or competence evidence.

Historical accounting remains nine inference/count admissions, $11.07 inference reservations and the single $4.00 uncertainty buffer. Provisional token-based costs $3.84; no authoritative settlements recorded; timed-out R2 usage and actual charges unknown. New provider/count/GET requests in this preparation: **zero**. Independent human effort, development subscription cost and engineering labor cost are unmeasured. The prepared operator interview remains separate and unsent.

The next owner decision is the exact approval above. No additional account setup is assumed complete, no new account access has been used, and no broader infrastructure or commercial execution is proposed.
