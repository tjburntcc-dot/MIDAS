# Mission 031 — value-gated execution, revision 4

Status: local implementation and evidence preparation; the final exact unsigned binding is recorded in COMPLETION-031-V4.md after verification. No paid execution is approved by this document. Original V3 proposals, sources and observations remain preserved.

## What is useful now

Open **http://127.0.0.1:43131/**. The home page leads with saved products, execution status and the owner decision. Choose **Quote desk — development preview v4 → Try it locally**. It is a functioning local quote/job application, authored by the development assistant and checked in Chromium. It starts with no customer records. Create a clearly identified example, save it, reopen and edit it, convert it to a job, mark the job completed, refresh and export CSV. Its SQLite state survives a fresh managed browser and a restart of this local server.

The preview is a separate task and immutable delivery. It is neither a live-model result nor the seed for the proposed paid build. All six new runtime tasks remain unexecuted. The live build starts from the documented blank `app.html`; the runtime must choose and author its source through recorded edits. No expected implementation, reference app, answer key or customer-validation claim enters that context.

The main workspace also retains the other ventures and their backlogs. This grant is limited to one assignment; the portfolio is not limited to one business. A new plan may persist further tasks, but their absence from the frozen task list prevents provider admission.

## The actual capability, rather than its name

| Action | Executable connection | Material boundary |
| --- | --- | --- |
| Investigate a business question | `value-release-v4.ts` supplies the seven decision questions; `engine.ts` assembles pinned evidence; `evidence.ts` implements `research.read` and bounded public `research.fetch`; `search.ts` meters optional hosted search | Public sources and owner-supplied facts only. Search output is model analysis; a fetched excerpt is separately retained publisher material. No account research, customer contact or inferred willingness to pay. |
| Decide to build, reject or recommend another direction | `planning.ts` validates the planning output; `engine.ts` persists the actual decision and applies the declared prototype gate | Non-prototype decisions cancel the four paid descendants. A different product can enter the backlog but cannot replace this paid assignment. |
| Create and allocate tasks | `core.ts` stores dependencies, priorities, capability requirements, workers and limits; `engine.ts` admits eligible tasks | Rule-based scheduling and capability matching. Role titles are not measured competence; optimal team construction is unproved. |
| Implement and modify a product | `task-preparation.ts` creates blank software; `tools.ts` performs scoped, hash-checked source replacement | A self-contained browser application within `quote-to-job-v2`. No arbitrary host commands, package installation, server implementation, authentication, payments or deployment. The runtime actually controls source content, rather than choosing a fixed generated template. |
| Run and test it | `product-check-v2.ts` executes the real submitted source in Chromium with the bridge/state handler from `preview.ts` | Fixed, disclosed journey checks, rendered text, controls, geometry and errors. Screenshots are retained for owner/development inspection. The text-only runtime does not receive images or perform visual model review. |
| Repair and review | `engine.ts` carries current source, hashes, actual failures, full relevant delivery and revision observations; `workspace.replace`, `check.run`, `artifact.publish_local` update the authoritative version | A substantive repair must recheck. A good artifact may pass unchanged. Changed source invalidates dependent checks/publication; authoritative artifact changes stale consumers. Review is a strong ordinary baseline, not independent human validation or a specialist comparison. |
| Deliver usable service work | `operating-profile.ts` checks and renders the bound operating packet; `tools.ts` publishes it; `server.ts` exposes the actual product/report | Product instructions, demonstration, contrary evidence, assumptions, interview guide, unsent wording, economics and owned obligations remain visible. Local publication is not sending or commercial acceptance. |
| Respond to outcomes | Portfolio observations feed an explicit reassessment; the final `adapt-v4` call receives the real operating result and proposes the next tasks | Proposals and observations retain provenance. No automatic conversion of task completion into demand, revenue, worker qualification or business success. |

A capable development agent could build this small app directly with less setup and more flexible tools. The reason to run MIDAS is to test the reusable connection from evidence and a consequential decision through its own source edits, actual tool feedback, bounded repair, authoritative delivery and a persisted next action. No claim of lower cost or better quality than that alternative has been measured. Pure ceremony, forced revisions, additional management roles and a specialist comparison were removed from this packet.

## Starting evidence and the decision worth making

The new source bundle retains nine small exact excerpts, each with URL, observed date, source type and separate development-assistant interpretation. Sources cover Jobber quote approval/conversion/import, Housecall Pro estimate conversion, Square estimates, competent spreadsheet substitutes and three public operator reports. Historical Astra, Checkly and Feedly preparation remains in the library but is not selected for V4. The exact selected source IDs and hashes are in the task manifest. A separately labeled founder-context restatement distinguishes historical facts from current controller authority; it is not represented as a verbatim founder quote.

The evidence weakens a generic tracker’s commercial case: capable established products and basic tools already cover much of the job. Scope fidelity and duplicate entry are more consequential hypotheses, but the current profile does not implement approved-scope snapshots or change lineage. The runtime must not claim to solve that stronger problem merely by building a calculator and status list. See [research synthesis](research-v4/synthesis.md), [retained excerpts](research-v4/sources.json) and [source verification](research-v4/VERIFICATION.md).

Investigation must answer who would use it, the exact job, present substitutes, switching advantage, access/distribution, supported/contradicted/unknown assumptions and what the bounded prototype could establish. It can use the supplied evidence without purchasing redundant discovery. A retrieval requires a decision-relevant uncertainty. Public anecdotes are neither a representative sample nor leads; forum rules do not permit solicitation merely because a post is public.

The build decision may have a **commercial** rationale or a named **engineering** rationale. Missing interviews alone do not cancel a useful engineering test. The presence of an adapter or unused budget does not justify a build. An unsupported alternative is a separate future assignment, not a silent substitution. A rejection must preserve the evidence, specific reason and next executable recommendation while cancelling dependent spending.

## What was repaired before freezing

1. **Visible product contract.** V2 declares selectors, state and persistence shapes, integer-money rules, input/record limits, native-form restrictions, source handoff bounds and every mandatory browser check. The old profile remains available for its historical consumers.
2. **A real complete journey.** Tests exercise invalid fields without state mutation, multiline arithmetic, save/reopen/edit without a duplicate ID, two records, job transitions, CSV quoting/formula-prefix handling, refresh/fresh browser state, narrow controls and hostile bridge messages. Mobile checks require visible controls, not an empty set that vacuously passes.
3. **Actionable bounded observations.** The worker receives source and current hashes without redundant file reads, the most recent meaningful tool results and actual rendered/DOM feedback. Complete observations, screenshots, source versions and failed checks remain persisted. Successive review/report stages receive the actual relevant source and authenticated delivery.
4. **Honest handoff limits.** V4 allows up to 72,000 local context bytes, versus the historical 56,000; the provider input limit remains 32,768 tokens and must be admitted by an actual count. Software `JSON.stringify(files)` is at most 24,000 UTF-8 bytes. The operating report is at most 9,000 raw UTF-8 bytes so it can travel alongside source/check evidence; ordinary briefs allow 18,000. Oversize content is rejected explicitly, not silently accepted and lost. Byte lengths are not reported as token counts.
5. **No lost report fields.** The operating contract specifies where every requested asset belongs, including observed checks and missing evidence; its renderer preserves owned obligations and direct trusted product links. New arbitrary JSON fields cannot masquerade as delivered sections.
6. **Admission and authority.** Ordinary remaining capacity excludes the held recovery slots. Unlisted backlog jobs are filtered before task claims. A failed local source read becomes a known tool failure, not an invented uncertain provider effect. V4 additionally binds the reviewed first request’s canonical body/schema hashes before provider admission. Later requests depend on actual outputs under the frozen assembly rules.
7. **Usable owner controls.** Saved products and status lead the dashboard. The contained preview queues ordinary keystrokes rather than dropping input during screenshot updates and uses the actual narrow viewport instead of shrinking a desktop image. Checks and screenshots do not write example customers into the owner’s state. A credential-free, read-only view of the signed grant and persisted ledger displays actual admissions, count dispatches, provisional estimates, authoritative settlements and retained exposure even when the UI cannot execute models. It never substitutes those numbers for business revenue or human effort; an unverifiable ledger displays unavailable rather than zero.
8. **Interrupted work is distinct from another attempt.** A durable continuation can reacquire a fenced lease for the same logical task after exact saved model/tool results are reconciled. It does not reset `attempts`, admit another copy of a saved request, release reservations or expand the signed known-incomplete recovery policy. An unresolved response remains blocked.

These changes do not add a scheduler, extra inference stages or a second orchestration system. Exact implementation and code review evidence is in [CONTRACT-REVIEW-V4.md](CONTRACT-REVIEW-V4.md); that critique is historical preparation, while final test results belong in the completion receipt.

## Frozen work and decisions per task

| Stage | Ordinary admissions | Useful completion path / decision |
| --- | ---: | --- |
| Investigation | 8 | Target consequential unknowns; retain useful evidence; write, check, publish and complete a source-bound brief. A hosted search selection consumes one ordinary decision plus its separately metered search admission. |
| Build-or-stop | 1 | One complete structured decision. `quote-to-job: prototype` must have the stated rationale; other choices cancel the paid build descendants. |
| Implementation, tests and repairs | 10 | Typically write → check → justified repair/recheck if needed → local publish → complete. Current code is already supplied; no ritual reads. |
| Product review and correction | 6 | Review actual source/check evidence; correct only a substantive defect; check → publish → complete. Unchanged acceptance is valid. |
| Operating deliverable | 6 | Write the exact reviewed-product package, check, repair only when needed, publish, complete. |
| Reassessment | 1 | Persist the supported next decision and scoped tasks using actual observations. No automatic execution of those tasks. |
| Held linked recovery | 2 total | Only the existing known-incomplete protocol; not ordinary exploration, task-quality retries or access probes. |
| Hosted search | 2 total | One permitted native search tool call per separate inference; no search recovery. |

All ordinary responses choose one action. Writing, checking, publication and completion each consume a decision; tools do not become free calls because they are local. Repair uses remaining ordinary capacity. No task borrows another task’s allowance. Each upcoming task sees its remaining capacity and required finish path.

## One proposed envelope, unchanged at $51.82

| Item | Maximum | Reserved exposure |
| --- | ---: | ---: |
| Primary ordinary work | 32 inferences | $39.36 |
| Held linked recoveries | 2 inferences | $2.46 |
| Hosted search | 2 inferences with at most one native search each | $6.00 |
| Supporting token counts | 36 total, at most one per admitted inference | $4.00 unpriced uncertainty allowance |
| **Total** | **36 inferences + 36 counts** | **$51.82** |

Direct OpenAI Responses, exact route `gpt-6-astra`, `reasoning.effort=max`, `service_tier=default`, complete admitted input at most 32,768 tokens, output at most 16,384 including reasoning, inference deadline 180 seconds, counting deadline 10 seconds, concurrency one, expiry **September 25, 2026, 22:00 UTC**. No fallback or automatic retry. Model alias availability is not an immutable model revision; this is exploratory execution, not a protected final comparison.

Official public documentation was checked September 12, 2026: [Astra capabilities/settings](https://developers.openai.com/api/docs/models/gpt-6-astra), [pricing](https://developers.openai.com/api/docs/pricing), [Responses token count](https://developers.openai.com/api/reference/typescript/resources/responses/subresources/input_tokens/methods/count). Standard short-context input is $10/M, cache writes $12.50/M and output $50/M; this reservation conservatively uses the cache-write input rate. `ceil((32768 × 1250 + 16384 × 5000) / 1000000) = 123` USD cents per ordinary call. Search reserves 300 cents, above its disclosed worst-case calculation of 288 cents with the 131,072-token search-context uncertainty allowance and one tool call. The $4 count buffer is not a published count price. A local ledger cannot guarantee provider billing limits.

The exact ordinary body uses the Responses structured-output schema and all relevant context. The token-count projection retains the documented input-bearing fields; unsupported generation/output-selection fields are excluded. Hosted search has its own complete frozen tool body and bounded projection, verified against the [web-search documentation](https://developers.openai.com/api/docs/guides/tools-web-search). No count was bought to estimate the packet, and no guessed character count substitutes for token admission.

Project: `proj_H01ORqdOPQM6vdGwQYsqFL5r`. The proposed credential is the already approved protected file; it is not copied or read during preparation. It must still have current project permissions, Responses/count access, Astra/search access and API billing. Historical access and a public model page do not establish that access today. The first authorized admission is the access check; there is no preliminary hidden probe. Access failures are not automatically repeated.

Two fresh linked recoveries are eligible only after preserved terminal `incomplete` status, expected model/provider identity, valid usage and provisional accounting under `known-incomplete-v1`. The original reservation is retained. The new identity, parent request hash and frozen recovery instruction are recorded. No recovery-of-recovery, search recovery or uncertain-response resend is allowed. A lost/uncertain completion remains blocked for reconciliation; neither waiting nor unused allowance authorizes replay. Material shared contract/authority defects stop affected dispatch and require a newly bound version before further affected requests.

Billing settlements require actual provider/project evidence and its authorized attestation. A signature authenticates the statement, not the factual accuracy of underlying bills. Provisional token estimates, retained exposure and authoritative settlements remain separate. Missing billing evidence does not erase an observed software result, but prevents definitive cost qualification. Founder labor and independent correction timing remain unknown until measured.

## Local commands and covered continuation

Run from `C:\Users\14844\Downloads\MIDAS\var\foundry-worktree-031`:

```powershell
# Current local owner entry point. No provider model is implicit or dispatched.
node packages/foundry/src/portfolio/cli.ts serve --root var/portfolio-031 --mock --port 43131

# Status and safe recovery of already persisted results, without provider transport.
node packages/foundry/src/portfolio/cli.ts preflight --root var/portfolio-031
node packages/foundry/src/portfolio/cli.ts status --root var/portfolio-031

# Completed development-preview preparation is idempotent and read-only on rerun.
node packages/foundry/tools/prepare-value-v4.mjs --root var/portfolio-031
```

The final proposal directory is `var/portfolio-031/proposal/portfolio-031-value-build-v4/`. It contains the unsigned outer/inner grant, exact initial request/count bytes, readable versions, selected source/task manifest and payload audit. The completion receipt records its final SHA-256 and exact post-approval signing command. Do not sign the old V3 hash for this changed work.

After exact approval and signature, the assistant runs the first investigation, verifies transport/accounting, then continues the covered chain without individual spending approvals:

```powershell
node packages/foundry/src/portfolio/cli.ts preflight --root var/portfolio-031 --live
node packages/foundry/src/portfolio/cli.ts run --root var/portfolio-031 --live --task quote-desk/investigate-v4
node packages/foundry/src/portfolio/cli.ts run --root var/portfolio-031 --live
node packages/foundry/src/portfolio/cli.ts report --root var/portfolio-031 --live
```

Resuming a complete stored result does not dispatch again. A known incomplete parent uses the existing `recover-incomplete --live --task <preserved-task> --parent <preserved-attempt>` mechanism only after eligibility inspection. The assistant resolves those exact recorded identities; the founder does not invent them. The UI can remain open in its model-disabled mode while the CLI executes.

No new account connection, Gmail recipient or customer action is needed for this bounded local run. The separately prepared Gmail test remains unselected. The owner need only approve the exact packet and keep the local host available; subsequent buyer access, customer contact, deployment and purchases remain separate authority. Any subjective owner/customer acceptance question will use the actual finished product, not a fabricated signature or review time.

## What different outcomes change

- **Useful product and adequate baseline:** retain the single strong baseline; use the real product to seek one consenting operator’s comparison with their current competent process under separately approved access. Do not infer general reliability or demand.
- **Useful rejection:** preserve contrary evidence, cancel dependent spending and prepare the smallest executable alternative as ungranted backlog work.
- **Consequential runtime weakness:** distinguish source/reasoning, missing evidence, tool/contract limitation and delivery failure. Keep actual failed output. Use ordinary repair capacity when allowed; any future procedure comparison needs a supported mechanism and fresh/regression evaluation.
- **Shared execution defect or uncertain completion:** preserve exposure and observations, stop the affected paid path, complete supported offline repair and disclose the changed version. No silent replay.
- **Commercially inconclusive:** do not buy model calls to manufacture willingness to pay. The missing evidence is a real operator’s workflow and switching behavior, plus founder acquisition/delivery effort.

Offline scripted outputs prove the connected machinery and failure boundaries only. They do not establish model competence, learned team assembly, specialist superiority, customer validation, profitable economics or autonomous business creation.
